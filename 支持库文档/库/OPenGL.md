# OPenGL支持库

- **库标识**: `OPenGL`
- **版本**: 2.0
- **GUID**: `F05D3E4CE9E84d0f82332D62BAF6447F`
- **作者**: 大连大有吴涛易语言软件开发有限公司
- **主页**: http://www.eyuyan.com
- **文件**: `OPenGL`
- **可用命令数**: 126

本支持库实现了对OPenGL的支持。可以进行OPenGL的坐标变换、建模、测试运算、缓存、效果、纹理、显示列表、光栅、文字、特殊模型、载入三维模型文件、交互操作的操作。

## 数据类型

| 名称 | 说明 |
|---|---|
| `像素格式` |  |

## 命令

### 选择像素格式

> 英文名 `ChoosePixelFormat` ｜ 返回值 **整数型**

为指定的设备选择一个匹配的像素格式。成功返回像素格式号，失败返回0。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `设备句柄` | 整数型 | - | 需要取得像素格式号的设备句柄。 |
| 2 | `像素格式` | 库类型#1 | - | 为指定的设备选择的像素格式。 |

### 置像素格式

> 英文名 `SetPixelFormat` ｜ 返回值 **逻辑型**

使用像素格式号为指定的设备设置像素格式。成功返回真，失败返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `设备句柄` | 整数型 | - | 需要设置像素格式的设备句柄。 |
| 2 | `像素格式号` | 整数型 | - | 与指定的设备相匹配的像素格式号 |
| 3 | `像素格式` | 库类型#1 | - | 为指定的设备选择的像素格式。 |

### 创建着色描述表

> 英文名 `wglCreateContext` ｜ 返回值 **整数型**

为设备创建着色描述表，成功返回着色描述表号，失败返回0。在不用的时候必须删除着色描述表。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `设备句柄` | 整数型 | - | 需要创建着色描述表的设备句柄。 |

### 置当前着色描述表

> 英文名 `wglMakeCurrent` ｜ 返回值 **逻辑型**

设置当前的着色描述表，成功以后所有OPenGL命令都作用在指定的设备上。成功返回真，失败返回假。在不使用着色描述表的时候要释放当前着色描述表。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `设备句柄` | 整数型 | - | 需要设置着色描述表的设备句柄。0表示释放当前着色描述表。 |
| 2 | `着色描述表号` | 整数型 | - | 需要设置的着色描述表，0表示释放当前着色描述表。 |

### 删除着色描述表

> 英文名 `wglDeleteContext` ｜ 返回值 **逻辑型**

删除当前的着色描述表，解除OPenGL命令对指定的设备的作用。成功返回真，失败返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `着色描述表号` | 整数型 | - | 需要删除的着色描述表。 |

### 取任意设备句柄

> 英文名 `gethdc` ｜ 返回值 **整数型**

可以取得任意一个窗口组件的设备句柄，成功返回着色描述表号，失败返回0。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `窗口句柄` | 整数型 | - |  |

### 显示光标

> 英文名 `ShowCursor` ｜ 返回值 **整数型**

指定当前应用程序是否显示光标，也可以用来判断当前的光标是否显示，返回值大于0表示正在显示，返回值小于0表示正在隐藏。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `是否显示` | 逻辑型 | - | 真表示显示，假表示隐藏光标 |

### 释放设备句柄

> 英文名 `ReleaseDC` ｜ 返回值 **整数型**

释放由调用“取任意设备句柄”命令获取的指定设备场景，执行成功为1，失败为0。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `窗口句柄` | 整数型 | - |  |
| 2 | `设备句柄` | 整数型 | - | 需要释放的设备句柄。 |

### 矩阵复位

> 英文名 `glLoadIdentity`

让当前选中的坐标系回到原点。

### 选择矩阵模式

> 英文名 `glMatrixMode`

选择接下来将要进行操作的坐标矩阵。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `矩阵模式` | 整数型 | - | 参数值可以为以下常量：<br>	5888、#观察矩阵，用来建模的坐标矩阵；<br>	5889、#投影矩阵，用来进行投影变换的坐标矩阵；<br>	5890、#纹理矩阵，用来改变纹理坐标的坐标矩阵。 |

### 坐标平移

> 英文名 `glTranslate`

平移当前坐标矩阵。在矩阵复位以前，将影响接下来的坐标操作。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `横向位置` | 双精度小数型 | 有默认值 | 默认情况下水平向右为正数，向左为负数，如果坐标已经变换，那么在上一次坐标系的基础上继续叠加。 |
| 2 | `竖向位置` | 双精度小数型 | 有默认值 | 默认情况下竖直向上为正数，向下为负数，如果坐标已经变换，那么在上一次坐标系的基础上继续叠加。 |
| 3 | `纵向位置` | 双精度小数型 | 有默认值 | 默认情况下屏幕前方为正数，后方为负数，如果坐标已经变换，那么在上一次坐标系的基础上继续叠加。 |

### 坐标旋转

> 英文名 `glRotate`

转动轴为从原点指向参数点的一个向量。在矩阵复位以前，将影响接下来的坐标操作。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `角度` | 双精度小数型 | - | 使用角度单位 |
| 2 | `横向位置` | 双精度小数型 | - | 默认情况下水平向右为正数，向左为负数，如果坐标已经变换，那么在上一次坐标系的基础上继续叠加。 |
| 3 | `竖向位置` | 双精度小数型 | - | 默认情况下竖直向上为正数，向下为负数，如果坐标已经变换，那么在上一次坐标系的基础上继续叠加。 |
| 4 | `纵向位置` | 双精度小数型 | - | 默认情况下屏幕前方为正数，后方为负数，如果坐标已经变换，那么在上一次坐标系的基础上继续叠加。 |

### 坐标缩放

> 英文名 `glScale`

缩放当前坐标矩阵。在矩阵复位以前，将影响接下来的坐标操作。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `横向位置缩放` | 双精度小数型 | 有默认值 | 默认情况下水平向右为正数，向左为负数，绝对值大于1表示放大，绝对值小于1表示缩小，如果坐标已经变换，那么在上一次坐标系的基础上继续叠加。 |
| 2 | `竖向位置缩放` | 双精度小数型 | 有默认值 | 默认情况下竖直向上为正数，向下为负数，绝对值大于1表示放大，绝对值小于1表示缩小，如果坐标已经变换，那么在上一次坐标系的基础上继续叠加。 |
| 3 | `纵向位置缩放` | 双精度小数型 | 有默认值 | 默认情况下屏幕前方为正数，后方为负数，绝对值大于1表示放大，绝对值小于1表示缩小，如果坐标已经变换，那么在上一次坐标系的基础上继续叠加。 |

### 创建透视投影

> 英文名 `glFrustum`

在屏幕里面建立一个长方体形状的透视投影的三维坐标空间。长方体的前后两面平行于屏幕，左右两面垂直屏幕并竖直，上下两面垂直屏幕并水平。近处物体大，远处物体小。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `最左端` | 双精度小数型 | - | 长方体形状的三维坐标空间的最左端 |
| 2 | `最右端` | 双精度小数型 | - | 长方体形状的三维坐标空间的最右端 |
| 3 | `最底端` | 双精度小数型 | - | 长方体形状的三维坐标空间的最底端 |
| 4 | `最顶端` | 双精度小数型 | - | 长方体形状的三维坐标空间的最顶端 |
| 5 | `最近端` | 双精度小数型 | - | 长方体形状的三维坐标空间的最近端 |
| 6 | `最远端` | 双精度小数型 | - | 长方体形状的三维坐标空间的最远端 |

### 创建正投影

> 英文名 `glOrtho`

在屏幕里面建立一个长方体形状的正投影的三维坐标空间。长方体的前后两面平行于屏幕，左右两面垂直屏幕并竖直，上下两面垂直屏幕并水平。近处物体和远处物体一样的大。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `最左端` | 双精度小数型 | - | 长方体形状的三维坐标空间的最左端 |
| 2 | `最右端` | 双精度小数型 | - | 长方体形状的三维坐标空间的最左端 |
| 3 | `最底端` | 双精度小数型 | - | 长方体形状的三维坐标空间的最底端 |
| 4 | `最顶端` | 双精度小数型 | - | 长方体形状的三维坐标空间的最顶端 |
| 5 | `最近端` | 双精度小数型 | - | 长方体形状的三维坐标空间的最近端 |
| 6 | `最远端` | 双精度小数型 | - | 长方体形状的三维坐标空间的最远端 |

### 设置视口

> 英文名 `glViewport`

设置设备中可以显示的范围。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `左下角横坐标` | 整数型 | - | 必须大于0。如果需要全部显示就设为0 |
| 2 | `左下角竖坐标` | 整数型 | - | 必须大于0。如果需要全部显示就设为0 |
| 3 | `宽度` | 整数型 | - | 必须大于0。如果需要全部显示就设为组件的完整宽度 |
| 4 | `高度` | 整数型 | - | 必须大于0。如果需要全部显示就设为组件的完整高度 |

### 矩阵入栈

> 英文名 `glPushMatrix`

把当前矩阵压入栈顶。

### 矩阵出栈

> 英文名 `glPopMatrix`

退出栈顶矩阵。

### 载入矩阵

> 英文名 `glLoadMatrix`

把当前坐标模式变换成指定的矩阵。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `指定矩阵` | 双精度小数型 | 数组 | 矩阵为一个拥有16个成员的数组，多余部分无效。 |

### 乘入矩阵

> 英文名 `glMultMatrix`

把当前坐标模式再乘以指定的矩阵使它变换成目标的矩阵。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `指定矩阵` | 双精度小数型 | 数组 | 矩阵为一个拥有16个成员的数组，多余部分无效。 |

### 创建对称透视投影

> 英文名 `gluPerspective`

在屏幕里面建立一个四棱台形状的透视投影的三维坐标空间。四棱台的前后两面平行于屏幕，其余四面呈喇叭口形状对称展开。近处物体大，远处物体小。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `仰角` | 双精度小数型 | - | 四棱台形状的三维坐标空间的上下两面张开的角度。角度越大，看到的范围越高，范围在0-180度之间。 |
| 2 | `宽度除以高度的比值` | 双精度小数型 | - | 只有与实际的视口大小相匹配的时候，界面看起来才不会变形。 |
| 3 | `最近距离` | 双精度小数型 | - | 四棱台形状的三维坐标空间距离屏幕最近的面，必须为正数。 |
| 4 | `最远距离` | 双精度小数型 | - | 四棱台形状的三维坐标空间距离屏幕最远的面，必须为正数。 |

### 开始画

> 英文名 `glBegin`

画一组相似图元的开始语句。画不同的图元需要执行不同条数的“设置点”的命令。以“停止画”命令结尾。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `画出模式` | 整数型 | - | 参数值可以为以下常量：<br>	0、#点集，在“设置点”命令所定义的位置画出一个点；<br>	1、#线段，在“设置点”命令所定义的位置上，每隔两个点画一条线段；<br>	2、#闭合线段，将“设置点”的命令所定义的点连成一个首尾相连线段；<br>	3、#连续线段，将“设置点”的命令所定义的点连成一串连续的线段；<br>	4、#三角形，在“设置点”命令所定义的位置上，每隔三个点画一个填充颜色的三角形；<br>	5、#连续三角形，在“设置点”命令所定义的位置上，每三个相邻的点画一个填充颜色的三角形；<br>	6、#扇形三角形，在“设置点”命令所定义的位置上，始终以第一个点为顶点，沿着相邻两个点画填充颜色的三角形；<br>	7、#四边形，在“设置点”命令所定义的位置上，每隔四个点画一个填充颜色的四边形；<br>	8、#连续四边形，在“设置点”命令所定义的位置上，每四个相邻的点画一个填充颜色的四边形，并且每增加两个点就再增加一个四边形；<br>	9、#凸多边形，在“设置点”命令所定义的位置上，沿着围绕所有的点组成一个首尾相连的填充颜色的凸多边形 |

### 停止画

> 英文名 `glEnd`

必须与“开始画”搭配使用，单独使用不会有效果。

### 设置点

> 英文名 `glVertex`

插入到“开始画”和“停止画”中来设置模型的顶点。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `横向位置` | 双精度小数型 | 有默认值 | 默认情况下水平向右为正数，向左为负数，如果坐标已经变换，那么在上一次坐标系的基础上继续叠加 |
| 2 | `竖向位置` | 双精度小数型 | 有默认值 | 默认情况下竖直向上为正数，向下为负数，如果坐标已经变换，那么在上一次坐标系的基础上继续叠加 |
| 3 | `纵向位置` | 双精度小数型 | 有默认值 | 默认情况下屏幕前方为正数，后方为负数，如果坐标已经变换，那么在上一次坐标系的基础上继续叠加 |

### 画矩形

> 英文名 `glRect`

在当前坐标原点指定的深度，使用先前选定的颜色画出一个填充颜色的矩形。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `左上角横坐标` | 双精度小数型 | - |  |
| 2 | `左上角纵坐标` | 双精度小数型 | - |  |
| 3 | `右下角横坐标` | 双精度小数型 | - |  |
| 4 | `右下角纵坐标` | 双精度小数型 | - |  |

### 设置正面

> 英文名 `glFrontFace`

为填充颜色的多边形指定正面的定义方向。当四个手指并拢依次绕着“设置点”命令所定义点的方向握紧时，大拇指所指的方向就是正面的方向。反面即为背面。必须启用“#精选面”才有效。默认为#逆时针。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `模式` | 整数型 | - | 参数值可以为以下常量：<br>	2304、#顺时针；<br>	2305、#逆时针 |

### 设置裁剪平面

> 英文名 `glClipPlane`

裁剪面的背面被减去。必须启用“#裁剪测试”和“#零号裁剪面～#五号裁剪面”，才能执行裁剪，不使用的时候需要关闭。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `平面号` | 整数型 | - | 参数值可以为以下常量：<br>	12288、#零号裁剪面；<br>	12289、#一号裁剪面；<br>	12290、#二号裁剪面；<br>	12291、#三号裁剪面；<br>	12292、#四号裁剪面；<br>	12293、#五号裁剪面 |
| 2 | `平面参数` | 双精度小数型 | 数组 | 参数值为四维数组。这四个数分别为一般平面方程“<br>		A（X - X0)+B（Y - Y0）+ C ( Y - Y0 ) + D = 0”<br>中的A、B、C、D这几个系数。如果D = 0，那么（A，B，C）代表平面的法向量。如果D = -1，那么1/A，1/B，1/C分别代表平面在X，Y，Z轴上的截距。D默认为0。多余部分无效。例如一个平行于XZ平面，正面指向Y正方向的裁剪面就是{0，1，0，0} |

### 设置多边形模式

> 英文名 `glPolygonMode`

当绘出图形为封闭的多边形时才起作用。默认设置正面和背面都为面。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `应用面` | 整数型 | - | 参数值可以为以下常量：<br>	1028、#正面；<br>	1029、#背面；<br>	1032、#正面和背面 |
| 2 | `模式` | 整数型 | - | 参数值可以为以下常量：<br>	6912、#点，将画出的多边形显示为点；<br>	6913、#线，将画出的多边形显示为线段；<br>	6914、#面，将画出的多边形显示为填充颜色的多边形 |

### 设置线型

> 英文名 `glLineStipple`

设置线段的类型。默认使用实线。必须启用“#线型”才有效，不使用的时候需要关闭。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `放大倍数` | 整数型 | - | 用来拉伸线型 |
| 2 | `线型` | 短整数型 | - | 按照循序排列的16位二进制数其中每一位代表一个象素，且从低位开始，1表示用当前颜色绘制一个象素，0表示当前不绘制。 |

### 设置图案填充

> 英文名 `glPolygonStipple`

设置多边形的填充方式。必须启用“#图案填充”才有效，不使用的时候需要关闭。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `图案` | 字节型 | 数组 | 一个拥有128个成员的数组，其中每个字节代表八个位，每一位代表一个象素，最后呈现出一个32×32位的图案。且从低位开始。1表示用当前颜色绘制一个象素，0表示当前不绘制。多余部分无效。 |

### 标记边界线

> 英文名 `glEdgeFlag`

在“设置点”之前使用，指定多边形的该点或者线是否是边界线的一部分。只有当多边形的模式为点或者线的时候才有意义。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `是否为边界线` | 逻辑型 | - | 真表示属于多边形，需要显示，假表示不显示。 |

### 矩形剪切

> 英文名 `glScissor`

定义一个矩形剪切框。裁剪框内的内容被保留下来，其余被减掉。必须启用“#裁剪测试”才有效，不使用的时候需要关闭。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `左下角横坐标` | 整数型 | - | 相对于显示设备的左下角，以像素为单位 |
| 2 | `左下角纵坐标` | 整数型 | - | 相对于显示设备的左下角，以像素为单位 |
| 3 | `宽度` | 整数型 | - | 以像素为单位 |
| 4 | `高度` | 整数型 | - | 以像素为单位 |

### 设置画面质量

> 英文名 `glHint`

在权衡运行速度和画面质量之后，为显示效果的质量和画面的细节处理设置等级。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `目标` | 整数型 | - | 参数值可以为以下常量：<br>	3152、#透视修正质量，控制颜色和纹理的显示效果；<br>	3153、#点质量，控制点的显示质量；<br>	3154、#线质量，控制线的显示质量；<br>	3155、#多边形质量，控制多边形的显示质量；<br>	3156、#雾质量，控制雾的显示质量；<br> |
| 2 | `效果` | 整数型 | - | 参数值可以为以下常量：<br>	4352、#不考虑；不做特殊要求，让OPenGL自己选择合适的方式进行显示；<br>	4353、#快速；使用最快的显示速度，但是显示效果会有所下降；<br>	4354、#最佳；使用最好的显示效果，但是显示速度会有所下降。当“目标”值为#雾质量时不能使用 |

### 通道混合测试

> 英文名 `glAlphaFunc`

当比较函数返回真的时候才显示出效果来。只有在红绿蓝混合通道模式下，启用“#通道测试”后才有效。不使用的时候需要关闭。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `比较函数` | 整数型 | - | 参数值可以为以下常量：<br>	512、#失效，始终为假；<br>	513、#小于，如果输入值小于测试值时为真；<br>	514、#等于，如果输入值等于测试值时为真；<br>	515、#小于等于，如果输入值小于等于测试值时为真；<br>	516、#大于，如果输入值大于测试值时为真；<br>	517、#不等于，如果输入值不等于测试值时为真；<br>	518、#大于等于，如果输入值大于等于测试值时为真；<br>	519、#有效，始终为真 |
| 2 | `测试值` | 小数型 | - | 范围在[0，1]内的数值。否则无效 |

### 混合测试

> 英文名 `glBlendFunc`

为混合运算设定运算方法。必须启用“#混合测试”才有效。不使用的时候需要关闭。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `源值` | 整数型 | - | 参数值可以为以下常量：<br>	0、0；<br>	1、1；<br>	770、#源值通道比例；<br>	771、#源值通道比例反；<br>	774、#目标值颜色比例；<br>	775、#目标值颜色比例反；<br>	776、#源值饱和通道 |
| 2 | `目标值` | 整数型 | - | 参数值可以为以下常量：<br>	0、0； <br>	1、1； <br>	768、#源值颜色比例；<br>	769、#源值颜色比例反；<br>	770、#源值通道比例；<br>	771、#源值通道比例反；<br>	772、#目标值通道比例；<br>	773、#目标值通道比例反 |

### 深度测试

> 英文名 `glDepthFunc`

当比较函数返回真的时候才显示出效果来。必须启用“#深度测试”，才能执行测试。不使用的时候需要关闭。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `测试方式` | 整数型 | - | 通过使用“清除深度缓存”来设置与其比较的测试值。然后与测试值比较并返回比较结果。参数值可以为以下常量：<br>	512、#失效，始终为假；<br>	513、#小于，如果输入的纵向坐标值小于测试值时为真；<br>	514、#等于，如果输入的纵向坐标值等于测试值时为真；<br>	515、#小于等于，如果输入的纵向坐标值小于等于测试值时为真；<br>	516、#大于，如果输入的纵向坐标值大于测试值时为真；<br>	517、#不等于，如果输入的纵向坐标值不等于测试值时为真；<br>	518、#大于等于，如果输入的纵向坐标值大于等于测试值时为真；<br>	519、#有效，始终为真 |

### 模板测试

> 英文名 `glStencilFunc`

为模板运算设定影响范围。必须启用“#模板测试”，才能执行测试。不使用的时候需要关闭。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `测试方式` | 整数型 | - | 参数值可以为以下常量：<br>	512、#失效，始终为假；<br>	513、#小于，如果测试值的测试位小于输入值的测试位时为真；<br>	514、#等于，如果测试值的测试位等于输入值的测试位时为真；<br>	515、#小于等于，如果测试值的测试位小于等于输入值的测试位时为真；<br>	516、#大于，如果测试值的测试位大于输入值的测试位时为真；<br>	517、#不等于，如果测试值的测试位不等于输入值的测试位时为真；<br>	518、#大于等于，如果测试值的测试位大于等于输入值的测试位时为真；<br>	519、#有效，始终为真 |
| 2 | `测试位` | 整数型 | - | 需要测试模板的位 |
| 3 | `测试值` | 整数型 | - |  |

### 模板测试操作

> 英文名 `glStencilOp`

为模板运算设定运算方法。必须启用“#模板测试”，才能执行测试。不使用的时候需要关闭。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `模板测试失败时的操作` | 整数型 | - | 参数值可以为以下常量：<br>	0、0；，把模板缓存值替换0；<br>	7680、#保持当前值，保持当前的模板缓存值；<br>	7681、#替换当前值，把模板缓存值替换为测试值；<br>	7682、#增加当前值，增加模板缓存值，但不能超过模板缓存所能表达的最大值；<br>	7683、#减去当前值，减去模板缓存值，但不能低于0；<br>	5386、#倒置当前值，把模板缓存值取反 |
| 2 | `深度测试失败时的操作` | 整数型 | - | 当模板测试通过，但是深度测试失败的时候所进行的操作。参数值可以为以下常量：<br>	0、0；，把模板缓存值替换0；<br>	7680、#保持当前值，保持当前的模板缓存值；<br>	7681、#替换当前值，把模板缓存值替换为测试值；<br>	7682、#增加当前值，增加模板缓存值，但不能超过模板缓存所能表达的最大值；<br>	7683、#减去当前值，减去模板缓存值，但不能低于0；<br>	5386、#倒置当前值，把模板缓存值取反；<br> |
| 3 | `深度测试通过时的操作` | 整数型 | - | 当模板测试和深度测试都通过时候所进行的操作。参数值可以为以下常量：	0、0；，把模板缓存值替换0；<br>	7680、#保持当前值，保持当前的模板缓存值；<br>	7681、#替换当前值，把模板缓存值替换为测试值；<br>	7682、#增加当前值，增加模板缓存值，但不能超过模板缓存所能表达的最大值；<br>	7683、#减去当前值，减去模板缓存值，但不能低于0；<br>	5386、#倒置当前值，把模板缓存值取反 |

### 交换缓存

> 英文名 `SwapBuffers` ｜ 返回值 **逻辑型**

交换后台缓存与前台缓存。将后台缓存显示出来，对前台缓存进行操作。成功返回真，失败返回假。在双缓存模式下才用效。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `设备句柄` | 整数型 | - |  |

### 结束绘图操作

> 英文名 `glFinish`

单缓存模式下才可以使用。等待所有已经提交的OpenGL命令执行完毕后才返回。不能在“开始画”和“停止画”之间执行本语句。

### 强制结束绘图操作

> 英文名 `glFlush`

单缓存模式下才可以使用。强制结束，并且返回，不管已经提交的OpenGL命令是否执行完毕。不能在“开始画”和“停止画”之间执行本语句。

### 操作累积缓存

> 英文名 `glAccum`

对裁剪区域里的缓存颜色进行累积缓存操作。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `操作方式` | 整数型 | - | 参数值可以为以下常量：<br>	256、#获取缓存，获取红绿蓝以及混合通道的数据，并加入到累积缓存里；<br>	257、#载入缓存，获取红绿蓝以及混合通道的数据，并加入到累积缓存里，但是不包括缓存现有的数据；<br>	258、#返回缓存，将累积缓存里的数据返回到颜色缓存中去；<br>	259、#缩放缓存，按照操作值来缩放累积缓存里的数据；<br>	260、#加入缓存，增加累积缓存里每个红绿蓝以及混合通道的数据 |
| 2 | `操作值` | 小数型 | - | 范围在[-1，1]内的数值。 |

### 清除缓存

> 英文名 `glClear`

指定清除的一项或几项缓存。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `缓存` | 整数型 | - | 可以使用下列参数值的位或后的数值。参数值可以为以下常量：<br>	256、#深度缓存位，将深度缓存恢复到默认值；<br>	512、#累积缓存位，将累积缓存恢复到默认值；<br>	1024、#模板缓存位，将模板缓存恢复到默认值；<br>	16384、#颜色缓存位，将颜色缓存恢复到默认值 |

### 清除累积缓存

> 英文名 `glClearAccum`

设置累积缓存的默认值。不执行本命令前参数全部为0。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `红色位` | 小数型 | 有默认值 | 范围在[-1，1]内的数值。 |
| 2 | `绿色位` | 小数型 | 有默认值 | 范围在[-1，1]内的数值。 |
| 3 | `蓝色位` | 小数型 | 有默认值 | 范围在[-1，1]内的数值。 |
| 4 | `通道位` | 小数型 | 有默认值 | 范围在[-1，1]内的数值。 |

### 清除颜色缓存

> 英文名 `glClearColor`

设置颜色缓存的默认值。不执行本命令前参数全部为0。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `红色位` | 小数型 | 有默认值 | 范围在[0，1]内的数值。 |
| 2 | `绿色位` | 小数型 | 有默认值 | 范围在[0，1]内的数值。 |
| 3 | `蓝色位` | 小数型 | 有默认值 | 范围在[0，1]内的数值。 |
| 4 | `通道位` | 小数型 | 有默认值 | 范围在[0，1]内的数值。 |

### 清除深度缓存

> 英文名 `glClearDepth`

设置深度缓存的默认值。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `深度` | 双精度小数型 | 有默认值 | 范围在[0，1]内的数值。 |

### 清除模板缓存

> 英文名 `glClearStencil`

设置模板缓存的默认值。不执行本命令前模板值为0。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `模板值` | 整数型 | 有默认值 |  |

### 清除颜色索引

> 英文名 `glClearIndex`

设置颜色索引的默认值。不执行本命令前颜色索引号为0。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `颜色索引号` | 小数型 | 有默认值 |  |

### 设置颜色

> 英文名 `glColor`

设置接下来的图元的颜色。能在“开始画”和“停止画”之间执行本语句。只有在红绿蓝混合通道模式下有效。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `红色位` | 双精度小数型 | 有默认值 | 范围在[0，1]内的数值。 |
| 2 | `绿色位` | 双精度小数型 | 有默认值 | 范围在[0，1]内的数值。 |
| 3 | `蓝色位` | 双精度小数型 | 有默认值 | 范围在[0，1]内的数值。 |
| 4 | `通道位` | 双精度小数型 | 有默认值 | 范围在[0，1]内的数值。 |

### 设置颜色索引

> 英文名 `glIndex`

设置接下来的图元的颜色。能在“开始画”和“停止画”之间执行本语句。只有在颜色索引模式下有效。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `颜色索引号` | 小数型 | - |  |

### 启用功能

> 英文名 `glEnable`

启用某项功能后，会一直保持启用状态。要关闭该功能，必须使用停用功能才能停止。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `功能` | 整数型 | - | 参数值可以为以下常量：<br>	3008、#通道测试；<br>	3042、#混合测试；<br>	2903、#材质颜色；<br>	2929、#深度测试；<br>	2912、#雾；<br>	2884、#精选面；<br>	2896、#光照；<br>	2852、#线型；<br>	3153、#点质量；<br>	3154、#线质量；<br>	2847、#光滑点；<br>	2848、#光滑线；<br>	2977、#单位法向量；<br>	2882、#图案填充；<br>	3479、#曲线坐标；<br>	3511、#曲面坐标；<br>	3456、#自动法向量	3089、#裁剪测试；<br>	2960、#模板测试；<br>	3552、#一维纹理；<br>	3553、#二维纹理；<br>	3168、#生成S向纹理；<br>	3169、#生成T向纹理；<br>	12288--12293、#零号裁剪面--五号裁剪面；<br>	16384--16391、#零号光源--七号光源 |

### 停用功能

> 英文名 `glDisable`

与启用功能搭配使用。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `功能` | 整数型 | - | 参数值可以为以下常量：<br>	3008、#通道测试；<br>	3042、#混合测试；<br>	2903、#材质颜色；<br>	2929、#深度测试；<br>	2912、#雾；<br>	2884、#精选面；<br>	2896、#光照；<br>	2852、#线型；<br>	3153、#点质量；<br>	3154、#线质量；<br>	2847、#光滑点；<br>	2848、#光滑线；<br>	2977、#单位法向量；<br>	2882、#图案填充；<br>	3479、#曲线坐标；<br>	3511、#曲面坐标；<br>	3456、#自动法向量	3089、#裁剪测试；<br>	2960、#模板测试；<br>	3552、#一维纹理；<br>	3553、#二维纹理；<br>	3168、#生成S向纹理；<br>	3169、#生成T向纹理；<br>	12288--12293、#零号裁剪面--五号裁剪面；<br>	16384--16391、#零号光源--七号光源 |

### 设置光源参数

> 英文名 `glLight`

必须启用“#光照”后才有效。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `光源号` | 整数型 | - | 参数值可以为以下常量：<br>	16384、#零号光源；<br>	16385、#一号光源；<br>	16386、#二号光源；<br>	16387、#三号光源；<br>	16388、#四号光源；<br>	16389、#五号光源；<br>	16390、#六号光源；<br>	16391、#七号光源 |
| 2 | `参数名称` | 整数型 | - | 参数值可以为以下常量：<br>	4608、#环境光，参数值为范围在[0，1]内的表示红绿蓝混合通道颜色的四维数组 ，默认值为（0，0，0，1），多余部分无效；<br>	4609、#漫反射光，参数值为范围在[0，1]内的表示红绿蓝混合通道颜色的四维数组 ，#零号光源的默认值为（1，1，1，1），其他光源的默认值为（0，0，0，1），多余部分无效；<br>	4610、#镜面光，参数值为范围在[0，1]内的表示红绿蓝混合通道颜色的四维数组，#零号光源的默认值为（1，1，1，1），其他光源的默认值为（0，0，0，1），多余部分无效；<br>	4611、#光源位置，参数值为四维数组，默认值（0，0，1，0），前面三位表示坐标，最后一位为0时，表示平行光。否则，表示点光源。多余部分无效；<br>	4612、#光源聚光方向，对聚光灯才有效。参数值为三维数组（0，0，-1），多余部分无效；<br>	4613、#光源聚光指数，对聚光灯才有效。参数值范围在[0，128]内的非数组数值，默认值0；<br>	4614、#点光源聚光截止角。参数值范围在[0，90]内和特殊值180的非数组数值，默认值180，表示不产生聚光效果，[0，90]内的数值表示使用聚光效果；<br>	4615、#常量衰减，对聚光灯才有效。参数值为非数组数值，默认值1；<br>	4616、#线形衰减，对聚光灯才有效。参数值为非数组数值，默认值0；<br>	4617、#二次方衰减，对聚光灯才有效。参数值为非数组数值，默认值0 |
| 3 | `参数值` | 小数型 | 任意类型 |  |

### 设置光照模式

> 英文名 `glLightModel`

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `参数名称` | 整数型 | - | 参数值可以为以下常量：<br>	2897、#光源观察点模式，顶点的高光强度不仅取决于顶点法向量，而且取决于顶点到光源的方向以及顶点到视点的方向.1表示设置为局部视点,需要计算视点到每个顶点的方向,会影响运行速度。0表示设置为无穷远视点,不论观察坐标如何改变，光的反射方向始终与纵向轴平行。默认为0；<br>	2898、#双面光模式，如果参数值为0表示使用单面光，只有正面接受光照效果。否则正面和背面都接受光照效果，背面的法向为正面法向的反方向。默认为0；<br>	2899、#全局光模式，参数值为范围在[0，1]内的表示红绿蓝混合通道颜色的四维数组，默认为（0.2, 0.2, 0.2, 1.0），多余部分无效；<br> |
| 2 | `参数值` | 小数型 | 任意类型 |  |

### 设置材质

> 英文名 `glMaterial`

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `应用面` | 整数型 | - | 参数值可以为以下常量：<br>   1028、#正面； 1029、#背面； 1030、#正面和背面；<br> |
| 2 | `参数名称` | 整数型 | - | 参数值可以为以下常量：<br>	4608、#环境光，参数值为范围在[0，1]内的表示红绿蓝混合通道颜色的四维数组，多余部分无效；<br>	4609、#漫反射光，参数值为范围在[0，1]内的表示红绿蓝混合通道颜色的四维数组，多余部分无效；<br>	4610、#镜面光，参数值为范围在[0，1]内的表示红绿蓝混合通道颜色的四维数组，多余部分无效；<br>	5632、#散射光，参数值为范围在[0，1]内的表示红绿蓝混合通道颜色的四维数组，多余部分无效；<br>	5633、#亮光，参数值为范围在[0，128]内的非数组数值；<br>	5634、#环境光和漫反射光，参数值为范围在[0，1]内的表示红绿蓝混合通道颜色的四维数组，多余部分无效；<br>	5635、#颜色索引，参数值为非数组数值 |
| 3 | `参数值` | 小数型 | 任意类型 |  |

### 设置材质颜色

> 英文名 `glColorMaterial`

使用当前颜色为材料的颜色。必须启用“#材质颜色”才有效，不使用的时候需要关闭。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `应用面` | 整数型 | - | 参数值可以为以下常量：<br>	1028、#正面；<br>	1029、#背面；<br>	1030、#正面和背面；<br> |
| 2 | `模式` | 整数型 | - | 参数值可以为以下常量：<br>	4608、#环境光；<br>	4609、#漫反射光；<br>	4610、#镜面光；<br>	5632、#散射光；<br>	5633、#亮光；<br>	5634、#环境光和漫反射光 |

### 设置雾

> 英文名 `glFog`

必须启用“#雾”后才有效。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `参数名称` | 整数型 | - | 参数值可以为以下常量：<br>	2913、#雾索引，参数值为非数组数值；<br>	2914、#雾密度，参数值为范围在[0，+∞)内的非数组数值；<br>	2915、#雾起点，参数值为非数组数值；<br>	2916、#雾终点，参数值为非数组数值；<br>	2917、#雾模式，参数值可以为以下常量：<br>		2048、#雾模式1；<br>		2049、#雾模式2；<br>		9729、#线性<br>	2918、#雾颜色，参数值为范围在[0，1]内的四维数组，多余部分无效 |
| 2 | `参数值` | 小数型 | 任意类型 |  |

### 设置法向量

> 英文名 `glNormal`

指定下一顶点的法向量。能在“开始画”和“停止画”之间执行本语句。启用“#单位法向量”后，法向量的长度就会变成1。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `横向量` | 双精度小数型 | - |  |
| 2 | `竖向量` | 双精度小数型 | - |  |
| 3 | `纵向量` | 双精度小数型 | - |  |

### 设置明暗模式

> 英文名 `glShadeModel`

设置明暗的表示方法。对点图元无效。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `模式` | 整数型 | - | 参数值可以为以下常量：<br>	7424、#平坦，明暗的过度为跳跃地变化；<br>	7425、#光滑，明暗的过度为连续地变化 |

### 绑定纹理号

> 英文名 `glBindTexture`

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `纹理维数` | 整数型 | - | 参数值可以为以下常量：<br>	3552、#一维纹理；<br>	3553、#二维纹理 |
| 2 | `纹理号` | 整数型 | - |  |

### 纹理控制

> 英文名 `glTexParameter`

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `纹理维数` | 整数型 | - | 参数值可以为以下常量：<br>	3552、#一维纹理； 3553、#二维纹理 |
| 2 | `控制方式` | 整数型 | - | 参数值可以为以下常量：<br>	10240、#放大纹理滤波，当纹理被放大时所进行的滤波。控制值可以为以下常量：<br>		9728、#最近滤镜，插入的颜色为最接近纹理图像原像素点位置的颜色； <br>		9729、#线性滤镜，使用线性插值方式插入颜色；<br>	10241、#缩小纹理滤波，当纹理被缩小时所进行的滤波。控制值可以为以下常量：<br>		9728、#最近滤镜，插入的颜色为最接近纹理图像原像素点位置的颜色； <br>		9729、#线性滤镜，使用线性插值方式插入颜色；<br>		9984、#最近多贴图滤镜，使用最接近纹理原始尺寸的图像，再使用 #最近滤镜 进行滤波；<br>		9985、#多贴图线性内插滤镜，使用最接近纹理原始尺寸的图像，再使用 #线性滤镜 进行滤波；<br>		9986、#线性内插多贴图滤镜，使用最接近纹理原始尺寸的两张图像，再使用 #最近滤镜 对两张图像中进行滤波，最后取两张图像的平均值；<br>		9987、#内插多贴图的线性内插滤镜，使用最接近纹理原始尺寸的两张图像，再使用 #线性滤镜 对两张图像中进行滤波，最后取两张图像的平均值；<br>	10242、#S向纹理约束，控制值可以为以下常量：<br>		10496、#纹理插值约束，纹理的坐标值被约束在[0，1]之间；<br>		10497、#纹理重复约束，允许纹理图片出现重复；<br>	10243、#T向纹理约束，控制值可以为以下常量：<br>		10496、#纹理插值约束，纹理的坐标值被约束在[0，1]之间；<br>		10497、#纹理重复约束，允许纹理图片出现重复 |
| 3 | `控制值` | 小数型 | 任意类型 |  |

### 定义一维纹理图片

> 英文名 `glTexImage1D` ｜ 返回值 **逻辑型**

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `等级` | 整数型 | 有默认值 | 通常为0，只有在多贴图情况下表，说明贴图的等级 |
| 2 | `颜色成份` | 整数型 | - | 说明在纹理图片中的颜色是由那些成份所组成。必须为1、2、3或4 |
| 3 | `宽度` | 整数型 | - | 为范围大于0的数值，并且必须为2的幂数 |
| 4 | `格式` | 整数型 | - | 指出所读象素数据元素的格式。参数值可以为以下常量：<br>	6400、#单个颜色索引，每个元素为一个颜色索引号；<br>	6403、#红色分量，每个元素为一个红色分量的值；<br>	6404、#绿色分量，每个元素为一个绿色分量的值；<br>	6405、#蓝色分量，每个元素为一个蓝色分量的值；<br>	6406、#混合通道分量，每个元素为一个混合通道分量的值；<br>	6407、#红绿蓝分量，每个元素为一个红绿蓝分量的值；<br>	6408、#红绿蓝混合通道分量，每个元素为一个红绿蓝混合通道分量的值；<br>	6409、#亮度，每个元素为一个亮度分量的值；<br>	6410、#亮度和混合通道分量，每个元素为亮度和混合通道分量的值 |
| 5 | `图片像素` | 字节集型 | - | 一组包含纹理图片像素信息的数组，数组成员组成为[宽度][高度][颜色成份],否则纹理图片将发生变形。 |

### 定义二维纹理图片

> 英文名 `glTexImage2D` ｜ 返回值 **逻辑型**

成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `等级` | 整数型 | 有默认值 | 通常为0，只有在多贴图情况下表，说明贴图的等级 |
| 2 | `颜色成份` | 整数型 | - | 必须为1、2、3或4 |
| 3 | `宽度` | 整数型 | - | 为范围大于0的数值，并且必须为2的幂数 |
| 4 | `高度` | 整数型 | - | 为范围大于0的数值，并且必须为2的幂数 |
| 5 | `格式` | 整数型 | - | 指出所读象素数据元素的格式。参数值可以为以下常量：<br>	6400、#单个颜色索引，每个元素为一个颜色索引号；<br>	6403、#红色分量，每个元素为一个红色分量的值；<br>	6404、#绿色分量，每个元素为一个绿色分量的值；<br>	6405、#蓝色分量，每个元素为一个蓝色分量的值；<br>	6406、#混合通道分量，每个元素为一个混合通道分量的值；<br>	6407、#红绿蓝分量，每个元素为一个红绿蓝分量的值；<br>	6408、#红绿蓝混合通道分量，每个元素为一个红绿蓝混合通道分量的值；<br>	6409、#亮度，每个元素为一个亮度分量的值；<br>	6410、#亮度和混合通道分量，每个元素为亮度和混合通道分量的值 |
| 6 | `图片像素` | 字节集型 | - | 一组包含纹理图片像素信息的数组，数组成员组成为[宽度][高度][颜色成份],否则纹理图片将发生变形。 |

### 设置纹理映射坐标

> 英文名 `glTexCoord`

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `横向位置` | 双精度小数型 | - | 纹理图片的左边为0，右边为1，超出[0，1]范围的值，纹理图片将被平移相应的距离。 |
| 2 | `竖向位置` | 双精度小数型 | - | 纹理图片的底边为0，顶边为1，超出[0，1]范围的值，纹理图片将被平移相应的距离。如果为空，表示定义的是一维纹理的坐标 |

### 设置纹理映射方式

> 英文名 `glTexEnv`

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `处理方式` | 整数型 | - | 参数值可以为以下常量：<br>	8704、#纹理映射模式；映射值可以为以下常量：<br>		8448、#调节处理；调节当前亮度和颜色 <br>		8849、#粘贴处理；不受颜色和亮度影响 <br>		3042、#混合测试；与原来色颜色进行混合测试。<br>	8705、#纹理映射颜色；映射值为范围在[0，1]内的四维数组，多余部分无效 |
| 2 | `映射值` | 小数型 | 任意类型 |  |

### 生成纹理号

> 英文名 `glGenTextures`

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `纹理数` | 整数型 | - | 需要生成纹理号 |
| 2 | `存放纹理号的变量` | 整数型 | - | 用来存放纹理号的变量，如果提供的变量为数组，那么纹理号将被依次存放在数组中。超出数组成员数的部分将无法生成。 |

### 生成纹理坐标

> 英文名 `glTexGen`

需要生成纹理号

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `坐标量` | 整数型 | - | 参数值可以为以下常量：<br>	8192、#S分量； <br>	8193、#T分量；<br> |
| 2 | `映射值` | 整数型 | - | 参数值可以为以下常量：<br>	9472、#纹理生成模式，坐标值可以为以下常量：<br>		9216、#观测线性插值； <br>		9217、#模型线性插值； <br>		9218、#纹理贴图形状；<br>	9473、#模型平面，四维数组，为生成纹理坐标的方程指定四个相应的参数，多余部分无效；<br>	9474、#观测平面，四维数组，为生成纹理坐标的方程指定四个相应的参数，多余部分无效； |
| 3 | `坐标值` | 双精度小数型 | 任意类型 |  |

### 载入位图数据

> 英文名 `auxDIBImageLoad` ｜ 返回值 **逻辑型**

成功载入返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `位图文件名` | 文本型 | - | 允许使用1位、4位、8位、16位、24位、32位的翻转行序或非翻转行序的非压缩BMP位图文件格式 |
| 2 | `背景色` | 整数型 | 可空 | 位图中指定为背景色的区域，其颜色值的通道部分将被定义为0。如果为空，将被定义为1。可以通过使用“通道混合测试”进行处理。<br>	如果希望将背景色部分显示为透明，可以执行：<br>	  启用功能 (#通道测试)<br>	  通道混合测试 (#大于, 0)<br>	  设置纹理映射方式 (#纹理映射模式, #调节处理) |

### 开始创建列表

> 英文名 `glNewList`

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `列表号` | 整数型 | - | 可以直接指定列表号或者使用由“生成列表号”取得的返回值，必须是大于0的整数。 |
| 2 | `列表模式` | 整数型 | - | 参数值可以为以下常量：<br>	4864、#编译列表； <br>	4865、#编译并且执行列表 |

### 停止创建列表

> 英文名 `glEndList`

与“开始创建列表”搭配使用。

### 生成列表号

> 英文名 `glGenLists` ｜ 返回值 **整数型**

按次序自动生成一串连续的空列表号。返回其中的第一个列表号。失败返回-1

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `列表长度` | 整数型 | - | 必须是大于0的整数 |

### 调用列表

> 英文名 `glCallList` ｜ 返回值 **逻辑型**

调用已经创建的列表。可以重复调用。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `列表号` | 整数型 | - | 可以直接指定列表号或者使用由“生成列表号”取得的返回值，必须是大于0的整数。 |

### 是否存在列表

> 英文名 `glIsList` ｜ 返回值 **逻辑型**

如果该列表号存在已经编译的列表就返回真，否则返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `列表号` | 整数型 | - | 可以直接指定列表号或者使用由“生成列表号”取得的返回值，必须是大于0的整数。 |

### 删除列表列

> 英文名 `glDeleteLists` ｜ 返回值 **逻辑型**

删除一列连续的列表。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `起始列表号` | 整数型 | - | 必须是大于0的整数 |
| 2 | `列表数` | 整数型 | - | 必须是大于0的整数 |

### 设置偏移列表数

> 英文名 `glListBase` ｜ 返回值 **逻辑型**

为“调用多显示列表”设定一个起始列表号。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `偏移列表数` | 整数型 | - | 必须是大于0的整数 |

### 调用多显示列表

> 英文名 `glCallLists`

等效为多次使用“调用列表”的结果。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `列表号数组` | 整数型 | 数组 | 保存一系列即将被依次调用的列表号的一维数组。如果列表号不存在，将看不到效果。 |

### 设置光栅位置

> 英文名 `glRasterPos`

设置下一次对光栅操作的坐标位置，会受到坐标变换的影响。如果要改变像素的颜色必须在本命令之前使用。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `横向位置` | 双精度小数型 | - | 光栅原点到当前矩阵的坐标系原点的横向距离，单位与坐标系相同。正数时光栅原点在坐标原点右边，反之在左边，0表示相同。 |
| 2 | `竖向位置` | 双精度小数型 | - | 光栅原点到当前矩阵的坐标系原点的竖向距离，单位与坐标系相同。正数时光栅原点在坐标原点上边，反之在下边，0表示相同。 |

### 画出位图

> 英文名 `glBitmap`

必须先使用“设置像素存储字节数”。坐标的原点只与“设置光栅位置”有关与坐标变换命令无关。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `宽度` | 整数型 | - | “位图数据”中将要显示的宽度，必须大于0。 |
| 2 | `高度` | 整数型 | - | “位图数据”中将要显示的高度，必须大于0。 |
| 3 | `原点横坐标` | 小数型 | - | 坐标原点与位图左下角的横向素数距离，正数时在原点左边，反之在右边。 |
| 4 | `原点竖坐标` | 小数型 | - | 坐标原点与位图左下角的竖向素数距离，正数时在原点下边，反之在上边。 |
| 5 | `横向平移` | 小数型 | - | 本次画出结束后，坐标原点向右移动的像素距离，负数为反方向。只会影响下一次“画出位图”时的画出位置，与本次画出无关。 |
| 6 | `纵向平移` | 小数型 | - | 本次画出结束后，坐标原点向上移动的像素距离，负数为反方向。只会影响下一次“画出位图”时的画出位置，与本次画出无关。 |
| 7 | `位图数据` | 字节集型 | - |  |

### 复制像素

> 英文名 `glCopyPixels` ｜ 返回值 **逻辑型**

复制一个矩形范围内的内容到现在的光栅位置处。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `左下角横向位置` | 整数型 | - | 矩形的左下角距离坐标原点的横向素数个数。 |
| 2 | `左下角纵向位置` | 整数型 | - | 矩形的左下角距离坐标原点的纵向素数个数。 |
| 3 | `宽度` | 整数型 | - | 以素数为单位，必须大于0。 |
| 4 | `高度` | 整数型 | - | 以素数为单位，必须大于0。 |
| 5 | `类型` | 整数型 | - | 参数值可以为以下常量：<br>	6144、#颜色类型； <br>	6145、#深度类型； <br>	6146、#模板类型 |

### 设置像素存储字节数

> 英文名 `glPixelStore`

设置位图像素数据在计算机内存中存储的方式

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `存储的字节数` | 整数型 | - | 只能是1、2、4或8，其他数值无效。 |

### 像素缩放

> 英文名 `glPixelZoom` ｜ 返回值 **逻辑型**

设置接下来的像素操作的缩放比例。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `横向位置缩放` | 小数型 | - | 大于1表示放大，小于1表示缩小，1表示不进行缩放，必须大于0 |
| 2 | `竖向位置缩放` | 小数型 | - | 大于1表示放大，小于1表示缩小，1表示不进行缩放，必须大于0 |

### 是否为双字节字符

> 英文名 `IsDBCSLeadByte` ｜ 返回值 **逻辑型**

如果为双字节返回真，否则返回假。如果指定位置超出文本长度，返回0。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `欲检查文本` | 文本型 | - |  |
| 2 | `欲检查的字符位置` | 整数型 | - | 0为首位置，1为第2个位置，如此类推，必须是大于0。 |

### 创建文字轮廓

> 英文名 `wglUseFontOutlines` ｜ 返回值 **逻辑型**

创建三维的文字外形轮廓。如果成功返回真，否则返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `设备句柄` | 整数型 | - |  |
| 2 | `欲使用其外形的首个字符的代码` | 整数型 | - |  |
| 3 | `字符数` | 整数型 | - | 必须是大于0。如果为1就显示首个字符，2就显示首个字符后面的一个字符。中文为双字节一个字符，不能大于1 |
| 4 | `基列表号` | 整数型 | - |  |
| 5 | `轮廓偏移量` | 小数型 | - | 数值越小越逼真，0的时候最好。 |
| 6 | `拉伸厚度` | 小数型 | - | 沿着Z轴方向拉伸的距离。 |
| 7 | `实心` | 逻辑型 | 有默认值 | 真表示使用实心字体，否则生成空心轮廓。 |

### 创建实心球

> 英文名 `glutSolidSphere`

创建一个圆心在坐标原点的实心球。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `半径` | 双精度小数型 | - | 负数自动被转化为正数，如果等于0，将由1来取代。 |
| 2 | `经线条数` | 整数型 | - | 负数自动被转化为正数，如果等于0，将由1来取代。数值越大越逼真，占用的系统资源也越大。 |
| 3 | `纬线条数` | 整数型 | - | 负数自动被转化为正数，如果等于0，将由1来取代。数值越大越逼真，占用的系统资源也越大。 |

### 创建空心球

> 英文名 `glutWireSphere`

创建一个圆心在坐标原点的空心球。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `半径` | 双精度小数型 | - | 负数自动被转化为正数，如果等于0，将由1来取代。 |
| 2 | `经线条数` | 整数型 | - | 负数自动被转化为正数，如果等于0，将由1来取代。数值越大越逼真，占用的系统资源也越大。 |
| 3 | `纬线条数` | 整数型 | - | 负数自动被转化为正数，如果等于0，将由1来取代。数值越大越逼真，占用的系统资源也越大。 |

### 创建实心正方体

> 英文名 `glutSolidCube`

创建一个中心在坐标原点的实心正方体。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `棱长` | 双精度小数型 | - | 负数自动被转化为正数，如果等于0，将由1来取代。 |

### 创建空心正方体

> 英文名 `glutWireCube`

创建一个中心在坐标原点的空心正方体。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `棱长` | 双精度小数型 | - | 负数自动被转化为正数，如果等于0，将由1来取代。 |

### 创建实心圆锥体

> 英文名 `glutSolidCone`

创建一个中心在坐标原点的实心圆锥体。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `底面半径` | 双精度小数型 | - | 负数自动被转化为正数，如果等于0，将由1来取代。 |
| 2 | `圆锥体高度` | 双精度小数型 | - | 负数自动被转化为正数，如果等于0，将由1来取代。 |
| 3 | `经线条数` | 整数型 | - | 负数自动被转化为正数，如果等于0，将由1来取代。数值越大越逼真，占用的系统资源也越大。 |
| 4 | `纬线条数` | 整数型 | - | 负数自动被转化为正数，如果等于0，将由1来取代。数值越大越逼真，占用的系统资源也越大。 |

### 创建空心圆锥体

> 英文名 `glutWireCone`

创建一个中心在坐标原点的空心圆锥体。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `底面半径` | 双精度小数型 | - | 负数自动被转化为正数，如果等于0，将由1来取代。 |
| 2 | `圆锥体高度` | 双精度小数型 | - | 负数自动被转化为正数，如果等于0，将由1来取代。 |
| 3 | `经线条数` | 整数型 | - | 负数自动被转化为正数，如果等于0，将由1来取代。数值越大越逼真，占用的系统资源也越大。 |
| 4 | `纬线条数` | 整数型 | - | 负数自动被转化为正数，如果等于0，将由1来取代。数值越大越逼真，占用的系统资源也越大。 |

### 创建实心圆环

> 英文名 `glutSolidTorus`

创建一个中心在坐标原点的实心圆环。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `内圆半径` | 双精度小数型 | - | 负数自动被转化为正数，如果等于0，将由1来取代。如果内圆半径大于外圆半径,就交换内圆半径和外圆半径的值。如果内圆半径和外圆半径的值相等，外圆半径自动加1。 |
| 2 | `外圆半径` | 双精度小数型 | - | 负数自动被转化为正数，如果等于0，将由1来取代。如果内圆半径大于外圆半径,就交换内圆半径和外圆半径的值。如果内圆半径和外圆半径的值相等，外圆半径自动加1。 |
| 3 | `边数` | 整数型 | - | 负数自动被转化为正数，如果小于3，将由3来取代。数值越大越逼真，占用的系统资源也越大。 |
| 4 | `环数` | 整数型 | - | 负数自动被转化为正数，如果小于3，将由3来取代。数值越大越逼真，占用的系统资源也越大。 |

### 创建空心圆环

> 英文名 `glutWireTorus`

创建一个中心在坐标原点的空心圆环。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `内圆半径` | 双精度小数型 | - | 负数自动被转化为正数，如果等于0，将由1来取代。如果内圆半径大于外圆半径,就交换内圆半径和外圆半径的值。如果内圆半径和外圆半径的值相等，外圆半径自动加1。 |
| 2 | `外圆半径` | 双精度小数型 | - | 负数自动被转化为正数，如果等于0，将由1来取代。如果内圆半径大于外圆半径,就交换内圆半径和外圆半径的值。如果内圆半径和外圆半径的值相等，外圆半径自动加1。 |
| 3 | `边数` | 整数型 | - | 负数自动被转化为正数，如果小于3，将由3来取代。数值越大越逼真，占用的系统资源也越大。 |
| 4 | `环数` | 整数型 | - | 负数自动被转化为正数，如果小于3，将由3来取代。数值越大越逼真，占用的系统资源也越大。 |

### 创建实心十二面体

> 英文名 `glutSolidDodecahedron`

创建一个中心在坐标原点的外接球半径为三分之二根号三的实心十二面体。

### 创建空心十二面体

> 英文名 `glutWireDodecahedron`

创建一个中心在坐标原点的外接球半径为三分之二根号三的空心十二面体。

### 创建实心八面体

> 英文名 `glutSolidOctahedron`

创建一个中心在坐标原点的外接球半径为一的实心八面体。

### 创建空心八面体

> 英文名 `glutWireOctahedron`

创建一个中心在坐标原点的外切球半径为一的空心八面体。

### 创建实心四面体

> 英文名 `glutSolidTetrahedron`

创建一个中心在坐标原点的外接球半径为根号三的实心四面体。

### 创建空心四面体

> 英文名 `glutWireTetrahedron`

创建一个中心在坐标原点的外接球半径为根号三的空心四面体。

### 创建实心二十面体

> 英文名 `glutSolidIcosahedron`

创建一个中心在坐标原点的外接球半径为一的实心二十面体。

### 创建空心二十面体

> 英文名 `glutWireIcosahedron`

创建一个中心在坐标原点的外接球半径为一的空心二十面体。

### 创建实心圆柱体

> 英文名 `glutSolidcylinder`

创建一个中心在坐标原点的实心圆柱体。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `底圆半径` | 双精度小数型 | - | 负数自动被转化为正数，如果等于0，将由1来取代。 |
| 2 | `高度` | 双精度小数型 | - | 负数自动被转化为正数，如果等于0，将由1来取代。 |
| 3 | `经线条数` | 整数型 | - | 负数自动被转化为正数，如果等于0，将由1来取代。数值越大越逼真，占用的系统资源也越大。 |

### 创建空心圆柱体

> 英文名 `glutWirecylinder`

创建一个中心在坐标原点的空心圆柱体。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `底圆半径` | 双精度小数型 | - | 负数自动被转化为正数，如果等于0，将由1来取代。 |
| 2 | `高度` | 双精度小数型 | - | 负数自动被转化为正数，如果等于0，将由1来取代。 |
| 3 | `经线条数` | 整数型 | - | 负数自动被转化为正数，如果等于0，将由1来取代。数值越大越逼真，占用的系统资源也越大。 |

### 取错误信息文本

> 英文名 `glGetError` ｜ 返回值 **文本型**

如果执行某OPenGL命令失败，在其后执行本命令可以取回错误信息文本。		如果该OPenGL命令执行成功，执行本命令将返回空文本。
		如果参数值超出整数的范围，返回“无效整数”
		如果参数值超出可选的范围，返回“无效数值”
		如果OPenGL命令出现在错误的位置，返回“无效操作”
		如果在堆栈已满时继续入栈，返回“堆栈溢出”
		如果在堆栈已空时继续出栈，返回“堆栈下溢”

### 设置观察坐标

> 英文名 `gluLookAt`

新的视图空间以坐标中心位置为坐标原点，坐标中心位置到眼睛的矢量为纵向坐标轴的正方向，以正上方矢量为竖向坐标轴的正方向。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `眼睛横坐标` | 双精度小数型 | - |  |
| 2 | `眼睛竖坐标` | 双精度小数型 | - |  |
| 3 | `眼睛纵坐标` | 双精度小数型 | - |  |
| 4 | `中心横坐标` | 双精度小数型 | - |  |
| 5 | `中心竖坐标` | 双精度小数型 | - |  |
| 6 | `中心纵坐标` | 双精度小数型 | - |  |
| 7 | `正上方横坐标` | 双精度小数型 | - |  |
| 8 | `正上方竖坐标` | 双精度小数型 | - |  |
| 9 | `正上方纵坐标` | 双精度小数型 | - |  |

### 载入3DS文件

> 英文名 `glLoad3DS` ｜ 返回值 **逻辑型**

载入以3DS为后缀的三维模型文件，并编译为一个显示列表。如果3DS文件中包含有纹理图片，那么只有BMP位图格式的纹理才可以被使用。否则无法显示纹理。使用“调用列表”命令可以显示模型。成功返回真，失败返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `3DS文件名` | 文本型 | - | 需要载入的3DS文件名称。 |
| 2 | `列表号` | 整数型 | - | 可以直接指定列表号或者使用由“生成列表号”取得的返回值，必须是大于0的整数。 |
| 3 | `存放被取回纹理号的数组变量` | 整数型 | - | 在命令执行完毕后，本变量数组内被顺序填入在载入3DS文件时候创建的纹理号。变量数组内原有数据被全部销毁，变量数组的维数被自动调整为一维数组。 |

### 创建实心圆台

> 英文名 `glutSolidtaper`

创建一个中心在坐标原点的实心圆台。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `下底圆半径` | 双精度小数型 | - | 负数自动被转化为正数，如果上下底圆半径都等于0，就无法显示。 |
| 2 | `上底圆半径` | 双精度小数型 | - | 负数自动被转化为正数，如果上下底圆半径都等于0，就无法显示。 |
| 3 | `高度` | 双精度小数型 | - | 负数自动被转化为正数，如果等于0，表示压成平面。 |
| 4 | `经线条数` | 整数型 | - | 负数自动被转化为正数，如果等于0，将由1来取代。数值越大越逼真，占用的系统资源也越大。 |
| 5 | `纬线条数` | 整数型 | - | 负数自动被转化为正数，如果等于0，将由1来取代。数值越大越逼真，占用的系统资源也越大。 |

### 创建空心圆台

> 英文名 `glutWiretaper`

创建一个中心在坐标原点的空心圆台。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `下底圆半径` | 双精度小数型 | - | 负数自动被转化为正数，如果上下底圆半径都等于0，就无法显示。 |
| 2 | `上底圆半径` | 双精度小数型 | - | 负数自动被转化为正数，如果上下底圆半径都等于0，就无法显示。 |
| 3 | `高度` | 双精度小数型 | - | 负数自动被转化为正数，如果等于0，表示压成平面。 |
| 4 | `经线条数` | 整数型 | - | 负数自动被转化为正数，如果等于0，将由1来取代。数值越大越逼真，占用的系统资源也越大。 |
| 5 | `纬线条数` | 整数型 | - | 负数自动被转化为正数，如果等于0，将由1来取代。数值越大越逼真，占用的系统资源也越大。 |

### 设置线宽

> 英文名 `glLineWidth` ｜ 返回值 **逻辑型**

为所画的线段设置线条宽度。如果操作系统支持指定的宽度大小，返回真，否则返回假。通过启用和停用“光滑线”功能，来控制线段的放样效果。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `宽度` | 小数型 | - | 以像素为单位的正数，负数自动取正，零还原为1。默认为1个像素大小。目前WINDOWS支持的宽度大小在0.5和10.0之间，间隔为0.125，最后使用的实际宽度为最接近这个值的宽度大小。 |

### 设置点大小

> 英文名 `glPointSize`

为所画的点设置点的大小。通过启用和停用“光滑点”功能，来控制点的放样效果。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `大小` | 小数型 | - | 以像素为单位的正数，负数自动取正，零还原为1。默认为1个像素大小。 |

### 设置曲线控制点

> 英文名 `glMap1` ｜ 返回值 **整数型**

为Bezier曲线定义控制点。定义了控制点后，才可以画曲线。成功返回有效的控制点数，失败返回0。始终保持这样的曲线，直到再次使用本命令改变控制点。
	本命令使用参数方程来定义曲线上的每个顶点。
		X = f （U ）
		Y = f （U ）
		Z = f （U ）
	其中X、Y、Z分别代表曲线上的点的横向位置、竖向位置和纵向位置，U代表参数方程f（）的参数
当参数 U 发生变化的时候，X、Y、Z也相应的改变。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `U参数上限` | 双精度小数型 | - | U参数的起始值。 |
| 2 | `U参数下限` | 双精度小数型 | - | U参数的终止值。 |
| 3 | `控制点` | 双精度小数型 | 数组 | 用来描述控制点的数组，按照U1（X、Y、Z），U2（X、Y、Z），U3（X、Y、Z）...的顺序排列。 |

### 设置曲线点

> 英文名 `glEvalCoord1`

插入到“开始画”和“停止画”中来设置曲线上的点。先使用“设置曲线控制点”命令生成曲线，然后指定U参数的值，计算出曲线上点的坐标。必须启用“#曲线坐标”才有效，不使用的时候需要关闭。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `U参数` | 双精度小数型 | - | 参数值应该介于“设置曲线控制点”命令中的“U参数上限”和“U参数下限”之间。 |

### 设置曲线等分点

> 英文名 `glMapGrid1`

先使用“设置曲线控制点”命令生成曲线，然后设置曲线的等分点数，让曲线光滑的过度。
	最终得到的每两个相邻U参数的间隔为：（U向等分下限 - U向等分上限）/ U向等分数。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `U向等分数` | 整数型 | - | 等分数目越多，曲线越光滑，占用的系统资源也越大。 |
| 2 | `U向等分上限` | 双精度小数型 | - | U参数的起始值。 |
| 3 | `U向等分下限` | 双精度小数型 | - | U参数的终止值。 |

### 画曲线

> 英文名 `glEvalMesh1`

先使用“设置曲线控制点”命令生成曲线，然后才能画出曲线。显示范围在“曲线起点”和“曲线终点”之间。必须启用“#曲线坐标”才有效，不使用的时候需要关闭。如果需要使用法向量，必须启用“#自动法向量”才有效，不使用的时候需要关闭。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `画出模式` | 整数型 | - | 参数值可以为以下常量：<br>	6912、#点，将画出的多边形显示为点；<br>	6913、#线，将画出的多边形显示为线段；<br> |
| 2 | `曲线起点` | 整数型 | - | 参数值应该介于 0 和“设置曲线等分点”命令中的“U向等分数”之间。 |
| 3 | `曲线终点` | 整数型 | - | 参数值应该介于 0 和“设置曲线等分点”命令中的“U向等分数”之间。 |

### 设置曲面控制点

> 英文名 `glMap2` ｜ 返回值 **整数型**

为Bezier曲面定义控制点。定义了控制点后，才可以画曲面。成功返回有效的控制点数，失败返回0。必须启用“#曲面坐标”才有效，不使用的时候需要关闭。始终保持这样的曲面，直到再次使用本命令改变控制点。
	本命令使用参数方程来定义曲线上的每个顶点。
		X = f （U ，V）
		Y = f （U ，V）
		Z = f （U ，V）
	其中X、Y、Z分别代表曲线上的点的横向位置、竖向位置和纵向位置，U、V代表参数方程f（）的参数
当参数 U 、V发生变化的时候，X、Y、Z也相应的改变。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `U参数上限` | 双精度小数型 | - | U参数的起始值。 |
| 2 | `U参数下限` | 双精度小数型 | - | U参数的终止值。 |
| 3 | `U向控制点数` | 整数型 | - | U向控制点的数目,必须为非零整数。负数自动取正。 |
| 4 | `V参数上限` | 双精度小数型 | - | V参数的起始值。 |
| 5 | `V参数下限` | 双精度小数型 | - | V参数的终止值。 |
| 6 | `控制点` | 双精度小数型 | 数组 | 用来描述控制点的数组，按照U1V1（X、Y、Z），U2V1（X、Y、Z）...，U1V2（X、Y、Z），U2V2（X、Y、Z）...的顺序排列。 |

### 设置曲面点

> 英文名 `glEvalCoord2`

插入到“开始画”和“停止画”中来设置曲线上的点。先使用“设置曲面控制点”命令生成曲线，然后指定U参数的值，计算出曲线上点的坐标。必须启用“#曲面坐标”才有效，不使用的时候需要关闭。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `U参数` | 双精度小数型 | - | 参数值应该介于“设置曲面控制点”命令中的“U参数上限”和“U参数下限”之间。 |
| 2 | `V参数` | 双精度小数型 | - | 参数值应该介于“设置曲面控制点”命令中的“V参数上限”和“V参数下限”之间。 |

### 设置曲面等分网格

> 英文名 `glMapGrid2`

先使用“设置曲面控制点”命令生成曲面，然后设置曲面的等分网格，让曲面光滑的过度。
	最终得到的每两个相邻U参数的间隔为：（U向等分下限 - U向等分上限）/ U向等分数。
	最终得到的每两个相邻V参数的间隔为：（V向等分下限 - V向等分上限）/ V向等分数。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `U向等分数` | 整数型 | - | 等分数目越多，曲线越光滑，占用的系统资源也越大。 |
| 2 | `U向等分上限` | 双精度小数型 | - | U参数的起始值。 |
| 3 | `U向等分下限` | 双精度小数型 | - | U参数的终止值。 |
| 4 | `V向等分数` | 整数型 | - | 等分数目越多，曲线越光滑，占用的系统资源也越大。 |
| 5 | `V向等分上限` | 双精度小数型 | - | V参数的起始值。 |
| 6 | `V向等分下限` | 双精度小数型 | - | V参数的终止值。 |

### 画曲面

> 英文名 `glEvalMesh2`

先使用“设置曲面控制点”命令生成曲面，然后才能画出曲面。U向显示范围在“曲面U向起点”和“曲面U向起点”之间。V向显示范围在“曲面V向起点”和“曲面V向起点”之间。必须启用“#曲面坐标”才有效，不使用的时候需要关闭。如果需要使用法向量，必须启用“#自动法向量”才有效，不使用的时候需要关闭。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `画出模式` | 整数型 | - | 参数值可以为以下常量：<br>	6912、#点，将画出的多边形显示为点；<br>	6913、#线，将画出的多边形显示为线段；<br>	6914、#面，将画出的多边形显示为填充颜色的多边形 |
| 2 | `曲面U向起点` | 整数型 | - | 参数值应该介于 0 和“设置曲面等分网格”命令中的“U向等分数”之间。 |
| 3 | `曲面U向终点` | 整数型 | - | 参数值应该介于 0 和“设置曲面等分网格”命令中的“U向等分数”之间。 |
| 4 | `曲面V向起点` | 整数型 | - | 参数值应该介于 0 和“设置曲面等分网格”命令中的“V向等分数”之间。 |
| 5 | `曲面V向终点` | 整数型 | - | 参数值应该介于 0 和“设置曲面等分网格”命令中的“V向等分数”之间。 |

### 是否存在纹理号

> 英文名 `glIsTexture` ｜ 返回值 **逻辑型**

如果该纹理号已经存在就返回真，否则返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `纹理` | 整数型 | - | 需要进行判断的纹理号。 |

### 删除纹理号

> 英文名 `glDeleteTextures` ｜ 返回值 **整数型**

删除已经创建的纹理号，与纹理号相应的纹理将无法使用。返回实际删除的纹理号的数量

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `需要删除的纹理号数量` | 整数型 | 可空 | 默认删除纹理数组中所有的纹理号，否则删除数组中指定的前几项。 |
| 2 | `纹理号数组` | 整数型 | 任意类型 | 保存了纹理号的数组。 |

### 清空名字栈

> 英文名 `glInitNames` ｜ 返回值 **整数型**

清空名字栈中的所有名字。返回最大名字栈长度。必须在选择模式下使用。否则无效。

### 名字入栈

> 英文名 `glPushName`

设置接下来的图元的名称。并把指定名称压入栈顶,不能超出最大名字栈长度。每个图元的名字与当时出现在名字栈中所有的名字有关。因此一个图元可以拥有多个名字。连续重复使用本命令即可达到命名多级名字的效果。必须在选择模式下使用。否则无效。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `名字` | 整数型 | - | 压入栈顶的名字。 |

### 名字出栈

> 英文名 `glPopName`

停止使用当前的名称。并把栈顶名称退出栈顶名称，不能在名字栈为空的时候出栈。必须在选择模式下使用。否则无效。

### 置栈顶名字

> 英文名 `glLoadName` ｜ 返回值 **逻辑型**

设置接下来的图元的名称。如果名字栈为空，将返回假。必须在选择模式下使用。否则无效。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `名字` | 整数型 | - | 栈顶的名字。 |

### 进入选择模式

> 英文名 `glSelect` ｜ 返回值 **整数型**

进入选择模式后画出的任何图元都不会显示出来。只有在选择模式下对模型进行命名的操作才有效。成功后将所有在选择区域中包含的命中记录存放在数组参数中。并且返回命中记录的个数。否则清空数组，并且返回0。
	选择缓存内容的排列规则：
		1.命中级别： 如果为顶级记录，值为1，二级为2，依次类推。
		2.最近距离：只表示在屏幕内的深度，并不对应Z坐标，数值为VC中的 unsigned int 数据类型，需要转换到0~4294967295范围的值。不同的投影方式，会有不同的结果。
		3.最远距离：同上。
		4.名字列表：成员1表示顶级名字，成员2表示二级名字，依次类推，最后一个成员表示命中名字。
		5.如果存在其他命中记录就重复1-4步。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `屏幕横向位置` | 整数型 | - | 在当前OPenGL所显示的窗口组件中的横向位置 |
| 2 | `屏幕纵向位置` | 整数型 | - | 在当前OPenGL所显示的窗口组件中的纵向位置 |
| 3 | `选择区域宽度` | 整数型 | - | 需要进行选择的区域的宽度，宽度越大搜索范围越大，计算量也越大。 |
| 4 | `选择区域高度` | 整数型 | - | 需要进行选择的区域的高度，高度越大搜索范围越大，计算量也越大。 |
| 5 | `命名图元子程序` | 子程序指针型 | - | 子程序没有使用参数和返回值。只有在这个子程序中使用与名字栈有关的命令才有效。 |
| 6 | `存放选择选择缓存的数组` | 整数型 | - | 原来的数据将被清除。并重新定义为一维数组。 |

### 到屏幕坐标

> 英文名 `gluProject` ｜ 返回值 **逻辑型**

将OPenGL的空间坐标转换到组件的屏幕坐标。成功返回真，否则返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `空间坐标横向位置` | 双精度小数型 | - | OPenGL的空间坐标系中的横向位置 |
| 2 | `空间坐标竖向位置` | 双精度小数型 | - | OPenGL的空间坐标系中的竖向位置 |
| 3 | `空间坐标纵向位置` | 双精度小数型 | - | OPenGL的空间坐标系中的纵向位置 |
| 4 | `存放屏幕坐标横向位置的变量` | 双精度小数型 | 参考 | 窗口组件中的横向位置 |
| 5 | `存放屏幕坐标纵向位置的变量` | 双精度小数型 | 参考 | 窗口组件中的纵向位置 |
| 6 | `存放屏幕深度的变量` | 双精度小数型 | 参考 | 0表示与投影坐标系中的最近的平面,1表示最远的平面。 |

### 到空间坐标

> 英文名 `gluUnProject` ｜ 返回值 **逻辑型**

将组件的屏幕坐标转换到OPenGL的空间坐标。成功返回真，否则返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `屏幕坐标横向位置` | 双精度小数型 | - | 窗口组件中的横向位置 |
| 2 | `屏幕坐标纵向位置` | 双精度小数型 | - | 窗口组件中的纵向位置 |
| 3 | `屏幕深度` | 双精度小数型 | - | 0表示与投影坐标系中的最近的平面,1表示最远的平面。 |
| 4 | `存放空间坐标横向位置的变量` | 双精度小数型 | 参考 | OPenGL的空间坐标系中的横向位置 |
| 5 | `存放空间坐标竖向位置的变量` | 双精度小数型 | 参考 | OPenGL的空间坐标系中的竖向位置 |
| 6 | `存放空间坐标纵向位置的变量` | 双精度小数型 | 参考 | OPenGL的空间坐标系中的纵向位置 |

### 载入TGA图片

> 英文名 `glTGALoad` ｜ 返回值 **逻辑型**

成功载入返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `位图文件名` | 文本型 | - | 允许使用24位、32位的压缩和非压缩的TGA文件格式 |
| 2 | `背景色` | 整数型 | 可空 | 位图中指定为背景色的区域，其颜色值的通道部分将被定义为0。如果为空，将被定义为1。可以通过使用“通道混合测试”进行处理。<br>	如果希望将背景色部分显示为透明，可以执行：<br>	  启用功能 (#通道测试)<br>	  通道混合测试 (#大于, 0)<br>	  设置纹理映射方式 (#纹理映射模式, #调节处理) |
| 3 | `宽度` | 整数型 | 参考 | 保存图片的宽度的变量 |
| 4 | `高度` | 整数型 | 参考 | 保存图片的高度的变量 |


## 常量

| 名称 | 值 | 说明 |
|---|---|---|
| `GL_画入窗口的像素格式` |  |  |
| `GL_支持GDI像素格式` |  | 与#双缓存像素格式互斥 |
| `GL_支持OPENGL像素格式` |  |  |
| `GL_双缓存像素格式` |  | 与#支持GDI像素格式互斥 |
| `GL_观察矩阵` |  |  |
| `GL_投影矩阵` |  |  |
| `GL_纹理矩阵` |  |  |
| `GL_点集` |  |  |
| `GL_线段` |  |  |
| `GL_闭合线段` |  |  |
| `GL_连续线段` |  |  |
| `GL_三角形` |  |  |
| `GL_连续三角形` |  |  |
| `GL_扇形三角形` |  |  |
| `GL_四边形` |  |  |
| `GL_连续四边形` |  |  |
| `GL_凸多边形` |  |  |
| `GL_顺时针` |  |  |
| `GL_逆时针` |  |  |
| `GL_零号裁剪面` |  |  |
| `GL_一号裁剪面` |  |  |
| `GL_二号裁剪面` |  |  |
| `GL_三号裁剪面` |  |  |
| `GL_四号裁剪面` |  |  |
| `GL_五号裁剪面` |  |  |
| `GL_正面` |  |  |
| `GL_背面` |  |  |
| `GL_正面和背面` |  |  |
| `GL_点` |  |  |
| `GL_线` |  |  |
| `GL_面` |  |  |
| `GL_透视修正质量` |  |  |
| `GL_点质量` |  |  |
| `GL_线质量` |  |  |
| `GL_多边形质量` |  |  |
| `GL_雾质量` |  |  |
| `GL_不考虑` |  |  |
| `GL_快速` |  |  |
| `GL_最佳` |  |  |
| `GL_失效` |  |  |
| `GL_小于` |  |  |
| `GL_等于` |  |  |
| `GL_小于等于` |  |  |
| `GL_大于` |  |  |
| `GL_不等于` |  |  |
| `GL_大于等于` |  |  |
| `GL_有效` |  |  |
| `GL_保持当前值` |  |  |
| `GL_替换当前值` |  |  |
| `GL_增加当前值` |  |  |
| `GL_减去当前值` |  |  |
| `GL_倒置当前值` |  |  |
| `GL_源值颜色比例` |  |  |
| `GL_源值颜色比例反` |  |  |
| `GL_源值通道比例` |  |  |
| `GL_源值通道比例反` |  |  |
| `GL_目标值通道比例` |  |  |
| `GL_目标值通道比例反` |  |  |
| `GL_目标值颜色比例` |  |  |
| `GL_目标值颜色比例反` |  |  |
| `GL_源值饱和通道` |  |  |
| `GL_获取缓存` |  |  |
| `GL_载入缓存` |  |  |
| `GL_返回缓存` |  |  |
| `GL_缩放缓存` |  |  |
| `GL_加入缓存` |  |  |
| `GL_深度缓存位` |  |  |
| `GL_累积缓存位` |  |  |
| `GL_模板缓存位` |  |  |
| `GL_颜色缓存位` |  |  |
| `GL_通道测试` |  |  |
| `GL_混合测试` |  |  |
| `GL_材质颜色` |  |  |
| `GL_深度测试` |  |  |
| `GL_雾` |  |  |
| `GL_光照` |  |  |
| `GL_线型` |  |  |
| `GL_单位法向量` |  |  |
| `GL_精选面` |  |  |
| `GL_图案填充` |  |  |
| `GL_裁剪测试` |  |  |
| `GL_模板测试` |  |  |
| `GL_一维纹理` |  |  |
| `GL_二维纹理` |  |  |
| `GL_生成S向纹理` |  |  |
| `GL_生成T向纹理` |  |  |
| `GL_零号光源` |  |  |
| `GL_一号光源` |  |  |
| `GL_二号光源` |  |  |
| `GL_三号光源` |  |  |
| `GL_四号光源` |  |  |
| `GL_五号光源` |  |  |
| `GL_六号光源` |  |  |
| `GL_七号光源` |  |  |
| `GL_环境光` |  |  |
| `GL_漫反射光` |  |  |
| `GL_镜面光` |  |  |
| `GL_光源位置` |  |  |
| `GL_光源聚光方向` |  |  |
| `GL_光源聚光指数` |  |  |
| `GL_点光源聚光截止角` |  |  |
| `GL_常量衰减` |  |  |
| `GL_线形衰减` |  |  |
| `GL_二次方衰减` |  |  |
| `GL_光源观察点模式` |  |  |
| `GL_双面光模式` |  |  |
| `GL_全局光模式` |  |  |
| `GL_散射光` |  |  |
| `GL_亮光` |  |  |
| `GL_环境光和漫反射光` |  |  |
| `GL_颜色索引` |  |  |
| `GL_雾索引` |  |  |
| `GL_雾密度` |  |  |
| `GL_雾起点` |  |  |
| `GL_雾终点` |  |  |
| `GL_雾模式` |  |  |
| `GL_线性` |  |  |
| `GL_雾颜色` |  |  |
| `GL_雾模式1` |  |  |
| `GL_雾模式2` |  |  |
| `GL_平坦` |  |  |
| `GL_光滑` |  |  |
| `GL_纹理插值约束` |  |  |
| `GL_纹理重复约束` |  |  |
| `GL_放大纹理滤波` |  |  |
| `GL_缩小纹理滤波` |  |  |
| `GL_S向纹理约束` |  |  |
| `GL_T向纹理约束` |  |  |
| `GL_最近滤镜` |  |  |
| `GL_线性滤镜` |  |  |
| `GL_最近多贴图滤镜` |  |  |
| `GL_多贴图线性内插滤镜` |  |  |
| `GL_线性内插多贴图滤镜` |  |  |
| `GL_内插多贴图的线性内插滤镜` |  |  |
| `GL_单个颜色索引` |  |  |
| `GL_模板索引` |  |  |
| `GL_红色分量` |  |  |
| `GL_绿色分量` |  |  |
| `GL_蓝色分量` |  |  |
| `GL_混合通道分量` |  |  |
| `GL_红绿蓝分量` |  |  |
| `GL_红绿蓝混合通道分量` |  |  |
| `GL_亮度` |  |  |
| `GL_亮度和混合通道分量` |  |  |
| `GL_纹理映射模式` |  |  |
| `GL_纹理映射颜色` |  |  |
| `GL_调节处理` |  |  |
| `GL_粘贴处理` |  |  |
| `GL_S分量` |  |  |
| `GL_T分量` |  |  |
| `GL_纹理生成模式` |  |  |
| `GL_模型平面` |  |  |
| `GL_观测平面` |  |  |
| `GL_观测线性插值` |  |  |
| `GL_模型线性插值` |  |  |
| `GL_纹理贴图形状` |  |  |
| `GL_编译列表` |  |  |
| `GL_编译并且执行列表` |  |  |
| `GL_颜色类型` |  |  |
| `GL_深度类型` |  |  |
| `GL_模板类型` |  |  |
| `GL_光滑线` |  |  |
| `GL_光滑点` |  |  |
| `GL_曲线坐标` |  |  |
| `GL_曲面坐标` |  |  |
| `GL_自动法向量` |  |  |
