# zySqlite3加密支持库

- **库标识**: `esqlite31`
- **版本**: 1.1
- **GUID**: `E3ED75AB9F47436EAD9407F4ACB63F14`
- **作者**: kyozy
- **文件**: `esqlite31`
- **可用命令数**: 112

封装wxsqlite3

## 数据类型

| 名称 | 说明 |
|---|---|
| `zySqlite数据库` | 数据库相关操作类，数据库打开后必须手动关闭 |
| `zySqlite记录集` | 记录集相关操作类，必须手动关闭记录集 |
| `zySqlite字段信息` | 创建表、添加字段时提供的字段信息 |

## 命令

### S3初始化

> 英文名 `sqlite3_initialize` ｜ 返回值 **整数型**

初始化Sqlite库，返回“SQLITE_”开头常量。

### S3卸载

> 英文名 `sqlite3_shutdown` ｜ 返回值 **整数型**

释放由 初始化S3() 分配的资源，返回“SQLITE_”开头常量。卸载之前必须关闭所有打开的数据库或其他资源。

### S3配置

> 英文名 `sqlite3_config` ｜ 返回值 **整数型**

此方法用于更改一些全局配置，必须在 初始化S3() 之前调用。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `配置项` | 整数型 | - | “SQLITE_配置_”开头常量 |
| 2 | `参数1` | 整数型 | 可空 |  |
| 3 | `参数2` | 整数型 | 可空 |  |

### S3取版本

> 英文名 `sqlite3_libversion` ｜ 返回值 **文本型**

返回版本的文本形式

### S3取版本号

> 英文名 `sqlite3_libversion_number` ｜ 返回值 **整数型**

返回版本的数字形式

### S3取用户数据

> 英文名 `sqlite3_user_data` ｜ 返回值 **整数型**

返回创建函数时提供的用户数据，这个方法应该是在创建函数的回调函数中使用。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `Sqlite上下文` | 整数型 | - |  |

### S3取参数整数

> 英文名 `sqlite3_value_int` ｜ 返回值 **整数型**

返回参数的整数值，这个方法应该是在创建函数的回调函数中使用。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `参数指针` | 整数型 | - |  |
| 2 | `索引` | 整数型 | - | 索引从0开始 |

### S3取参数长整数

> 英文名 `sqlite3_value_int64` ｜ 返回值 **长整数型**

返回参数的长整数值，这个方法应该是在创建函数的回调函数中使用。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `参数指针` | 整数型 | - |  |
| 2 | `索引` | 整数型 | - | 索引从0开始 |

### S3取参数双精度

> 英文名 `sqlite3_value_double` ｜ 返回值 **双精度小数型**

返回参数的双精度小数值，这个方法应该是在创建函数的回调函数中使用。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `参数指针` | 整数型 | - |  |
| 2 | `索引` | 整数型 | - | 索引从0开始 |

### S3取参数文本

> 英文名 `sqlite3_value_text` ｜ 返回值 **文本型**

返回参数的文本值，这个方法应该是在创建函数的回调函数中使用。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `参数指针` | 整数型 | - |  |
| 2 | `索引` | 整数型 | - | 索引从0开始 |

### S3取参数字节集

> 英文名 `sqlite3_value_blob` ｜ 返回值 **字节集型**

返回参数的字节集值，这个方法应该是在创建函数的回调函数中使用。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `参数指针` | 整数型 | - |  |
| 2 | `索引` | 整数型 | - | 索引从0开始 |

### S3取参数类型

> 英文名 `sqlite3_value_type` ｜ 返回值 **整数型**

返回参数的类型，“SQLITE_类型_”开头常量，这个方法应该是在创建函数的回调函数中使用。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `参数指针` | 整数型 | - |  |
| 2 | `索引` | 整数型 | - | 索引从0开始 |

### S3返回文本

> 英文名 `sqlite3_result_text`

指定创建自定义函数的返回值，这个方法应该是在创建函数的回调函数中使用。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `Sqlite上下文` | 整数型 | - |  |
| 2 | `文本` | 文本型 | - | 用作返回的文本 |
| 3 | `静态` | 逻辑型 | 有默认值 |  |

### S3返回字节集

> 英文名 `sqlite3_result_text`

指定创建自定义函数的返回值，这个方法应该是在创建函数的回调函数中使用。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `Sqlite上下文` | 整数型 | - |  |
| 2 | `字节集` | 字节集型 | - | 用作返回的字节集 |
| 3 | `静态` | 逻辑型 | 有默认值 |  |

### S3返回整数

> 英文名 `sqlite3_result_int`

指定创建自定义函数的返回值，这个方法应该是在创建函数的回调函数中使用。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `Sqlite上下文` | 整数型 | - |  |
| 2 | `整数` | 整数型 | - | 用作返回的整数 |

### S3返回长整数

> 英文名 `sqlite3_result_int64`

指定创建自定义函数的返回值，这个方法应该是在创建函数的回调函数中使用。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `Sqlite上下文` | 整数型 | - |  |
| 2 | `长整数` | 长整数型 | - | 用作返回的长整数 |

### S3返回双精度

> 英文名 `sqlite3_result_int64`

指定创建自定义函数的返回值，这个方法应该是在创建函数的回调函数中使用。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `Sqlite上下文` | 整数型 | - |  |
| 2 | `双精度` | 双精度小数型 | - | 用作返回的双精度小数 |

### S3返回空

> 英文名 `sqlite3_result_null`

指定创建自定义函数的返回值，这个方法应该是在创建函数的回调函数中使用。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `Sqlite上下文` | 整数型 | - |  |

### S3返回错误信息

> 英文名 `sqlite3_result_error`

指定创建自定义函数的错误信息，这个方法应该是在创建函数的回调函数中使用。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `Sqlite上下文` | 整数型 | - |  |
| 2 | `错误信息` | 文本型 | - | 用作返回的错误信息 |

### S3返回错误代码

> 英文名 `sqlite3_result_error_code`

指定创建自定义函数的错误代码，这个方法应该是在创建函数的回调函数中使用。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `Sqlite上下文` | 整数型 | - |  |
| 2 | `错误代码` | 整数型 | - | 用作返回的错误代码，“SQLITE_”开头常量 |

### S3取线程安全

> 英文名 `sqlite3_threadsafe` ｜ 返回值 **整数型**

仅返回编译时[SQLITE_THREADSAFE]宏的值，0表示单线程，1表示串行模式，2表示多线程。

### S3ToGB2312Ptr

> 英文名 `Utf8PointToGB2312` ｜ 返回值 **文本型**

将UTF8文本指针转换为GB2312文本。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `文本指针` | 整数型 | - |  |

### WXS3取配置

> 英文名 `wxsqlite3_config` ｜ 返回值 **整数型**

获取默认配置

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `配置名` | 文本型 | - | “wxSQLITE_Config_”开头常量 |

### WXS3取配置加密

> 英文名 `wxsqlite3_config_cipher` ｜ 返回值 **整数型**

获取指定加密算法配置

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `加密名` | 文本型 | - | “wxSQLITE_Cipher_”开头常量 |
| 2 | `配置名` | 文本型 | - | “wxSQLITE_Config_”开头常量 |

### S3互斥体进入

> 英文名 `sqlite3_mutex_enter`

类似 进入许可区

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `互斥体句柄` | 整数型 | - | 可由 数据库.取互斥体() 返回。 |

### S3互斥体退出

> 英文名 `sqlite3_mutex_leave`

类似 退出许可区

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `互斥体句柄` | 整数型 | - | 可由 数据库.取互斥体() 返回。 |

### 关闭

> 英文名 `sqlite3_close` ｜ 返回值 **逻辑型**

关闭数据库的连接。如果有未完成的准备语句等，则会返回 #SQLITE_繁忙。

### 关闭V2

> 英文名 `sqlite3_close_v2` ｜ 返回值 **逻辑型**

关闭数据库的连接。如果有未完成的准备语句等，会延迟释放相关资源。

### 打开

> 英文名 `sqlite3_open_v2` ｜ 返回值 **逻辑型**

打开数据库。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `文件名` | 通用型 | - | 数据库文件路径，如果为“:memory:”表示内存数据库；如果为“”表示临时数据库。 |
| 2 | `打开选项` | 整数型 | 有默认值 | “SQLITE_打开_”开头常量组合 |

### 密码

> 英文名 `sqlite3_key` ｜ 返回值 **整数型**

指定加密数据库密码，这个方法应该在 打开数据库 之后调用。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `密码` | 通用型 | - | 数据库密码。只能传递文本或字节集 |
| 2 | `数据库名` | 文本型 | 可空 | 指定数据库的名称 |

### 重置密码

> 英文名 `sqlite3_rekey` ｜ 返回值 **整数型**

如果之前不是加密数据库，则此方法为数据库增加密码，如果已经是加密数据库，则重新修改密码。这个方法应该在 打开数据库 之后调用。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `密码` | 通用型 | - | 数据库密码。只能传递文本或字节集 |
| 2 | `数据库名` | 文本型 | 可空 | 指定数据库的名称 |

### 执行SQL语句

> 英文名 `sqlite3_exec` ｜ 返回值 **逻辑型**

执行一条或多条SQL语句。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `SQL语句` | 通用型 | - | 多条语句用;分开 |
| 2 | `回调函数` | 通用型 | 可空 | 返回值：整数型（整数型 用户数据，整数型 列数量，整数型 数据指针，整数型 列名称指针） |
| 3 | `用户数据` | 整数型 | 可空 | 传递给回调函数的第一个参数 |
| 4 | `错误信息` | 文本型 | 可空、参考 |  |

### 取错误代码

> 英文名 `sqlite3_errcode` ｜ 返回值 **整数型**

返回“SQLITE_”开头常量。

### 取错误文本

> 英文名 `sqlite3_errmsg` ｜ 返回值 **文本型**

### 取句柄

> 英文名 `getdb` ｜ 返回值 **整数型**

### 置句柄

> 英文名 `setdb` ｜ 返回值 **整数型**

设置数据库句柄当当前类

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `数据库句柄` | 整数型 | - |  |

### 取记录集

> 英文名 `sqlite3_prepare_v2` ｜ 返回值 **zySqlite字段信息**

准备一条记录集

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `SQL语句` | 通用型 | - |  |
| 2 | `执行结果` | 逻辑型 | 可空、参考 |  |

### 取最后插入ID

> 英文名 `sqlite3_last_insert_rowid` ｜ 返回值 **长整数型**

每一个表都会有一个rowid列，如果已经有主键自增列，rowid则表示这个主键自增列的别名，这个方法返回最后插入的rowid

### 取影响行

> 英文名 `sqlite3_changes` ｜ 返回值 **整数型**

返回上一次insert、update、delete等操作的行数。

### 开始事务

> 英文名 `BeginTransaction` ｜ 返回值 **整数型**

开始一个新的事务，如果提供<事务名称>参数则可以嵌套，但是不能使用<事务锁状态>参数。成功返回 #SQLITE_成功。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `事务名称` | 文本型 | 可空 |  |
| 2 | `事务锁状态` | 整数型 | 可空 | SQLITE3_事务锁状态_ 开头常量，如果提供了此参数，则事务名称无效。默认为：SQLITE3_事务锁状态_推迟 |

### 提交事务

> 英文名 `CommitTransaction` ｜ 返回值 **整数型**

成功返回 #SQLITE_成功。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `事务名称` | 文本型 | 可空 |  |

### 回滚事务

> 英文名 `RollbackTransaction` ｜ 返回值 **整数型**

成功返回 #SQLITE_成功。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `事务名称` | 文本型 | 可空 |  |

### 枚举表

> 英文名 `EnumTable` ｜ 返回值 **zySqlite字段信息**

返回记录集，字段0：表名称；字段1：创建时的SQL语句。

### 枚举索引

> 英文名 `EnumIndex` ｜ 返回值 **zySqlite字段信息**

返回记录集，字段0：索引名；字段1：创建时的SQL语句。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `表名` | 文本型 | - |  |

### 枚举视图

> 英文名 `EnumView` ｜ 返回值 **zySqlite字段信息**

返回记录集，字段0：视图名称；字段1：创建时的SQL语句。

### 枚举触发器

> 英文名 `EnumTrigger` ｜ 返回值 **zySqlite字段信息**

返回记录集，字段0：触发器名；字段1：创建时的SQL语句。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `表名` | 文本型 | - |  |

### 表是否存在

> 英文名 `IsTableExist` ｜ 返回值 **逻辑型**

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `表名` | 文本型 | - |  |

### 取表内容

> 英文名 `sqlite3_get_table` ｜ 返回值 **文本型**

获取表的全部内容，返回一个二维文本数组

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `SQL语句` | 通用型 | - |  |
| 2 | `行数` | 整数型 | 可空、参考 | 包含表头的行数 |
| 3 | `列数` | 整数型 | 可空、参考 |  |
| 4 | `错误信息` | 文本型 | 可空、参考 |  |
| 5 | `执行结果` | 逻辑型 | 可空、参考 |  |

### 取表定义

> 英文名 `GetTableSql` ｜ 返回值 **文本型**

返回创建指定表时所用的SQL语句（CREATE TABLE ...）。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `表名` | 文本型 | - |  |

### 创建表

> 英文名 `CreateTable` ｜ 返回值 **逻辑型**

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `表名` | 文本型 | - |  |
| 2 | `字段信息` | 库类型#3 | 数组 |  |
| 3 | `联合唯一` | 文本型 | 可空 | 字段名, 多个字段用“,”分开。如：“Column1 ACS,Column2 DESC” |
| 4 | `联合主键` | 文本型 | 可空 | 字段名, 多个字段用“,”分开。如：“Column1 ACS,Column2 DESC” |

### 删除表

> 英文名 `DeleteTable` ｜ 返回值 **逻辑型**

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `表名` | 文本型 | - |  |

### 重命名表

> 英文名 `ReNameTable` ｜ 返回值 **逻辑型**

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `源表名` | 文本型 | - |  |
| 2 | `新表名` | 文本型 | - |  |

### 添加字段

> 英文名 `AddColumn` ｜ 返回值 **逻辑型**

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `表名` | 文本型 | - |  |
| 2 | `字段信息` | 库类型#3 | - |  |

### 创建索引

> 英文名 `CreateIndex` ｜ 返回值 **逻辑型**

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `索引名` | 文本型 | - |  |
| 2 | `表名` | 文本型 | - |  |
| 3 | `字段名` | 文本型 | - | 多个字段用“,”分开。如：“Column1 ACS,Column2 DESC” |
| 4 | `唯一` | 逻辑型 | 有默认值 |  |

### 删除索引

> 英文名 `DeleteIndex` ｜ 返回值 **逻辑型**

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `索引名` | 文本型 | - |  |

### 创建视图

> 英文名 `CreateView` ｜ 返回值 **逻辑型**

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `视图名` | 文本型 | - |  |
| 2 | `查询语句` | 文本型 | - | select 语句 |
| 3 | `是否临时视图` | 逻辑型 | 有默认值 |  |

### 删除视图

> 英文名 `DeleteView` ｜ 返回值 **逻辑型**

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `视图名` | 文本型 | - |  |

### 创建触发器

> 英文名 `CreateTrigger` ｜ 返回值 **逻辑型**

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `触发器名` | 文本型 | - |  |
| 2 | `表名` | 文本型 | - |  |
| 3 | `执行语句` | 文本型 | - | 当事件成立时，所触发的SQL语句。注意：必须以“;”号结尾 |
| 4 | `触发条件` | 整数型 | - | “SQLITE3_触发条件_”开头常量。 |
| 5 | `字段名` | 文本型 | 可空 | 当参数<触发条件> =“SQLITE3_触发条件_字段更新”时，指定字段名，多个字段用“,”分开 |
| 6 | `触发时间` | 整数型 | 可空 | “SQLITE3_触发时间_”开头常量。默认为“SQLITE3_触发时间_普通模式” |
| 7 | `是否临时触发器` | 逻辑型 | 有默认值 |  |

### 删除触发器

> 英文名 `DeleteTrigger` ｜ 返回值 **逻辑型**

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `触发器名` | 文本型 | - |  |

### 收缩数据库

> 英文名 `ShrinkDB` ｜ 返回值 **逻辑型**

收缩数据所占用的磁盘空间。经过大批量记录增删之后，数据库文件可能会变的较大，调用此方法可释放一部分缓冲区域，以减少文件尺寸。

### 附加数据库

> 英文名 `AttachDatabase` ｜ 返回值 **逻辑型**

附加一个数据库到当前连接，使之可以同时操作多个数据库。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `文件名` | 通用型 | - | 数据库文件路径 |
| 2 | `数据库名` | 文本型 | - | 以后可以通过这个数据库名，访问附加的数据库。如：“数据库名.表名” |

### 分离数据库

> 英文名 `DetachDatabase` ｜ 返回值 **逻辑型**

拆分一个之前使用“对象.附加数据库()”附加的数据库连接。此方法不能用于事务中

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `数据库名` | 文本型 | - | 附加的数据库名 |

### 备份数据库

> 英文名 `BackupDatabase` ｜ 返回值 **逻辑型**

备份指定的数据库到指定文件

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `目标数据库` | zySqlite记录集 | - | 备份到此数据库 |
| 2 | `目标数据库名` | 文本型 | 可空 | 备份到此数据库名，默认为：main 主数据库。 |
| 3 | `源数据库名` | 文本型 | 可空 | 源数据库名，默认为：main 主数据库。 |
| 4 | `回调函数` | 通用型 | 可空 | 无返回值 (整数型 剩余页数，整数型 总页数)  |

### 创建函数

> 英文名 `CreateFunction` ｜ 返回值 **逻辑型**

创建一个自定义Sql函数

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `函数名` | 文本型 | - |  |
| 2 | `参数个数` | 整数型 | - | 如果为 -1 表示任意参数个数 |
| 3 | `用户数据` | 整数型 | 有默认值 | 传递给析构函数的参数 |
| 4 | `实现函数` | 通用型 | - | 无返回值 (整数型 sqlite3上下文，整数型 参数个数，整数型 参数指针)。 |
| 5 | `析构函数` | 通用型 | 可空 | 无返回值 (整数型 用户数据)，当数据库关闭或函数被覆盖时调用此方法。 |

### 创建聚合函数

> 英文名 `CreateAggFunction` ｜ 返回值 **逻辑型**

创建一个自定义Sql聚合函数，类似sum()

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `函数名` | 文本型 | - |  |
| 2 | `参数个数` | 整数型 | - | 如果为 -1 表示任意参数个数 |
| 3 | `用户数据` | 整数型 | 有默认值 | 传递给析构函数的参数 |
| 4 | `单步函数` | 通用型 | - | 无返回值 (整数型 sqlite3上下文，整数型 参数个数，整数型 参数指针)。  |
| 5 | `完成函数` | 通用型 | - | 无返回值 (整数型 sqlite3上下文)。  |
| 6 | `析构函数` | 通用型 | 可空 | 无返回值 (整数型 用户数据)，当数据库关闭或函数被覆盖时调用此方法。 |

### 繁忙超时

> 英文名 `BusyTimeOut` ｜ 返回值 **整数型**

当数据库处于繁忙状态时，可用此方法等待指定的毫秒数。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `等待毫秒` | 整数型 | - | 如果小于或等于0，则取消繁忙处理程序 |

### 繁忙处理

> 英文名 `BusyHandler` ｜ 返回值 **整数型**

当数据库处于繁忙状态时，可用此方法等待，会调用回调函数，当回调函数返回0停止等待，返回非0则继续等待。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `处理程序` | 通用型 | - | 返回值 整数型（整数型 用户数据，整数型 处理次数）。只能提供 【子程序指针】 或 【整数型】 如果为0则取消处理程序 |
| 2 | `用户数据` | 整数型 | 可空 | 传递给处理程序的参数 |

### 取文件名

> 英文名 `GetFileName` ｜ 返回值 **文本型**

获取当前数据库的文件名。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `数据库名` | 文本型 | 可空 | 留空默认为：main |

### 是否只读

> 英文名 `IsReadOnly` ｜ 返回值 **逻辑型**

判断当前数据库是否只读。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `数据库名` | 文本型 | 可空 | 留空默认为：main |

### 取互斥体

> 英文名 `GetDBMutex` ｜ 返回值 **整数型**

仅当配置为 串行化 模式时，可返回数据库的互斥体。

### 是否自动提交

> 英文名 `IsAutoCommit` ｜ 返回值 **逻辑型**

判断数据库是否处于自动提交模式。默认情况下，自动提交模式是打开的。 自动提交模式被开始事务禁用。 通过提交事务或回滚事务重新启用自动提交模式。

### 进度处理

> 英文名 `ProgressHandler`

在 数据库.执行SQL语句()、数据库.取表内容()和 记录集.到下一行() 时，提供的进度处理回调函数，用于更新UI。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `近似总数` | 整数型 | - | 在回调函数的连续调用之间评估的虚拟机指令的近似数量。如果小于1，将禁用进度处理回调 |
| 2 | `处理程序` | 通用型 | - | 返回值 整数型（整数型 用户数据）。返回非0停止处理。只能提供 【子程序指针】 或 【整数型】 |
| 3 | `用户数据` | 整数型 | 可空 | 传递给处理程序的参数 |

### 取下一记录集

> 英文名 `NextStmt` ｜ 返回值 **zySqlite字段信息**

获取与本数据库所关联的当前记录集的下一个记录集，可能返回空记录集。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `当前记录集` | zySqlite字段信息 | - |  |

### 置配置

> 英文名 `wxsqlite3_config` ｜ 返回值 **整数型**

设置当前加密算法配置，必须在打开数据库后，设置密码前调用。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `配置名` | 文本型 | - | “wxSQLITE_Config_”开头常量 |
| 2 | `值` | 整数型 | - |  |

### 置配置加密

> 英文名 `wxsqlite3_config_cipher` ｜ 返回值 **整数型**

设置指定加密算法配置，必须在打开数据库后，设置密码前调用。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `加密名` | 文本型 | - | “wxSQLITE_Cipher_”开头常量 |
| 2 | `配置名` | 文本型 | - | “wxSQLITE_Config_”开头常量 |
| 3 | `值` | 整数型 | - |  |

### 关闭

> 英文名 `sqlite3_finalize` ｜ 返回值 **逻辑型**

### 置SQL语句

> 英文名 `sqlite3_prepare_v2` ｜ 返回值 **逻辑型**

设置本对象所使用的SQL语句。本方法等效于“zySqlite数据库.取记录集()”

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `SQL语句` | 通用型 | - |  |
| 2 | `数据库` | zySqlite记录集 | - |  |

### 到下一行

> 英文名 `sqlite3_step` ｜ 返回值 **逻辑型**

移动到下一条记录

### 读字段文本值

> 英文名 `sqlite3_column_text` ｜ 返回值 **文本型**

读取当前记录中指定字段的值，并转换为文本型数据后返回。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从0开始。 |
| 2 | `执行结果` | 逻辑型 | 可空、参考 | 如果提供本参数，其中将存放本方法执行结果——成功为“真”，失败为“假”。 |

### 读字段整数值

> 英文名 `sqlite3_column_int` ｜ 返回值 **整数型**

读取当前记录中指定字段的整数型值。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从0开始。 |
| 2 | `执行结果` | 逻辑型 | 可空、参考 | 如果提供本参数，其中将存放本方法执行结果——成功为“真”，失败为“假”。 |

### 读字段双精度值

> 英文名 `sqlite3_column_double` ｜ 返回值 **双精度小数型**

读取当前记录中指定字段的双精度小数型值。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从0开始。 |
| 2 | `执行结果` | 逻辑型 | 可空、参考 | 如果提供本参数，其中将存放本方法执行结果——成功为“真”，失败为“假”。 |

### 读字段长整数值

> 英文名 `sqlite3_column_int64` ｜ 返回值 **长整数型**

读取当前记录中指定字段的长整数型值。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从0开始。 |
| 2 | `执行结果` | 逻辑型 | 可空、参考 | 如果提供本参数，其中将存放本方法执行结果——成功为“真”，失败为“假”。 |

### 读字段字节集值

> 英文名 `sqlite3_column_blob` ｜ 返回值 **字节集型**

读取当前记录中指定字段的字节集型值。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从0开始。 |
| 2 | `执行结果` | 逻辑型 | 可空、参考 | 如果提供本参数，其中将存放本方法执行结果——成功为“真”，失败为“假”。 |

### 重置

> 英文名 `sqlite3_reset` ｜ 返回值 **整数型**

将记录集重置到第一条记录之前。

### 取记录集列数

> 英文名 `sqlite3_data_count` ｜ 返回值 **整数型**

如果以移动到记录尾，将返回0。

### 取字段数

> 英文名 `sqlite3_column_count` ｜ 返回值 **整数型**

### 取字段名

> 英文名 `sqlite3_column_name` ｜ 返回值 **文本型**

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段索引` | 整数型 | - | 索引从0开始 |

### 取字段表名

> 英文名 `sqlite3_column_table_name` ｜ 返回值 **文本型**

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从0开始。 |

### 取字段数据库名

> 英文名 `sqlite3_column_database_name` ｜ 返回值 **文本型**

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从0开始。 |

### 取字段长度

> 英文名 `sqlite3_column_bytes` ｜ 返回值 **整数型**

获取字段内容的字节长度。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从0开始。 |

### 取字段定义类型

> 英文名 `sqlite3_column_decltype` ｜ 返回值 **文本型**

返回定义的字段类型名

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从0开始。 |

### 取字段源名

> 英文名 `sqlite3_column_origin_name` ｜ 返回值 **文本型**

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从0开始。 |

### 取字段类型

> 英文名 `sqlite3_column_type` ｜ 返回值 **整数型**

返回“SQLITE_类型_”开头常量。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从0开始。 |

### 取SQL语句

> 英文名 `sqlite3_sql` ｜ 返回值 **文本型**

获取SQL语句。

### 取展开SQL语句

> 英文名 `sqlite3_expanded_sql` ｜ 返回值 **文本型**

获取展开后的SQL语句。

### 读字段值

> 英文名 `GetFieldValue` ｜ 返回值 **逻辑型**

读取当前记录中指定字段的值，并写入第二个参数“字段值”中。执行成功返回“真”，失败返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从0开始。 |
| 2 | `字段值` | 通用型 | 参考 | 其中将存放取得的字段值。如果必要，将自动进行适当的数据类型转换。 |

### 绑定参数

> 英文名 `sqlite3_bind` ｜ 返回值 **逻辑型**

应该先调用“zySqlite记录集.置SQL语句”或“zySqlite数据库.取记录集”，然后调用本方法来绑定SQL语句中的“?”、“?NNN”、“@VVV”、“:VVV”、“$VVV”值

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `参数名称或位置` | 通用型 | - | 使用参数名称时注意，“:”“$”“@”也是参数名称的一部分；使用参数索引时注意，索引从1开始。 |
| 2 | `要绑定的值` | 通用型 | 可空 | 如果省略本参数，默认为null。 |

### 取参数个数

> 英文名 `sqlite3_bind_parameter_count` ｜ 返回值 **整数型**

获取一个SQL语句中的参数个数。

### 清除绑定参数

> 英文名 `sqlite3_clear_bindings` ｜ 返回值 **逻辑型**

清除所有绑定的参数

### 取参数名

> 英文名 `sqlite3_bind_parameter_name` ｜ 返回值 **文本型**

根据参数索引获取参数名

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `参数索引` | 整数型 | - | 索引从1开始。 |

### 执行

> 英文名 `Execute` ｜ 返回值 **整数型**

执行准备好的记录集，如果是查询语句，返回 #SQLITE_行 表示还有下一行记录，返回 #SQLITE_完成 表示已经到了尾部，如果是非查询语句成功也也返回 #SQLITE_完成。

### 查询标量文本

> 英文名 `ExecuteScalar` ｜ 返回值 **文本型**

执行查询语句，并返回第一行第一列的文本数据。

### 查询标量

> 英文名 `ExecuteScalar` ｜ 返回值 **整数型**

执行查询语句，并返回第一行第一列的整数数据。

### 取句柄

> 英文名 `GethStmt` ｜ 返回值 **整数型**

### 是否繁忙

> 英文名 `IsBusy` ｜ 返回值 **逻辑型**

记录集至少执行了一次 到下一行，且没有到末尾，又没有用 重置 命令，这时返回真。

### 是否只读

> 英文名 `IsReadOnly` ｜ 返回值 **逻辑型**

如果记录集不会修改数据库的数据，则返回真。

### S3聚合上下文

> 英文名 `AggregateContext` ｜ 返回值 **整数型**

在同一聚合查询中，第一次调用会申请并清空内存返回，后续的调用会返回同一内存地址，sqlite3在聚合查询结束时会自动释放内存。

### S3取数据库自上下文

> 英文名 `ContextDBHandle` ｜ 返回值 **整数型**

返回数据库句柄。

### 取总影响行数

> 英文名 `GetTotalChanges` ｜ 返回值 **整数型**

返回自打开数据库连接以来已完成的所有INSERT，UPDATE或DELETE语句插入，修改或删除的行总数，包括作为触发器程序一部分执行的行。

### 取数据库句柄

> 英文名 `GetDBHandle` ｜ 返回值 **整数型**

获取与本记录集相关联的数据库句柄。

### 取行数

> 英文名 `GetLineCount` ｜ 返回值 **整数型**

获取记录集行数。如果有绑定参数，必须绑定参数后才能获取到行数。


## 常量

| 名称 | 值 | 说明 |
|---|---|---|
| `SQLITE_成功` |  |  |
| `SQLITE_错误` |  | 一般错误 |
| `SQLITE_内部错误` |  | SQLite中的内部逻辑错误 |
| `SQLITE_权限` |  | 访问权限被拒绝 |
| `SQLITE_终止` |  | 回滚事务请求中止 |
| `SQLITE_繁忙` |  | 数据库文件已锁定 |
| `SQLITE_锁定` |  | 数据库中的表已锁定 |
| `SQLITE_内存不足` |  | 分配内存失败 |
| `SQLITE_只读` |  | 尝试写入只读数据库 |
| `SQLITE_中断` |  |  |
| `SQLITE_IO错误` |  | 发生某种磁盘I / O错误 |
| `SQLITE_损坏` |  | 数据库磁盘映像格式错误 |
| `SQLITE_未找到` |  |  |
| `SQLITE_已满` |  | 插入失败，因为数据库已满 |
| `SQLITE_不能打开` |  | 无法打开数据库文件 |
| `SQLITE_协议` |  | 数据库锁协议错误 |
| `SQLITE_空` |  | 限内部使用 |
| `SQLITE_结构` |  | 数据库结构被改变 |
| `SQLITE_太大` |  | 文本或字节集超出大小限制 |
| `SQLITE_约束` |  | 由于约束冲突而中止 |
| `SQLITE_不匹配` |  | 数据类型不匹配 |
| `SQLITE_误用` |  | 库被不正确使用 |
| `SQLITE_系统不支持` |  | 使用主机不支持的操作系统功能 |
| `SQLITE_认证` |  | 授权被拒绝 |
| `SQLITE_格式` |  | 未使用 |
| `SQLITE_范围` |  | 绑定参数时索引超出范围 |
| `SQLITE_不是数据库` |  | 打开的文件不是数据库文件 |
| `SQLITE_通知` |  | 来自日志的通知 |
| `SQLITE_警告` |  | 来自日志的警告 |
| `SQLITE_行` |  | 记录集有下一行 |
| `SQLITE_完成` |  | 记录集完成执行 |
| `SQLITE_配置_单线程` |  | 无参数 |
| `SQLITE_配置_多线程` |  | 无参数，使用互斥锁，各个线程使用不同的连接和准备的SQL语句 |
| `SQLITE_配置_串行化` |  | 无参数，使用所有互斥锁，多个线程都可以使用相同的连接和准备的SQL语句 |
| `SQLITE_配置_内存状态` |  | 逻辑参数 启用或禁用内存分配统计信息 |
| `SQLITE_配置_日志` |  | 全局错误日志，第一个参数 void(*)(void*,int,const char*)，第二个参数 回调函数的第一个参数 |
| `SQLITE_配置_URI` |  | 逻辑参数 启用或禁用URI处理 |
| `SQLITE_配置_覆盖索引扫描` |  | 逻辑参数 启用或禁用查询优化器中覆盖索引以进行全表扫描 |
| `SQLITE_打开_只读` |  |  |
| `SQLITE_打开_读写` |  |  |
| `SQLITE_打开_创建` |  |  |
| `SQLITE_打开_URI` |  | 支持URI |
| `SQLITE_打开_内存` |  |  |
| `SQLITE_打开_无互斥` |  |  |
| `SQLITE_打开_全互斥` |  |  |
| `SQLITE_打开_共享缓存` |  |  |
| `SQLITE_打开_私有缓存` |  |  |
| `SQLITE_类型_整数型` |  |  |
| `SQLITE_类型_浮点型` |  |  |
| `SQLITE_类型_文本型` |  |  |
| `SQLITE_类型_字节集型` |  |  |
| `SQLITE_类型_空` |  |  |
| `SQLITE_字段属性_主键` |  | Primary Key |
| `SQLITE_字段属性_自动递增` |  | AutoIncrement |
| `SQLITE_字段属性_非空` |  | Not Null |
| `SQLITE_字段属性_唯一` |  | Unique |
| `wxCODEC_TYPE_UNKNOWN` |  |  |
| `wxCODEC_TYPE_AES128` |  |  |
| `wxCODEC_TYPE_AES256` |  |  |
| `wxCODEC_TYPE_CHACHA20` |  |  |
| `wxCODEC_TYPE_SQLCIPHER` |  |  |
| `wxSQLITE_Cipher_Global` | global |  |
| `wxSQLITE_Cipher_AES128` | aes128cbc |  |
| `wxSQLITE_Cipher_AES256` | aes256cbc |  |
| `wxSQLITE_Cipher_CHACHA20` | chacha20 |  |
| `wxSQLITE_Cipher_SQLCIPHER` | sqlcipher |  |
| `wxSQLITE_Config_cipher` | cipher | [全局]“wxCODEC_TYPE_”开头常量，可用于配置加密算法 |
| `wxSQLITE_Config_legacy` | legacy | [AES128，AES256，CHACHA20，SQLCIPHER]，0或1 |
| `wxSQLITE_Config_legacy_page_size` | legacy_page_size | [AES128，AES256，CHACHA20，SQLCIPHER]，最大65536 |
| `wxSQLITE_Config_kdf_iter` | kdf_iter | [AES256，CHACHA20，SQLCIPHER] |
| `wxSQLITE_Config_fast_kdf_iter` | fast_kdf_iter | [SQLCIPHER] |
| `wxSQLITE_Config_hmac_use` | hmac_use | [SQLCIPHER] |
| `wxSQLITE_Config_hmac_pgno` | hmac_pgno | [SQLCIPHER] |
| `wxSQLITE_Config_hmac_salt_mask` | hmac_salt_mask | [SQLCIPHER] |
| `SQLITE3_触发条件_插入` |  |  |
| `SQLITE3_触发条件_删除` |  |  |
| `SQLITE3_触发条件_更新` |  |  |
| `SQLITE3_触发条件_字段更新` |  |  |
| `SQLITE3_触发时间_普通模式` |  |  |
| `SQLITE3_触发时间_之前` |  |  |
| `SQLITE3_触发时间_之后` |  |  |
| `SQLITE3_事务锁状态_推迟` |  | 事务不获取任何锁(直到它需要锁的时候) |
| `SQLITE3_事务锁状态_立即` |  | 事务会尝试获取RESERVED锁。如果成功，保证没有别的连接可以写数据库。但是，别的连接可以对数据库进行读操作 |
| `SQLITE3_事务锁状态_独占` |  | 事务会试着获取对数据库的EXCLUSIVE锁。如果成功，别的连接无法读写数据库。 |
