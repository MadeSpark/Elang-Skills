# Java支持库

- **库标识**: `Javalib`
- **版本**: 2.0
- **GUID**: `F3DA9F65E55F47cb8A8DAC95A189F4B1`
- **作者**: 大连大有吴涛易语言软件开发有限公司
- **主页**: http://www.eyuyan.com
- **文件**: `Javalib`
- **可用命令数**: 66

本支持库使易语言具备了访问Java类库的能力。“Java虚拟机”和“Java本地接口”是本支持库中最重要的两个数据类型。由“Java虚拟机”负责真实Java虚拟机的生命周期管理，而“Java本地接口”则提供Java类库的访问接口。使用时，可直接通过“Java本地接口”的各类方法访问Java类库（即与“Java虚拟机”通信），由支持库本身自动完成“Java虚拟机”的创建和销毁。如果需要特别指定“用户类路径”或“本地库路径”，则必需在使用“Java本地接口”之前显式调用“Java虚拟机.创建()”方法创建Java虚拟机，以传入相关路径参数。
    本支持库要求目标机器中已安装“Java运行时环境”（JRE）。如果在没有配置好Java环境的情况下使用本支持库，所有对象方法都将返回空值（假，0，“”）。
    本支持库安装后，将自动在易语言主菜单中添加一项工具菜单，[工具]-[JavaLib：Java类型签名查询]。这是一个查询“Java 类型签名（Type Signature）”的实用工具。

## 数据类型

| 名称 | 说明 |
|---|---|
| `Java虚拟机` | Java虚拟机，即Java Virtual Machine（JVM）。本类型主要实现对Java虚拟机的生命周期管理。一般情况下，“Java虚拟机”会自动创建和销毁，不需要用户介入。但如需特别指定“用户类路径”或“本地库路径”，则用户必须显式调用“创建()”方法自行创建“Java虚拟机”。 |
| `Java本地接口` | 本类型是对JNI Environment的封装。JNI即Java Native Interface：JAVA本地接口。程序中主要使用本类型完成与Java虚拟机的交互，通过调用本类型的一系列方法，达到访问Java类库的目的。本类型的方法大致分为以下几类：类/对象操作；全局/局部引用；访问对象成员；调用对象方法；字符串操作；数组操作；异常处理；其它。这些方法基本上涵盖了访问Java类库所需的一切。对于Java领域中的“类”“对象”“成员”“方法”“字符串”“数组”“异常”等类或对象，在易语言中分别用一个“整数型”标志符来表示，相应的称为“类标志符”“对象标志符”“成员标志符”“方法标志符”“字符串对象标志符”“数组标志符”“异常对象标志符”等。此类标志符在本支持库中被广泛使用。调用相关的方法可以获得所需标志符。 |
| `数组类型` | 用于“Java本地接口.创建数组()”方法的第二个参数，指定将创建可容纳何种基本数据类型的数组。 |

## 命令

### 创建

> 英文名 `Create` ｜ 返回值 **逻辑型**

创建JAVA虚拟机，并同时设置“用户类路径(classpath)”和“本地库路径(librarypath)”。如果不需要特别指定“用户类路径”或“本地库路径”，不必显式调用本方法，“Java虚拟机”会在必要的时机（进入对象作用域时）自动创建。成功返回“真”，失败返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `用户类路径` | 文本型 | 可空 | 指定java.class.path属性，各路径间以半角分号;分隔，如“c:\myclasses;d:\xx.jar”。 |
| 2 | `本地库路径` | 文本型 | 可空 | 指定java.library.path属性，各路径间以半角分号;分隔，如“c:\myclasses”。此类目录下一般存放实现了JNI接口的dll或so文件。 |

### 取版本

> 英文名 `GetVersionText` ｜ 返回值 **文本型**

取Java本地接口环境的当前版本。返回的文本格式为“A.B”，A、B均为数字，分别表示主、次版本号。

### 加载类

> 英文名 `FindClass` ｜ 返回值 **整数型**

根据类名称加载指定的Java类。它将搜索由classpath环境变量中指定的目录和jar文件，以及调用“Java虚拟机.创建()”方法时指定的“用户类路径”和“本地库路径”。返回加载后的Java类标志符。如果出错则返回0。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `要加载的类名称` | 文本型 | - | 必须使用完整类名称，可用字符.或/分隔，如：“java.lang.String”“java/lang/String”等。 |

### 字节集加载类

> 英文名 `DefineClass` ｜ 返回值 **整数型**

从包含原始类数据(.class文件内容)的字节集中加载类，返回加载后的Java类标志符。如果出错则返回0。JNI不允许重复加载同一个类。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `类名称` | 文本型 | - | 使用带包名称的类时，可用字符.或/分隔，如：“java.lang.String”“java/lang/String”等。 |
| 2 | `类加载器` | 整数型 | 有默认值 | 分派给所定义的类的类加载器（的对象标志符）。注：如果本参数为0，默认使用系统类加载器java.lang.ClassLoader.getSystemClassLoader()。 |
| 3 | `类文件数据` | 字节集型 | - | 包含 .class 文件数据的字节集数据。 |

### 取父类

> 英文名 `GetSuperclass` ｜ 返回值 **整数型**

取指定类的父类（或超类，SuperClass）。返回父类标志符，失败返回0。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `类标志符` | 整数型 | - | 指定欲取其超类的类。该标志符不应为0。 |

### 分配对象

> 英文名 `AllocObject` ｜ 返回值 **整数型**

分配指定Java类的新对象而不调用该类的任何构造函数。返回分配后的对象标志符，失败返回0。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `类标志符` | 整数型 | - | 指定要分配的对象所属类型。 |

### 创建对象

> 英文名 `NewObject` ｜ 返回值 **整数型**

创建Java对象。返回创建后的对象标志符，失败返回0。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `类标志符` | 整数型 | - | 指定欲创建其对象的Java类。该标志符可通过调用“加载类()”等方法获得。 |
| 2 | `构造函数的方法标志符` | 整数型 | - | 指定应调用的构造函数方法。该标志符必须通过调用“取方法标志符()”方法获得，且调用时的方法名必须为<init>，而返回类型必须为void (V)。 |
| 3 | `参数值` | 整数型 | 可空 | 对应于被调用的Java类构造函数的参数值，注意传入的类型要与实际需要的类型一致。本参数可重复添加。 |

### 取对象类

> 英文名 `GetObjectClass` ｜ 返回值 **整数型**

返回“对象所属类”的类标志符，失败返回0。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `对象标志符` | 整数型 | - | 指定欲取其类的对象。该标志符不应为0。 |

### 是否为类实例

> 英文名 `IsInstanceOf` ｜ 返回值 **逻辑型**

判断指定对象是否为指定类的实例。是则返回“真”，否则返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `对象标志符` | 整数型 | - | 指定一个对象。该标志符不应为0。 |
| 2 | `类标志符` | 整数型 | - | 指定一个类。该标志符不应为0。 |

### 是否相同

> 英文名 `IsSameObject` ｜ 返回值 **逻辑型**

判断两个变量是否引用同一Java对象。是则返回“真”，否则返回“假”。注意：如果两个参数标志符有其一为0，必将返回“假”（这一点与JNI不同）。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `对象一标志符` | 整数型 | - | 指定一个对象。该标志符不应为0。 |
| 2 | `对象二标志符` | 整数型 | - | 指定一个对象。该标志符不应为0。 |

### 可否强制转换

> 英文名 `IsAssignableFrom` ｜ 返回值 **逻辑型**

判断参数一（“将被转换的类”）的对象是否可安全地强制转换为参数二（“将被转换到的类”）。以下情况下将返回“真”：第一及第二个类参数引用同一个 Java 类；第一个类是第二个类的子类；第二个类是第一个类的某个接口。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `将被转换的类标志符` | 整数型 | - | 指定将被转换的类 |
| 2 | `将被转换到的类标志符` | 整数型 | - | 指定将被转换到的类 |

### 创建全局引用

> 英文名 `NewGlobalRef` ｜ 返回值 **整数型**

创建指定对象的新全局引用。返回创建后的全局引用对象标志符，失败返回0。全局引用需要通过调用“销毁全局引用()”方法来销毁。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `被引用的对象标志符` | 整数型 | - | 该参数所指定的对象既可以是全局引用，也可以是局部引用。 |

### 销毁全局引用

> 英文名 `DeleteGlobalRef` ｜ 返回值 **逻辑型**

销毁由参数所指定的全局引用对象。成功返回“真”，失败返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `欲销毁的全局引用对象标志符` | 整数型 | - |  |

### 销毁局部引用

> 英文名 `DeleteLocalRef` ｜ 返回值 **逻辑型**

销毁由参数所指定的局部引用对象。成功返回“真”，失败返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `欲销毁的局部引用对象标志符` | 整数型 | - |  |

### 取成员标志符

> 英文名 `GetFieldID` ｜ 返回值 **整数型**

返回类的实例成员（非静态成员）的成员标志符，该成员由其名称及签名指定。失败返回0。本方法可自动将未初始化的类初始化。本方法不能用于获取数组的长度，应调用“取数组长度()”方法。调用“取成员()”“置成员()”系列方法时都将用到本方法返回的成员标志符。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `类标志符` | 整数型 | - | 指定欲取其成员(Field)标志符的类。该标志符不应为0。 |
| 2 | `成员名称` | 文本型 | - | 欲取其标志符的成员名称。 |
| 3 | `成员签名` | 文本型 | - | 欲取其标志符的成员签名。使用Java中的“类型签名(Type Signature)”协助前一参数“成员名称”唯一标识欲访问的成员。关于“类型签名”的更多信息，请点击易语言[工具]菜单“JavaLib: 查询Java类的类型签名”；或参考相关文档。 |

### 取成员

> 英文名 `GetField` ｜ 返回值 **逻辑型**

取对象成员的值，结果存到第三个参数“结果”中。成功返回“真”，失败返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `对象标志符` | 整数型 | - | 指定欲取其成员(Field)的类。该标志符不应为0。 |
| 2 | `成员标志符` | 整数型 | - | 欲取其值的成员。该标志符不应为0。 |
| 3 | `结果` | 通用型 | 参考 | 存放取出的成员值。注意：要提供与成员的数据类型相一致的变量。 |

### 置成员

> 英文名 `SetField` ｜ 返回值 **逻辑型**

设置对象成员的值。成功返回“真”，失败返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `对象标志符` | 整数型 | - | 指定欲取其成员(Field)的类。该标志符不应为0。 |
| 2 | `成员标志符` | 整数型 | - | 欲取其值的成员。该标志符不应为0。 |
| 3 | `值` | 通用型 | - | 欲赋于成员的值。 |

### 取对象成员

> 英文名 `GetObjectField` ｜ 返回值 **逻辑型**

取对象成员的值，取出的是一个对象标志符，结果存到第三个参数“结果”中。成功返回“真”，失败返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `对象标志符` | 整数型 | - | 指定欲取其成员(Field)的类。该标志符不应为0。 |
| 2 | `成员标志符` | 整数型 | - | 欲取其值的成员。该标志符不应为0。 |
| 3 | `结果` | 整数型 | 参考 | 存放取出的成员值——对象标志符。 |

### 置对象成员

> 英文名 `SetObjectField` ｜ 返回值 **逻辑型**

设置对象成员的值。成功返回“真”，失败返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `对象标志符` | 整数型 | - | 指定欲取其成员(Field)的类。该标志符不应为0。 |
| 2 | `成员标志符` | 整数型 | - | 欲取其值的成员。该标志符不应为0。 |
| 3 | `对象标志符` | 整数型 | - | 指定欲赋于该成员的对象。 |

### 取静态成员标志符

> 英文名 `GetStaticFieldID` ｜ 返回值 **整数型**

返回类的静态成员(static Field)的成员标志符，该成员由其名称及签名指定。失败返回0。本方法可自动将未初始化的类初始化。调用“取静态成员()”“置静态成员()”系列方法时都将用到本方法返回的静态成员标志符。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `类标志符` | 整数型 | - | 指定欲取其成员(Field)标志符的类。该标志符不应为0。 |
| 2 | `成员名称` | 文本型 | - | 欲取其标志符的成员名称。 |
| 3 | `成员签名` | 文本型 | - | 欲取其标志符的成员签名。使用Java中的“类型签名(Type Signature)”协助前一参数“成员名称”唯一标识欲访问的成员。关于“类型签名”的更多信息，请点击易语言[工具]菜单“JavaLib: 查询Java类的类型签名”；或参考相关文档。 |

### 取静态成员

> 英文名 `GetStaticField` ｜ 返回值 **逻辑型**

取对象静态成员的值，取出的是一个对象标志符，结果存到第三个参数“结果”中。成功返回“真”，失败返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `对象标志符` | 整数型 | - | 指定欲取其成员(Field)的类。该标志符不应为0。 |
| 2 | `成员标志符` | 整数型 | - | 欲取其值的成员。该标志符不应为0。 |
| 3 | `结果` | 通用型 | 参考 | 存放取出的成员值。注意：要提供与成员的数据类型相一致的变量。 |

### 置静态成员

> 英文名 `SetStaticField` ｜ 返回值 **逻辑型**

设置对象静态成员的值。成功返回“真”，失败返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `对象标志符` | 整数型 | - | 指定欲取其成员(Field)的类。该标志符不应为0。 |
| 2 | `成员标志符` | 整数型 | - | 欲取其值的成员。该标志符不应为0。 |
| 3 | `值` | 通用型 | - | 欲赋于成员的值。 |

### 取静态对象成员

> 英文名 `GetStaticObjectField` ｜ 返回值 **逻辑型**

取对象静态成员的值，结果存到第三个参数“结果”中。成功返回“真”，失败返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `对象标志符` | 整数型 | - | 指定欲取其成员(Field)的类。该标志符不应为0。 |
| 2 | `成员标志符` | 整数型 | - | 欲取其值的成员。该标志符不应为0。 |
| 3 | `结果` | 整数型 | 参考 | 存放取出的成员值——对象标志符。 |

### 置静态对象成员

> 英文名 `SetStaticObjectField` ｜ 返回值 **逻辑型**

设置对象静态成员的值。成功返回“真”，失败返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `对象标志符` | 整数型 | - | 指定欲取其成员(Field)的类。该标志符不应为0。 |
| 2 | `成员标志符` | 整数型 | - | 欲取其值的成员。该标志符不应为0。 |
| 3 | `对象标志符` | 整数型 | - | 指定欲赋于该成员的对象。 |

### 取方法标志符

> 英文名 `GetMethodID` ｜ 返回值 **整数型**

返回类或接口实例方法（非静态方法）的方法标志符，该方法由其名称及签名指定。失败返回0。方法可在参数“类标志符”所指定的类的超类中定义。本方法可自动将未初始化的类初始化。要获得构造函数的方法标志符，应将<init>作为方法名，同时将void (V)作为返回类型。调用“[非虚拟][对象/空]方法()”系列方法时都将用到本方法返回的方法标志符。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `类标志符` | 整数型 | - | 指定欲取其方法的类。可为“加载类()”等方法的返回值。 |
| 2 | `方法名称` | 文本型 | - | 欲调用的方法名称 |
| 3 | `方法签名` | 文本型 | - | 欲调用的方法签名。使用Java中的“类型签名(Type Signature)”协助前一参数“方法名称”唯一标识欲调用的类方法——Java类方法可能被重载，仅“方法名称”不能唯一标识之。关于“类型签名”的更多信息，请点击易语言[工具]菜单“JavaLib: 查询Java类的类型签名”；或参考相关文档。 |

### 方法

> 英文名 `CallMethod` ｜ 返回值 **逻辑型**

调用对象的具有基本数据类型返回值方法。调用成功返回“真”，失败返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `对象标志符` | 整数型 | - | 指定欲调用其方法的对象。 |
| 2 | `方法标志符` | 整数型 | - | 指定欲调用的方法。应为“取方法标志符()”方法的返回值。 |
| 3 | `返回结果` | 通用型 | 参考 | 存放方法调用后的返回值。注意，提供的变量的数据类型要与方法的实际返回值数据类型一致。 |
| 4 | `参数值` | 通用型 | 可空 | 对应于被调用方法的参数值，注意传入的类型要与该方法实际需要的类型一致。本参数可重复添加。 |

### 对象方法

> 英文名 `CallObjectMethod` ｜ 返回值 **逻辑型**

调用对象的具有对象型返回值的方法。调用成功返回“真”，失败返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `对象标志符` | 整数型 | - | 指定欲调用其方法的对象。 |
| 2 | `方法标志符` | 整数型 | - | 指定欲调用的方法。应为“取方法标志符()”方法的返回值。 |
| 3 | `返回结果` | 整数型 | 参考 | 存放方法调用后的返回值——对象标志符。 |
| 4 | `参数值` | 通用型 | 可空 | 对应于被调用方法的参数值，注意传入的类型要与该方法实际需要的类型一致。本参数可重复添加。 |

### 空方法

> 英文名 `CallVoidMethod` ｜ 返回值 **逻辑型**

调用对象的没有返回值的方法。调用成功返回“真”，失败返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `对象标志符` | 整数型 | - | 指定欲调用其方法的对象。 |
| 2 | `方法标志符` | 整数型 | - | 指定欲调用的方法。应为“取方法标志符()”方法的返回值。 |
| 3 | `参数值` | 通用型 | 可空 | 对应于被调用方法的参数值，注意传入的类型要与该方法实际需要的类型一致。本参数可重复添加。 |

### 非虚拟方法

> 英文名 `CallNonvirtualMethod` ｜ 返回值 **逻辑型**

根据指定的类和方法标志符调用某对象的具有基本数据类型返回值的实例（非静态）方法。调用成功返回“真”，失败返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `对象标志符` | 整数型 | - | 指定欲调用其方法的对象。 |
| 2 | `类标志符` | 整数型 | - | 指定欲调用其方法的类。可为“加载类()”等方法的返回值。 |
| 3 | `方法标志符` | 整数型 | - | 指定欲调用的方法。应为“取方法标志符()”方法的返回值。 |
| 4 | `返回结果` | 通用型 | 参考 | 存放方法调用后的返回值。注意，提供的变量的数据类型要与方法的实际返回值数据类型一致。 |
| 5 | `参数值` | 通用型 | 可空 | 对应于被调用方法的参数值，注意传入的类型要与该方法实际需要的类型一致。本参数可重复添加。 |

### 非虚拟对象方法

> 英文名 `CallNonvirtualObjectMethod` ｜ 返回值 **逻辑型**

根据指定的类和方法标志符调用某对象的具有对象类型返回值的实例（非静态）方法。调用成功返回“真”，失败返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `对象标志符` | 整数型 | - | 指定欲调用其方法的对象。 |
| 2 | `类标志符` | 整数型 | - | 指定欲调用其方法的类。可为“加载类()”等方法的返回值。 |
| 3 | `方法标志符` | 整数型 | - | 指定欲调用的方法。应为“取方法标志符()”方法的返回值。 |
| 4 | `返回结果` | 整数型 | 参考 | 存放方法调用后的返回值——对象标志符。 |
| 5 | `参数值` | 通用型 | 可空 | 对应于被调用方法的参数值，注意传入的类型要与该方法实际需要的类型一致。本参数可重复添加。 |

### 非虚拟空方法

> 英文名 `CallNonvirtualVoidMethod` ｜ 返回值 **逻辑型**

根据指定的类和方法标志符调用某对象的没有返回值的实例（非静态）方法。调用成功返回“真”，失败返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `对象标志符` | 整数型 | - | 指定欲调用其方法的对象。 |
| 2 | `类标志符` | 整数型 | - | 指定欲调用其方法的类。可为“加载类()”等方法的返回值。 |
| 3 | `方法标志符` | 整数型 | - | 指定欲调用的方法。应为“取方法标志符()”方法的返回值。 |
| 4 | `参数值` | 通用型 | 可空 | 对应于被调用方法的参数值，注意传入的类型要与该方法实际需要的类型一致。本参数可重复添加。 |

### 取静态方法标志符

> 英文名 `GetStaticMethodID` ｜ 返回值 **整数型**

取指定类的静态方法(static Method)的方法标志符，该方法由其名称和签名指定。失败返回0。本方法可自动将未初始化的类初始化。调用“静态[对象/空]方法()”系列方法时都将用到本方法返回的静态方法标志符。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `类标志符` | 整数型 | - | “欲取其静态方法的”类的标志符，可为“查询类()”的返回值。 |
| 2 | `静态方法名称` | 文本型 | - | 欲调用的静态方法名称 |
| 3 | `静态方法签名` | 文本型 | - | 欲调用的方法签名。使用Java中的“类型签名(Type Signature)”协助前一参数“静态方法名称”唯一标识欲调用的类方法——Java类方法可能被重载，仅“方法名称”不能唯一标识之。关于“类型签名”的更多信息，请点击易语言[工具]菜单“JavaLib: 查询Java类的类型签名”；或参考相关文档。 |

### 静态方法

> 英文名 `CallStaticMethod` ｜ 返回值 **逻辑型**

调用类的具有基本数据类型返回值静态方法。调用成功返回“真”，失败返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `类标志符` | 整数型 | - | 指定欲调用其静态方法的类。可为“加载类()”等方法的返回值。 |
| 2 | `方法标志符` | 整数型 | - | 指定欲调用的静态方法。应为“取静态方法标志符()”方法的返回值。 |
| 3 | `返回结果` | 通用型 | 参考 | 存放静态方法调用后的返回值。注意，提供的变量的数据类型要与方法的实际返回值数据类型一致。 |
| 4 | `参数值` | 通用型 | 可空 | 对应于被调用方法的参数值，注意传入的类型要与该方法实际需要的类型一致。本参数可重复添加。 |

### 静态对象方法

> 英文名 `CallStaticObjectMethod` ｜ 返回值 **逻辑型**

调用类的具有对象型返回值的静态方法。调用成功返回“真”，失败返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `类标志符` | 整数型 | - | 指定欲调用其静态方法的类。可为“加载类()”等方法的返回值。 |
| 2 | `方法标志符` | 整数型 | - | 指定欲调用的静态方法。应为“取静态方法标志符()”方法的返回值。 |
| 3 | `返回结果` | 整数型 | 参考 | 存放静态方法调用后的返回值——对象标志符。 |
| 4 | `参数值` | 通用型 | 可空 | 对应于被调用静态方法的参数值，注意传入的类型要与该方法实际需要的类型一致。本参数可重复添加。 |

### 静态空方法

> 英文名 `CallStaticVoidMethod` ｜ 返回值 **逻辑型**

调用类的静态无返回值方法，即static void方法。调用成功返回“真”，失败返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `类标志符` | 整数型 | - | “欲调用其静态方法的类”的类标志符，可为“加载类()”的返回值。 |
| 2 | `方法标志符` | 整数型 | - | “欲调用的静态方法”的方法标志符，应为“取静态方法标志符()”的返回值。 |
| 3 | `参数值` | 通用型 | 可空 | 对应于被调用方法的参数值，注意传入的类型要与该方法实际需要的类型一致。本参数可重复添加。 |

### 创建字符串

> 英文名 `NewString` ｜ 返回值 **整数型**

使用易语言文本构造新的java.lang.String对象。返回创建后的字符串对象标志符，失败返回0。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `文本` | 文本型 | 有默认值 | 将被转换到Java字符串的易语言文本。 |

### 取字符串长度

> 英文名 `GetStringLength` ｜ 返回值 **整数型**

返回Java字符串的长度（Unicode字符数），数字、字母、汉字等都被当作一个字符。失败返回0。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字符串标志符` | 整数型 | - | 指定欲取其长度的Java字符串对象 |

### 取字符串文本

> 英文名 `GetStringChars` ｜ 返回值 **文本型**

返回与指定Java字符串相对应的易语言文本。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字符串标志符` | 整数型 | - | 指定欲取其文本的Java字符串对象 |
| 2 | `执行结果` | 逻辑型 | 可空、参考 | 如果提供本参数，则在方法执行后存放执行结果：执行成功，其值为“真”；执行失败，其值为“假”。 |

### 取数组长度

> 英文名 `GetArrayLength` ｜ 返回值 **整数型**

返回指定Java数组的长度。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `数组标志符` | 整数型 | - | 指定欲取其长度的Java数组 |

### 创建数组

> 英文名 `NewArray` ｜ 返回值 **整数型**

返回创建后的数组标志符。失败返回0。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `数组长度` | 整数型 | - |  |
| 2 | `数组类型` | 整数型 | - | 指定该数组容纳的基本数据类型。请为本参数提供枚举类型“数组类型”中的常量值，如：“#数组类型.数据型”。 |

### 取数组成员

> 英文名 `GetArrayElement` ｜ 返回值 **逻辑型**

取出指定数组中指定成员并存放到本方法的第三个参数中。成功返回“真”，失败返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `数组标志符` | 整数型 | - | 指定要取其数据成员的Java数组。为“创建数组()”方法的返回值，注意不要混用不同数据类型的数组。 |
| 2 | `成员索引` | 整数型 | - | 指定欲取其值的数组成员的索引。索引值从0开始。 |
| 3 | `取出的值` | 通用型 | 参考 | 存放从数组中取出的值。注意：要保证本参数的数据类型与数组所能容纳的数据类型一致。 |

### 置数组成员

> 英文名 `SetArrayElement` ｜ 返回值 **逻辑型**

成功返回“真”，失败返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `数组标志符` | 整数型 | - | 指定要对其数据成员赋值的Java数组。可为“创建数组()”方法的返回值，注意不要混用不同基本数据类型的数组。 |
| 2 | `成员索引` | 整数型 | - | 指定欲对其赋值的数组成员的索引。索引值从0开始。 |
| 3 | `值` | 通用型 | - | 欲赋给指定数组成员的值。注意：要保证本参数的数据类型与数组所能容纳的数据类型一致。 |

### 创建对象数组

> 英文名 `NewObjectArray` ｜ 返回值 **整数型**

创建一个可以容纳Java对象(object)的数组。返回创建后的数组标志符，失败返回0。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `数组大小` | 整数型 | - |  |
| 2 | `类标志符` | 整数型 | - | 指定该数组用于存放何种“类(class)”。 |
| 3 | `初始对象标志符` | 整数型 | - | 指定该数组所有成员的初始值。 |

### 取对象数组成员

> 英文名 `GetObjectArrayElement` ｜ 返回值 **整数型**

取指定“对象数组”中的某一个成员，由参数“成员索引”指定取该数组的哪一个成员。返回该成员的对象标志符，失败返回0。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `数组标志符` | 整数型 | - | 指定欲取其成员的Java对象数组 |
| 2 | `成员索引` | 整数型 | - | 指定欲取数组的第几个成员。索引值从0开始。 |

### 置对象数组成员

> 英文名 `SetObjectArrayElement` ｜ 返回值 **逻辑型**

为Java“对象数组”的指定成员赋值。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `数组标志符` | 整数型 | - | 指定欲赋值的Java对象数组 |
| 2 | `成员索引` | 整数型 | - | 指定欲对其赋值的成员索引。索引值从0开始。 |
| 3 | `欲赋值的对象标志符` | 整数型 | - | 指定欲赋值的对象 |

### 抛出异常

> 英文名 `Throw` ｜ 返回值 **逻辑型**

向Java虚拟机抛出一个已存在的异常对象。成功返回“真”，失败返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `异常对象标志符` | 整数型 | - | 指定欲抛出的Java异常对象。 |

### 抛出新异常

> 英文名 `ThrowNew` ｜ 返回值 **逻辑型**

创建一个异常对象，并向Java虚拟机抛出该异常对象。成功返回“真”，失败返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `异常类标志符` | 整数型 | 有默认值 | 指定欲创建的异常类，应为java.lang.Throwable或其子类。如果本参数为0，默认使用java.lang.Throwable。 |
| 2 | `异常描述文本` | 文本型 | 有默认值 |  |

### 是否有异常

> 英文名 `IsExceptionOccurred` ｜ 返回值 **逻辑型**

判断当前是否有正被抛出的异常对象。在调用“清除异常()”方法或Java代码处理该异常前，异常将始终保持抛出状态。如果有异常发生，返回“真”；否则返回“假”。

### 取异常对象

> 英文名 `ExceptionOccurred` ｜ 返回值 **整数型**

返回当前正被抛出的异常对象。在调用“清除异常()”方法或Java代码处理该异常前，异常将始终保持抛出状态。如果有异常发生，返回该异常对象标志符；如果没有异常发生，返回0。

### 输出异常

> 英文名 `ExceptionDescribe` ｜ 返回值 **逻辑型**

将异常及堆栈的回溯信息文本输出到系统标准错误输出设备（例如stderr）。执行后将清除当前异常对象。成功返回“真”，失败返回“假”。本方法主要用于调试。

### 取异常文本

> 英文名 `GetExceptionMsg` ｜ 返回值 **文本型**

返回当前异常对象的描述文本。执行后将清除当前异常对象。

### 清除异常

> 英文名 `ExceptionClear` ｜ 返回值 **逻辑型**

清除当前抛出的所有异常。如果当前无异常，则不做任何操作。成功返回“真”，失败返回“假”。

### 致命错误

> 英文名 `FatalError` ｜ 返回值 **逻辑型**

抛出致命错误并且不希望Java虚拟机进行修复。成功返回“真”，失败返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `错误描述文本` | 文本型 | 有默认值 |  |

### 进入监视

> 英文名 `MonitorEnter` ｜ 返回值 **逻辑型**

进入与指定对象所引用的基本Java对象相关联的监视程序。成功返回“真”，失败返回“假”。
    每个Java对象都有一个相关联的监视程序。如果当前线程已经拥有与指定对象相关联的监视程序，它将使指示该线程进入监视程序次数的监视程序计数器增1。如果与指定对象相关联的监视程序并非由某个线程所拥有，则当前线程将变为该监视程序的所有者，同时将该监视程序的计数器设置为1。如果另一个线程已拥有与指定对象关联的监视程序，则在监视程序被释放前当前线程将处于等待状态。监视程序被释放后，当前线程将尝试重新获得所有权。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `对象标志符` | 整数型 | - | 可为常规Java对象或类对象。 |

### 退出监视

> 英文名 `MonitorExit` ｜ 返回值 **逻辑型**

当前线程必须是与指定对象所引用的基本Java对象相关联的监视程序的所有者。线程将使指示进入监视程序次数的计数器减1。如果计数器的值变为0，当前线程释放监视程序。成功返回“真”，失败返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `对象标志符` | 整数型 | - | 可为常规Java对象或类对象。 |


## 常量

| 名称 | 值 | 说明 |
|---|---|---|
| `本库开发代号` | JavaLib |  |
