# 通用对象支持库

- **库标识**: `commobj`
- **版本**: 2.0
- **GUID**: `{A068799B-7551-46b9-8CA8-EEF8357AFEA4}`
- **作者**: 大有吴涛易语言软件开发有限公司
- **主页**: http://www.eyuyan.com
- **文件**: `commobj`
- **可用命令数**: 140

目前提供“快速文本对象”“快速字节集对象”等通用对象数据类型，分别实现对“文本”“字节集”等易语言基本数据类型的包装。除了易用性，本库还特别注重大批量数据添加删除时的操作效率。

## 数据类型

| 名称 | 说明 |
|---|---|
| `快速文本对象` | 提供对文本数据的相关操作。在大批量数据操作时，相对于“文本型”有较高的执行效率。 |
| `快速字节集对象` | 提供对字节集数据的相关操作。在大批量数据操作时，相对于“字节集”有较高的执行效率。 |

## 命令

### 取长度

> 英文名 `GetLength` ｜ 返回值 **整数型**

返回本对象文本中的字符个数（每个汉字视为两个字符）。

### 取文本

> 英文名 `GetText` ｜ 返回值 **文本型**

取本对象中的全部或部分文本。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `起始位置` | 整数型 | 可空 | 位置索引可以为正值，1表示第一个字符，2表示第二个字符，依次类推；位置索引也可以为负值，-1表示最后一个字符，-2表示倒数第二个字符，依次类推。如果本参数被省略，默认为1。 |
| 2 | `字符数` | 整数型 | 可空 | 如果本参数被省略，默认为从“起始位置”到文本结尾的字符个数。 |

### 置文本

> 英文名 `SetText` ｜ 返回值 **逻辑型**

将参数数据转换为文本并设置为本对象文本。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `数据` | 通用型 | 任意类型 | 参数类型支持各种基本数据类型及其数组数据。 |

### 清除文本

> 英文名 `EmptyText` ｜ 返回值 **逻辑型**

### 添加

> 英文名 `Append` ｜ 返回值 **逻辑型**

将参数转换成文本并添加到本对象文本的末尾。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `欲添加的数据` | 通用型 | 任意类型 | 参数类型支持各种基本数据类型及其数组数据。 |

### 插入

> 英文名 `Insert` ｜ 返回值 **逻辑型**

将参数数据转换为文本后插入本对象文本中的指定位置之前。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `插入位置` | 整数型 | 可空 | 位置索引可以为正值，1表示第一个字符，2表示第二个字符，依次类推；位置索引也可以为负值，-1表示最后一个字符，-2表示倒数第二个字符，依次类推。如果本参数被省略，默认为1。 |
| 2 | `欲插入的数据` | 通用型 | 任意类型 | 参数类型支持各种基本数据类型及其数组数据。 |

### 删除字符

> 英文名 `DeleteChars` ｜ 返回值 **逻辑型**

删除文本中从指定字符开始的指定个数的字符（包括起始删除位置处的字符）

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `起始删除位置` | 整数型 | - | 位置索引可以为正值，1表示第一个字符，2表示第二个字符，依次类推；位置索引也可以为负值，-1表示最后一个字符，-2表示倒数第二个字符，依次类推。 |
| 2 | `欲删除的字符数` | 整数型 | - | 如果参数值为-1，表示删除从“起始删除位置”开始到文本结尾的所有字符。 |

### 寻找文本

> 英文名 `Find` ｜ 返回值 **整数型**

寻找指定文本在本对象文本中从“起始寻找位置”开始第一次出现时的位置索引（>0），未找到则返回-1。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `欲寻找的文本` | 文本型 | - |  |
| 2 | `起始寻找位置` | 整数型 | 可空 | 位置索引可以为正值，1表示第一个字符，2表示第二个字符，依次类推；位置索引也可以为负值，-1表示最后一个字符，-2表示倒数第二个字符，依次类推。如果本参数被省略，默认为1。 |
| 3 | `是否区分大小写` | 逻辑型 | 有默认值 |  |

### 倒找文本

> 英文名 `FindReverse` ｜ 返回值 **整数型**

在本对象文本中从“起始寻找位置”开始向文本开头方向寻找指定文本第一次出现时的位置索引（相对于文本头），未找到则返回-1。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `欲寻找的文本` | 文本型 | - |  |
| 2 | `起始寻找位置` | 整数型 | 可空 | 位置索引可以为正值，1表示第一个字符，2表示第二个字符，依次类推；位置索引也可以为负值，-1表示最后一个字符，-2表示倒数第二个字符，依次类推。如果本参数被省略，默认为1。 |
| 3 | `是否区分大小写` | 逻辑型 | 有默认值 |  |

### 替换文本

> 英文名 `ReplaceText` ｜ 返回值 **逻辑型**

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `起始替换位置` | 整数型 | - | 位置索引可以为正值，1表示第一个字符，2表示第二个字符，依次类推；位置索引也可以为负值，-1表示最后一个字符，-2表示倒数第二个字符，依次类推。 |
| 2 | `替换长度` | 整数型 | - |  |
| 3 | `替换为文本` | 文本型 | 可空 | 如果本参数被省略，则删除文本中的指定部分。 |

### 替换子文本

> 英文名 `ReplaceSubText` ｜ 返回值 **整数型**

在本对象文本中查找指定文本，并将其替换为其它文本。返回替换成功次数。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `被替换文本` | 文本型 | - |  |
| 2 | `替换为文本` | 文本型 | 可空 | 如果本参数被省略，则表示将删除被替换文本。 |
| 3 | `起始替换位置` | 整数型 | 可空 | 位置索引可以为正值，1表示第一个字符，2表示第二个字符，依次类推；位置索引也可以为负值，-1表示最后一个字符，-2表示倒数第二个字符，依次类推。如果本参数被省略，默认为1。 |
| 4 | `最大替换次数` | 整数型 | 可空 | 如果为-1，表示不限制替换次数。如果本参数被省略，默认为-1。 |
| 5 | `是否区分大小写` | 逻辑型 | 有默认值 |  |

### 分割文本

> 英文名 `SplitText` ｜ 返回值 **文本型**

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `用作分割的文本` | 文本型 | 可空 | 如果被省略，则默认使用半角逗号“,”字符作为分隔符。如果是一个长度为零的文本，则返回的数组仅包含一个成员，其内容是本对象中的文本。 |
| 2 | `要返回的子文本数目` | 整数型 | 可空 | 如果为-1，表示不限制替换次数。如果本参数被省略，默认为-1。 |

### 到大写

> 英文名 `UpperCase` ｜ 返回值 **逻辑型**

将本对象文本中的全部或部分英文字母变为大写字母。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `起始字符位置` | 整数型 | 可空 | 位置索引可以为正值，1表示第一个字符，2表示第二个字符，依次类推；位置索引也可以为负值，-1表示最后一个字符，-2表示倒数第二个字符，依次类推。如果本参数被省略，默认为1。 |
| 2 | `字符个数` | 整数型 | 可空 | 如果本参数被省略，默认为从参数“起始字符位置”开始到文本结尾的字符个数。 |

### 到小写

> 英文名 `LowerCase` ｜ 返回值 **逻辑型**

将本对象文本中的全部或部分英文字母变为小写字母。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `起始字符位置` | 整数型 | 可空 | 位置索引可以为正值，1表示第一个字符，2表示第二个字符，依次类推；位置索引也可以为负值，-1表示最后一个字符，-2表示倒数第二个字符，依次类推。如果本参数被省略，默认为1。 |
| 2 | `字符个数` | 整数型 | 可空 | 如果本参数被省略，默认为从参数“起始字符位置”开始到文本结尾的字符个数。 |

### 到全角

> 英文名 `QJCase` ｜ 返回值 **逻辑型**

将本对象文本中的所有半角字符变为全角。

### 到半角

> 英文名 `QJCase` ｜ 返回值 **逻辑型**

将本对象文本中的所有全角字符变为半角。

### 删首空

> 英文名 `TrimLeft` ｜ 返回值 **逻辑型**

删除本对象文本中首部所有全角或半角空格。

### 删尾空

> 英文名 `TrimRight` ｜ 返回值 **逻辑型**

删除本对象文本中尾部所有全角或半角空格。

### 删首尾空

> 英文名 `Trim` ｜ 返回值 **逻辑型**

删除本对象文本中首部和尾部所有全角或半角空格。

### 删全部空

> 英文名 `TrimAll` ｜ 返回值 **逻辑型**

删除本对象文本中所有全角或半角空格（包括首部、尾部及中间）。

### 从文件读

> 英文名 `ReadFromFile` ｜ 返回值 **逻辑型**

从指定的文本文件中读取其所有文本到本对象中。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `文件名` | 文本型 | - | 请提供包含完整路径的文本名称。 |

### 写到文件

> 英文名 `WriteToFile` ｜ 返回值 **逻辑型**

将本对象中的所有数据写入指定的文件中。如果该文件不存在，则自动创建；如果该文件已经存在，其原有内容将被直接覆盖！

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `文件名` | 文本型 | - | 请提供包含完整路径的文本名称。 |

### 取缓冲区

> 英文名 `GetBuffer` ｜ 返回值 **整数型**

返回指定大小的缓冲区内存首地址。如果不能获取足够大小的缓冲区，将返回0。可以向该缓冲区直接写入文本数据，但事后必须调用“释放缓冲区()”以调整本对象内部状态。向缓冲区写入数据时请注意不要写到缓冲区之外。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `缓冲区大小` | 整数型 | 可空 | 应大于零并小于“最大可用内存”。如果本参数被省略，默认为当前文本长度。 |

### 释放缓冲区

> 英文名 `ReleaseBuffer` ｜ 返回值 **逻辑型**

本方法只是调整内部状态，并不修改内部文本。另请参考“取缓冲区()”中的说明。

### 置可用内存

> 英文名 `SetMemLength` ｜ 返回值 **逻辑型**

设置本对象内部使用的最小可用内存和最大可用内存。使用本方法可以优化内存使用，但因为内部已经有适当的初始值，普通用户不必调用本方法。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `最小可用内存` | 整数型 | 可空 | 单位为字节。如果本参数被省略，则保持现有“最小可用内存”数值不变。其内部初始值为10240字节，约10K。 |
| 2 | `最大可用内存` | 整数型 | 可空 | 单位为字节。如果本参数被省略，则保持现有“最大可用内存”数值不变。其内部初始值为2147483647字节，约2G。 |

### 置内存增量

> 英文名 `SetMemGrowSize` ｜ 返回值 **逻辑型**

设置本对象内部使用的内存增量值。所谓“内存增量”，即每次需要申请内存时在所需内存的基础上增加的内存数量（多申请出来的内存可供后续操作使用，从而减少了申请内存的次数，提高了执行效率）。正确使用本方法可以优化内存使用并提高执行效率，但因为内部已经有适当的初始值，普通用户不必调用本方法。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `内存增量` | 整数型 | - | 单位为字节。如果此值过小，可能导致数据操作效率降低；如果此值过大，可能导致消耗较多内存。内部初始值为10240字节，约10K。 |

### 取长度

> 英文名 `GetLength` ｜ 返回值 **整数型**

单位为字节(byte)

### 取字节集

> 英文名 `GetData` ｜ 返回值 **字节集型**

取本对象中的全部或部分字节集。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `起始位置` | 整数型 | 可空 | 位置索引可以为正值，1表示第一个字节，2表示第二个字节，依次类推；位置索引也可以为负值，-1表示最后一个字节，-2表示倒数第二个字节，依次类推。如果本参数被省略，默认为1。 |
| 2 | `字节数` | 整数型 | 可空 | 如果本参数被省略，默认为从“起始位置”到字节集结尾的字节数。 |

### 置字节集

> 英文名 `SetData` ｜ 返回值 **逻辑型**

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `数据` | 通用型 | 任意类型 | 参数类型支持各种基本数据类型及其数组数据。 |

### 清除字节集

> 英文名 `Empty` ｜ 返回值 **逻辑型**

### 添加

> 英文名 `Append` ｜ 返回值 **逻辑型**

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `欲添加的数据` | 通用型 | 任意类型 | 参数类型支持各种基本数据类型及其数组数据。 |

### 插入

> 英文名 `Insert` ｜ 返回值 **逻辑型**

在指定的插入位置之前插入指定的字节集数据

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `插入位置` | 整数型 | - | 位置索引可以为正值，1表示第一个字节，2表示第二个字节，依次类推；位置索引也可以为负值，-1表示最后一个字节，-2表示倒数第二个字节，依次类推。 |
| 2 | `欲插入的数据` | 通用型 | 任意类型 | 参数类型支持各种基本数据类型及其数组数据。 |

### 删除字节

> 英文名 `DeleteBytes` ｜ 返回值 **逻辑型**

删除字节集中从指定字节开始的指定个数的字节（包括起始删除位置处的字节）

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `起始删除位置` | 整数型 | - | 位置索引可以为正值，1表示第一个字节，2表示第二个字节，依次类推；位置索引也可以为负值，-1表示最后一个字节，-2表示倒数第二个字节，依次类推。 |
| 2 | `欲删除的字节数` | 整数型 | - |  |

### 取字节

> 英文名 `GetByte` ｜ 返回值 **字节型**

取指定位置处的字节值。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `位置索引` | 整数型 | - | 位置索引可以为正值，1表示第一个字节，2表示第二个字节，依次类推；位置索引也可以为负值，-1表示最后一个字节，-2表示倒数第二个字节，依次类推。 |

### 寻找字节集

> 英文名 `Find` ｜ 返回值 **整数型**

寻找指定数据在本对象数据中从“起始寻找位置”开始第一次出现时的位置索引，未找到则返回-1。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `欲寻找的数据` | 通用型 | - | 参数类型支持各种基本数据类型。 |
| 2 | `起始寻找位置` | 整数型 | 可空 | 位置索引可以为正值，1表示第一个字节，2表示第二个字节，依次类推；位置索引也可以为负值，-1表示最后一个字节，-2表示倒数第二个字节，依次类推。如果本参数被省略，默认为1。 |

### 倒找字节集

> 英文名 `FindReverse` ｜ 返回值 **整数型**

在本对象数据中从“起始寻找位置”开始向数据开头方向寻找指定数据第一次出现时的位置索引（相对数据开头），未找到则返回-1。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `欲寻找的数据` | 通用型 | - | 参数类型支持各种基本数据类型。 |
| 2 | `起始寻找位置` | 整数型 | 可空 | 位置索引可以为正值，1表示第一个字节，2表示第二个字节，依次类推；位置索引也可以为负值，-1表示最后一个字节，-2表示倒数第二个字节，依次类推。默认为-1，即从数据结尾开始向前寻找。 |

### 替换字节集

> 英文名 `ReplaceBin` ｜ 返回值 **逻辑型**

将本对象数据中从指定位置开始指定长度的数据替换为新的数据。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `起始替换位置` | 整数型 | - | 位置索引可以为正值，1表示第一个字节，2表示第二个字节，依次类推；位置索引也可以为负值，-1表示最后一个字节，-2表示倒数第二个字节，依次类推。 |
| 2 | `替换长度` | 整数型 | - |  |
| 3 | `替换为字节集` | 通用型 | 可空 | 参数类型支持各种基本数据类型。如果本参数被省略，则删除字节集中的指定部分。 |

### 替换子字节集

> 英文名 `ReplaceSubBin` ｜ 返回值 **整数型**

在本对象数据中查找“被替换数据”，并将其替换为“替换为数据”。返回实际替换次数。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `被替换数据` | 通用型 | - | 参数类型支持各种基本数据类型。 |
| 2 | `替换为数据` | 通用型 | 可空 | 参数类型支持各种基本数据类型。如果本参数被省略，则表示将删除被替换数据。 |
| 3 | `起始替换位置` | 整数型 | 可空 | 位置索引可以为正值，1表示第一个字节，2表示第二个字节，依次类推；位置索引也可以为负值，-1表示最后一个字节，-2表示倒数第二个字节，依次类推。如果本参数被省略，默认为1。 |
| 4 | `最大替换次数` | 整数型 | 可空 | 如果本参数被省略，或值为-1，表示不限制替换次数。 |

### 分割字节集

> 英文名 `SplitBinary` ｜ 返回值 **字节集型**

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `用作分隔的字节集` | 字节集型 | 可空 | 如果被省略，则默认使用字节 0 作为分隔符。如果是一个长度为零的文本，则返回的数组仅包含一个成员，其内容是本对象中的文本。 |
| 2 | `要返回的子文本数目` | 整数型 | 可空 | 如果为-1，表示不限制替换次数。如果本参数被省略，默认为-1。 |

### 从文件读

> 英文名 `ReadFromFile` ｜ 返回值 **逻辑型**

从指定的文件中读取其所有数据到本对象中。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `文件名` | 文本型 | - | 请提供包含完整路径的文本名称。 |

### 写到文件

> 英文名 `WriteToFile` ｜ 返回值 **逻辑型**

将本对象中的数据写入指定的文件中。如果该文件不存在，则自动创建；如果该文件已经存在，其原有内容将被直接覆盖！

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `文件名` | 文本型 | - | 请提供包含完整路径的文本名称。 |

### 取缓冲区

> 英文名 `GetBuffer` ｜ 返回值 **整数型**

返回指定大小的缓冲区内存首地址，该地址同时也是内部数据指针。如果不能获取足够大小的缓冲区，将返回0。可以向该缓冲区直接写入数据，注意写入数据时请不要写到缓冲区之外。如果改变了缓冲区中数据的长度，必须及时调用“释放缓冲区()”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `缓冲区大小` | 整数型 | 可空 | 应大于零并小于“最大可用内存”。如果本参数被省略，默认为当前数据长度。 |

### 释放缓冲区

> 英文名 `ReleaseBuffer` ｜ 返回值 **逻辑型**

告知对象“最新的、经外界修改过的”数据长度。另请参考“取缓冲区()”中的说明。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `数据长度` | 整数型 | - | 经外界修改后的数据长度。 |

### 置可用内存

> 英文名 `SetMemLength` ｜ 返回值 **逻辑型**

设置本对象内部使用的最小可用内存和最大可用内存。使用本方法可以优化内存使用，但因为内部已经有适当的初始值，普通用户不必调用本方法。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `最小可用内存` | 整数型 | 可空 | 单位为字节。如果本参数被省略，则保持现有“最小可用内存”数值不变。其内部初始值为10240字节，约10K。 |
| 2 | `最大可用内存` | 整数型 | 可空 | 单位为字节。如果本参数被省略，则保持现有“最大可用内存”数值不变。其内部初始值为2147483647字节，约2G。 |

### 置内存增量

> 英文名 `SetMemGrowSize` ｜ 返回值 **逻辑型**

设置本对象内部使用的内存增量值。所谓“内存增量”，即每次需要申请内存时在原来的基础上增加的内存数量（多申请出来的内存可供后续操作使用，从而减少了申请内存的次数，提高了执行效率）。正确使用本方法可以优化内存使用并提高执行效率，但因为内部已经有适当的初始值，普通用户不必调用本方法。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `内存增量` | 整数型 | - | 单位为字节。如果此值过小，可能导致数据操作效率降低；如果此值过大，可能导致消耗较多内存。内部初始值为10240字节，约10K。 |

