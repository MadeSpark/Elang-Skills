# Sqlite数据库支持库

- **库标识**: `SqliteDB`
- **版本**: 2.0
- **GUID**: `{295DD8F2-3584-4780-818D-9569AC5BC9D8}`
- **作者**: 大有吴涛易语言软件开发有限公司
- **主页**: http://www.eyuyan.com
- **文件**: `SqliteDB`
- **可用命令数**: 230

Sqlite数据库是一个小型关系型文件数据库；跨平台；支持SQL语句、事务、触发器、视图；速度相当快；小巧且不依赖任何额外的驱动程序。Sqlite数据库是本地数据库，不是网络数据库。
    相对于易语言数据库，Sqlite数据库的优势是支持SQL语句、事务、触发器、视图；
    相对于Microsoft Access，Sqlite数据库的优势是跨平台、无需额外驱动；
    相对于其它非本地数据库，如Oracle、DB2、MS SQL Server、MySQL、PostgreSQL，Sqlite数据库具有小巧、速度快的优势。
    内部使用的Sqlite版本是3.2.5。本支持库将只为向后兼容而存在，不会再有版本升级，推荐使用“Sqlite3数据库支持库”。

## 数据类型

| 名称 | 说明 |
|---|---|
| `Sqlite数据库` | 对应于一个Sqlite数据库，用于数据库的打开/关闭，执行SQL语句，查询，及其它基本操作。 |
| `Sqlite表` | 对应于Sqlite数据库中的一个表，用于读写表内容。 |
| `Sqlite记录集` | 主要用于处理查询，对应于一个查询结果记录集；同时也可用于执行“非查询类”SQL语句（可含“SQL参数”） |
| `Sqlite字段信息` | 描述Sqlite字段定义信息，供“Sqlite数据库.创建表()”时使用。因为Sqlite数据库本质上是无类型的数据库，因而您完全可以忽略“类型”“最大文本长度”两个成员，而仅使用“名称”成员。 |

## 命令

### 是否已打开

> 英文名 `IsOpen` ｜ 返回值 **逻辑型**

### 打开

> 英文名 `Open` ｜ 返回值 **逻辑型**

打开或新建数据库。要打开或新建的数据库由第一个参数“数据库文件”指定（一个Sqlite数据库就是一个文件）；如果指定的数据库文件不存在，则由第二个参数“是否允许创建”决定是否以该文件名创建新数据库。如果参数“数据库文件”被省略或为空文本，且参数“是否允许创建”为“真”，则在“内存中”创建并打开数据库。执行成功返回“真”，失败返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `数据库文件` | 文本型 | 可空 | 欲打开的数据库文件，请使用完整的路径和文件名。 |
| 2 | `是否允许创建` | 逻辑型 | 可空 | 在指定的数据库文件不存在的情况下，如果本属性为“真”，则创建该数据库，否则直接返回“假”。本参数如果被省略，默认为“假”。 |

### 关闭

> 英文名 `Close` ｜ 返回值 **逻辑型**

### 执行SQL语句

> 英文名 `ExecuteSQL` ｜ 返回值 **逻辑型**

执行指定的一条或多条SQL语句。执行成功返回真，失败返回假。本方法不能处理带“SQL参数”的SQL语句。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `欲执行的SQL语句` | 文本型 | - | 多个SQL语句之间请以半角分号“;”隔开，最后一个SQL语句后面可以不加“;”。 |

### 取记录集

> 英文名 `GetDataset` ｜ 返回值 **库类型#3**

返回“Sqlite记录集”对象。本方法等效于“Sqlite记录集.置SQL语句()”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `SQL语句` | 文本型 | - | SQL语句中可包含零到多个“SQL参数”，形如“?”“:n”“$v”等，其中n代表一个数值，v代表一个文本。所有“SQL参数”均需经过“Sqlite记录集.绑定参数()”进行值绑定，未经绑定的“SQL参数”其值将视为null。 |

### 置最大等待时间

> 英文名 `SetBusyTimeout` ｜ 返回值 **逻辑型**

设置数据库被锁定时的最大等待时间，如果数据库超过最大等待时间后还没有被解锁，则中止尝试执行当前操作。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `最大等待时间(单位: 秒)` | 整数型 | - |  |

### 取错误码

> 英文名 `GetLastErrorCode` ｜ 返回值 **整数型**

取前面最近一次操作的执行结果的错误代码。如果错误代码为0表示前一次操作成功。

### 取错误文本

> 英文名 `GetLastErrorText` ｜ 返回值 **文本型**

取前面最近一次操作的执行结果的错误信息文本。如果前一次操作成功或本方法调用失败，将返回空文本。

### 取最新插入ID

> 英文名 `GetLastInsertRowId` ｜ 返回值 **长整数型**

取前面最近一次插入的记录的记录ID。记录ID从1开始。如果执行失败将返回0。

### 开始事务

> 英文名 `BeginTransaction` ｜ 返回值 **逻辑型**

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `事务名称` | 文本型 | 可空 |  |

### 提交事务

> 英文名 `CommitTransaction` ｜ 返回值 **逻辑型**

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `事务名称` | 文本型 | 可空 |  |

### 回滚事务

> 英文名 `RollbackTransaction` ｜ 返回值 **逻辑型**

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `事务名称` | 文本型 | 可空 |  |

### 表是否存在

> 英文名 `IsTableExist` ｜ 返回值 **逻辑型**

判断数据库是否已有指定的表或视图，包含临时表和视图。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `表名` | 文本型 | - |  |

### 取所有表

> 英文名 `GetTables` ｜ 返回值 **文本型**

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `是否包含表` | 逻辑型 | 有默认值 |  |
| 2 | `是否包含视图` | 逻辑型 | 有默认值 |  |
| 3 | `是否包含临时表或视图` | 逻辑型 | 有默认值 |  |

### 取表内容

> 英文名 `GetTableContent` ｜ 返回值 **文本型**

返回一个二维文本数组，其中存放了指定表的所有记录（含所有字段）内容；表中没有记录或操作失败，将返回空数组。如果表中有非文本型字段，将自动转换为文本型（可能丢失部分数据）。提示：使用“Sqlite表.读字段值()”或“Sqlite记录集.读字段值()”可正确地处理非文本类型字段。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `表名称` | 文本型 | - |  |
| 2 | `是否包含表头` | 逻辑型 | 有默认值 | 是否在返回值数组的第一行包含字段名称。如果指定表中没有任何记录，即使本参数为“真”，也将返回空数组。 |

### 取表定义

> 英文名 `GetTableSql` ｜ 返回值 **文本型**

返回创建指定表时所用的SQL语句（CREATE TABLE ...）。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `表名称` | 文本型 | - |  |

### 读字段值

> 英文名 `GetFieldValue` ｜ 返回值 **逻辑型**

读取指定表或视图中指定字段的值，并写入第四个参数“字段值”。如果“查询条件”参数指定不当或未指定，有可能导致产生多个满足查询条件的记录，此时将取第一条记录的指定字段值。执行成功返回“真”，失败返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `表名称` | 文本型 | - |  |
| 2 | `字段名称或位置` | 通用型 | - | 参数值可以为一个字段名称文本或者一个字段位置数值，字段位置数值从1开始。 |
| 3 | `查询条件` | 文本型 | 可空 |  |
| 4 | `字段值` | 通用型 | 参考 | 其中将存放取得的字段值。如果必要，将自动进行适当的数据类型转换。 |

### 创建表

> 英文名 `CreateTable` ｜ 返回值 **逻辑型**

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `表名称` | 文本型 | - |  |
| 2 | `字段信息` | 库类型#10 | 任意类型 |  |

### 删除表

> 英文名 `DropTable` ｜ 返回值 **逻辑型**

将指定表连同其中的所有记录全部删除。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `要删除的表名` | 文本型 | - |  |

### 清空表

> 英文名 `ClearTable` ｜ 返回值 **逻辑型**

删除指定表中所有记录。本方法不删除表自身，也不改变表结构。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `要清空的表名` | 文本型 | - |  |

### 收缩数据库

> 英文名 `ShrinkDB` ｜ 返回值 **逻辑型**

收缩数据所占用的磁盘空间。经过大批量记录增删之后，数据库文件可能会变的较大，调用此方法可释放一部分缓冲区域，以减少文件尺寸。

### 是否已打开

> 英文名 `IsOpen` ｜ 返回值 **逻辑型**

判断表是否处理打开状态

### 打开

> 英文名 `Open` ｜ 返回值 **逻辑型**

打开指定表。成功返回真，否则返回假。表成功打开后，记录集指针将指向首记录前。一个“Sqlite表”对象同一时刻只能打开一个表，打开后一个表意味着将关闭前一个已打开的表(如果有的话)。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `表名称` | 文本型 | - |  |
| 2 | `Sqlite数据库` | Sqlite表 | - | 指定要打开哪个Sqlite数据库中的表。该数据库必须已经打开。 |

### 关闭

> 英文名 `Close` ｜ 返回值 **逻辑型**

关闭当前打开的表。如果用户未调用本方法关闭已打开的表，支持库会在适当的时机自动关闭该表。

### 到首记录

> 英文名 `First` ｜ 返回值 **逻辑型**

移动到表的第一条记录，执行成功返回真，否则返回假。

### 到尾记录

> 英文名 `Last` ｜ 返回值 **逻辑型**

移动到表的最后一条记录，执行成功返回真，否则返回假。

### 到下一记录

> 英文名 `Next` ｜ 返回值 **逻辑型**

移动到下一条记录，执行成功返回真，否则返回假。

### 到上一记录

> 英文名 `Prior` ｜ 返回值 **逻辑型**

移动到上一条记录，执行成功返回真，否则返回假。

### 跳过

> 英文名 `Skip` ｜ 返回值 **逻辑型**

将当前记录指针向前或者向后移动数条记录。执行成功返回真，失败返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `欲跳过的记录数` | 整数型 | 可空 | 参数值如果为负数，则向前移动，否则向后移动。如果本参数被省略，默认值为 1 ，即向后移动一条记录。 |

### 跳到

> 英文名 `Goto` ｜ 返回值 **逻辑型**

改变当前记录指针到指定的记录号。成功返回真，失败返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `欲跳到的记录号` | 整数型 | - | 参数值所指定的记录号从 1 开始，即首记录的记录号为 1 ，依此类推。 |

### 首记录前

> 英文名 `Bof` ｜ 返回值 **逻辑型**

如果当前记录指针已在首记录的前面，返回真，否则返回假。

### 尾记录后

> 英文名 `eof` ｜ 返回值 **逻辑型**

如果当前记录指针已在尾记录的后面，返回真，否则返回假。

### 编辑

> 英文名 `Edit` ｜ 返回值 **逻辑型**

进入“编辑”状态，准备修改当前记录。在调用本方法后，可使用“写字段值()”修改当前记录中的各字段值；只有调用“提交()”后才会实际执行修改操作。

### 插入

> 英文名 `Insert` ｜ 返回值 **逻辑型**

进入“插入”状态，准备向表中插入一条记录。在调用本方法后，可使用“写字段值()”填充要插入记录中的各字段值；只有调用“提交()”后才会实际执行插入操作。

### 删除

> 英文名 `Delete` ｜ 返回值 **逻辑型**

删除当前记录。删除成功后，记录指针指向着记录前。

### 提交

> 英文名 `Post` ｜ 返回值 **逻辑型**

将“编辑()”或“插入()”后对记录所作的修改（通过“写字段值()”）提交到数据库，并退出“编辑”或“插入”状态。执行成功返回真，失败返回假。如果不执行此方法，数据库不会被实际修改。

### 取消

> 英文名 `Cancel` ｜ 返回值 **逻辑型**

取消“编辑()”或“插入()”后对记录所作的修改（通过“写字段值()”），并退出“编辑”或“插入”状态。

### 刷新

> 英文名 `Refresh` ｜ 返回值 **逻辑型**

重新从数据库中读取最新数据。一般在其它程序或线程修改了数据库后需调用本方法。注意本方法不改变当前记录号。

### 查找

> 英文名 `Locate` ｜ 返回值 **逻辑型**

本命令从当前记录开始（包括当前记录）寻找符合给定条件的记录。如成功找到返回真，并且将当前记录指针移至所找到的记录。如出错或未找到则返回假，当前记录指针位置保持不变。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `查找条件` | 文本型 | - | 查找条件应该类似于“字段名称=字段值”或“字段1名称=字段值1 AND 字段2名称=字段值2”等形式，也可以使用Sqlite所支持的其它SQL语法。此查找条件将作为SQL select语句中where子句的一部分。 |

### 读字段值

> 英文名 `GetFieldValue` ｜ 返回值 **逻辑型**

读取当前记录中指定字段的值，并写入第二个参数“字段值”中。执行成功返回“真”，失败返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从1开始。 |
| 2 | `字段值` | 通用型 | 参考 | 其中将存放取得的字段值。如果必要，将自动进行适当的数据类型转换。 |

### 写字段值

> 英文名 `SetFieldValue` ｜ 返回值 **逻辑型**

将第二个参数“字段值”数据写入当前记录中的指定字段。执行成功返回“真”，失败返回“假”。本方法通常在“编辑()”或“插入()”之后、“提交()”之前被调用，且仅当“提交()”调用后数据库才被实际修改。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 参数值可以为一个字段名称文本或者一个字段位置数值，字段位置数值从1开始。 |
| 2 | `字段值` | 通用型 | - | 指定欲写入的值 |

### 读字段文本值

> 英文名 `GetFieldTextValue` ｜ 返回值 **文本型**

读取当前记录中指定字段的值，并转换为文本型数据后返回。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从1开始。 |
| 2 | `执行结果` | 逻辑型 | 可空、参考 | 如果提供本参数，其中将存放本方法执行结果——成功为“真”，失败为“假”。 |

### 读字段整数值

> 英文名 `GetFieldIntValue` ｜ 返回值 **整数型**

读取当前记录中指定字段的值，并转换为整数型数据后返回。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从1开始。 |
| 2 | `执行结果` | 逻辑型 | 可空、参考 | 如果提供本参数，其中将存放本方法执行结果——成功为“真”，失败为“假”。 |

### 读字段小数值

> 英文名 `GetFieldFloatValue` ｜ 返回值 **小数型**

读取当前记录中指定字段的值，并转换为小数型数据后返回。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从1开始。 |
| 2 | `执行结果` | 逻辑型 | 可空、参考 | 如果提供本参数，其中将存放本方法执行结果——成功为“真”，失败为“假”。 |

### 读字段双精度小数值

> 英文名 `GetFieldDoubleValue` ｜ 返回值 **双精度小数型**

读取当前记录中指定字段的值，并转换为双精度小数型数据后返回。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从1开始。 |
| 2 | `执行结果` | 逻辑型 | 可空、参考 | 如果提供本参数，其中将存放本方法执行结果——成功为“真”，失败为“假”。 |

### 读字段字节集值

> 英文名 `GetFieldBinValue` ｜ 返回值 **字节集型**

读取当前记录中指定字段的值，并转换为字节集型数据后返回。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从1开始。 |
| 2 | `执行结果` | 逻辑型 | 可空、参考 | 如果提供本参数，其中将存放本方法执行结果——成功为“真”，失败为“假”。 |

### 读字段逻辑值

> 英文名 `GetFieldBoolValue` ｜ 返回值 **逻辑型**

读取当前记录中指定字段的值，并转换为逻辑型数据后返回。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从1开始。 |
| 2 | `执行结果` | 逻辑型 | 可空、参考 | 如果提供本参数，其中将存放本方法执行结果——成功为“真”，失败为“假”。 |

### 读字段日期时间值

> 英文名 `GetFieldDatetimeValue` ｜ 返回值 **日期时间型**

读取当前记录中指定字段的值，并转换为日期时间型数据后返回。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从1开始。 |
| 2 | `执行结果` | 逻辑型 | 可空、参考 | 如果提供本参数，其中将存放本方法执行结果——成功为“真”，失败为“假”。 |

### 读字段长整数值

> 英文名 `GetFieldInt64Value` ｜ 返回值 **长整数型**

读取当前记录中指定字段的值，并转换为长整数型数据后返回。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从1开始。 |
| 2 | `执行结果` | 逻辑型 | 可空、参考 | 如果提供本参数，其中将存放本方法执行结果——成功为“真”，失败为“假”。 |

### 读字段短整数值

> 英文名 `GetFieldShortValue` ｜ 返回值 **短整数型**

读取当前记录中指定字段的值，并转换为短整数型数据后返回。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从1开始。 |
| 2 | `执行结果` | 逻辑型 | 可空、参考 | 如果提供本参数，其中将存放本方法执行结果——成功为“真”，失败为“假”。 |

### 读字段字节值

> 英文名 `GetFieldByteValue` ｜ 返回值 **字节型**

读取当前记录中指定字段的值，并转换为字节型数据后返回。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从1开始。 |
| 2 | `执行结果` | 逻辑型 | 可空、参考 | 如果提供本参数，其中将存放本方法执行结果——成功为“真”，失败为“假”。 |

### 取表名

> 英文名 `GetTableName` ｜ 返回值 **文本型**

返回当前已打开的表名。如果表已被关闭，将返回空文本。

### 取记录号

> 英文名 `GetRecordNO` ｜ 返回值 **整数型**

返回当前记录的记录号，第一条记录为1，第二条记录为2，依次类推。如果当前记录指针在首记录前，返回0 ；如果在尾记录后，返回值为记录总数加一。

### 取记录个数

> 英文名 `GetRecordCount` ｜ 返回值 **整数型**

返回表中的记录个数。

### 取所有记录

> 英文名 `GetContent` ｜ 返回值 **文本型**

返回一个二维文本数组，其中存放了表中的所有记录（含所有字段）内容；表中没有记录或操作失败，将返回空数组。如果表中有非文本型字段，将自动转换为文本型（可能丢失部分数据）。本方法与“Sqlite数据库.取表内容()”等效。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `是否包含表头` | 逻辑型 | 有默认值 | 是否在返回值数组的第一行包含字段名称。如果指定表中没有任何记录，即使本参数为“真”，也将返回空数组。 |

### 取字段个数

> 英文名 `GetFieldCount` ｜ 返回值 **整数型**

取表中的字段个数

### 取所有字段

> 英文名 `GetFields` ｜ 返回值 **文本型**

返回一个一维文本型数组，其中包含了当前表中的所有字段名称。

### 置SQL语句

> 英文名 `SetSQL` ｜ 返回值 **逻辑型**

设置本对象所使用的SQL语句。本方法等效于“Sqlite数据库.取记录集()”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `SQL语句` | 文本型 | - | SQL语句中可包含零到多个“SQL参数”，形如“?”“:n”“$v”等，其中n代表一个数值，v代表一个文本。所有“SQL参数”均需经过“绑定参数()”进行值绑定，未经绑定的“SQL参数”其值将视为null。 |
| 2 | `Sqlite数据库` | Sqlite表 | - | 指定对哪个Sqlite数据库进行查询，该数据库必须已经打开 |

### 取参数个数

> 英文名 `GetParameterCount` ｜ 返回值 **整数型**

返回当前SQL语句（由“置SQL语句()”或“Sqlite数据库.取记录集()”指定）中所包含的“SQL参数”个数。

### 取所有参数

> 英文名 `GetParameters` ｜ 返回值 **文本型**

返回所有由“SQL参数”的参数名称组成的一维文本数组。如果某“SQL参数”没有名称，则对应的数组成员为空文本。

### 绑定参数

> 英文名 `BindParameter` ｜ 返回值 **逻辑型**

对当前SQL语句（由“置SQL语句()”或“Sqlite数据库.取记录集()”所指定）中所包含的某个“SQL参数”进行值绑定。所有“SQL参数”均需进行值绑定，未经绑定的“SQL参数”其值将视为null。如果对同一“SQL参数”进行多次绑定，则最后一次绑定的值将覆盖前面所绑定的值。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `参数名称或索引` | 通用型 | - | 使用参数名称时注意，“:”“$”也是参数名称的一部分；使用参数索引时注意，索引从1开始。 |
| 2 | `要绑定的值` | 通用型 | 可空 | 如果省略本参数，默认为null。 |

### 是否已打开

> 英文名 `IsOpen` ｜ 返回值 **逻辑型**

判断记录集是否已经打开。

### 打开

> 英文名 `Open` ｜ 返回值 **逻辑型**

打开或重新打开记录集。对于查询类(select)SQL语句，将当前记录指针指向第一条记录之前；对于执行类(update)SQL语句，执行该语句一次。此方法可以被多次重复调用。读取记录集数据之前，必须“打开()”记录集。

### 关闭

> 英文名 `Close` ｜ 返回值 **逻辑型**

关闭记录集。关闭后的记录集不再可用，所有内部数据全部丢失。如果用户没有调用本方法关闭记录集，支持库会在适当的时机自动关闭记录集。

### 读字段值

> 英文名 `GetFieldValue` ｜ 返回值 **逻辑型**

读取当前记录中指定字段的值，并写入第二个参数“字段值”中。执行成功返回“真”，失败返回“假”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 参数值可以为一个字段名称文本或者一个字段位置数值，字段位置数值从1开始。 |
| 2 | `字段值` | 通用型 | 参考 | 其中将存放取得的字段值。如果必要，将自动进行适当的数据类型转换。 |

### 到下一记录

> 英文名 `Next` ｜ 返回值 **逻辑型**

移动到下一条记录

### 首记录前

> 英文名 `Bof` ｜ 返回值 **逻辑型**

判断当前记录指针是否指向第一条记录之前。

### 尾记录后

> 英文名 `Eof` ｜ 返回值 **逻辑型**

判断当前记录指针是否指向最后一条记录之后。

### 取记录个数

> 英文名 `GetRecordCount` ｜ 返回值 **整数型**

### 取所有记录

> 英文名 `GetContent` ｜ 返回值 **文本型**

返回一个二维文本数组，其中存放了记录集中所有记录（含所有字段）内容；如果记录集中没有记录或操作失败，将返回空数组。如果记录集中有非文本型字段，将自动转换为文本型（可能丢失部分数据）。提示：使用“Sqlite表.读字段值()”或“Sqlite记录集.读字段值()”可正确地处理非文本类型字段。注意：本方法执行后，当前记录指针将指向首记录前（相当于重新打开记录集）。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `是否包含表头` | 逻辑型 | 有默认值 | 是否在返回值数组的第一行包含字段名称。如果本记录集中没有任何记录，即使本参数为“真”，也将返回空数组。 |

### 取字段个数

> 英文名 `GetFieldCount` ｜ 返回值 **整数型**

### 取所有字段

> 英文名 `GetFields` ｜ 返回值 **文本型**

返回所有字段名称组成的一维文本数组

### 读字段文本值

> 英文名 `GetFieldTextValue` ｜ 返回值 **文本型**

读取当前记录中指定字段的值，并转换为文本型数据后返回。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从1开始。 |
| 2 | `执行结果` | 逻辑型 | 可空、参考 | 如果提供本参数，其中将存放本方法执行结果——成功为“真”，失败为“假”。 |

### 读字段整数值

> 英文名 `GetFieldIntValue` ｜ 返回值 **整数型**

读取当前记录中指定字段的值，并转换为整数型数据后返回。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从1开始。 |
| 2 | `执行结果` | 逻辑型 | 可空、参考 | 如果提供本参数，其中将存放本方法执行结果——成功为“真”，失败为“假”。 |

### 读字段小数值

> 英文名 `GetFieldFloatValue` ｜ 返回值 **小数型**

读取当前记录中指定字段的值，并转换为小数型数据后返回。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从1开始。 |
| 2 | `执行结果` | 逻辑型 | 可空、参考 | 如果提供本参数，其中将存放本方法执行结果——成功为“真”，失败为“假”。 |

### 读字段双精度小数值

> 英文名 `GetFieldDoubleValue` ｜ 返回值 **双精度小数型**

读取当前记录中指定字段的值，并转换为双精度小数型数据后返回。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从1开始。 |
| 2 | `执行结果` | 逻辑型 | 可空、参考 | 如果提供本参数，其中将存放本方法执行结果——成功为“真”，失败为“假”。 |

### 读字段字节集值

> 英文名 `GetFieldBinValue` ｜ 返回值 **字节集型**

读取当前记录中指定字段的值，并转换为字节集型数据后返回。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从1开始。 |
| 2 | `执行结果` | 逻辑型 | 可空、参考 | 如果提供本参数，其中将存放本方法执行结果——成功为“真”，失败为“假”。 |

### 读字段逻辑值

> 英文名 `GetFieldBoolValue` ｜ 返回值 **逻辑型**

读取当前记录中指定字段的值，并转换为逻辑型数据后返回。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从1开始。 |
| 2 | `执行结果` | 逻辑型 | 可空、参考 | 如果提供本参数，其中将存放本方法执行结果——成功为“真”，失败为“假”。 |

### 读字段日期时间值

> 英文名 `GetFieldDatetimeValue` ｜ 返回值 **日期时间型**

读取当前记录中指定字段的值，并转换为日期时间型数据后返回。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从1开始。 |
| 2 | `执行结果` | 逻辑型 | 可空、参考 | 如果提供本参数，其中将存放本方法执行结果——成功为“真”，失败为“假”。 |

### 读字段长整数值

> 英文名 `GetFieldInt64Value` ｜ 返回值 **长整数型**

读取当前记录中指定字段的值，并转换为长整数型数据后返回。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从1开始。 |
| 2 | `执行结果` | 逻辑型 | 可空、参考 | 如果提供本参数，其中将存放本方法执行结果——成功为“真”，失败为“假”。 |

### 读字段短整数值

> 英文名 `GetFieldShortValue` ｜ 返回值 **短整数型**

读取当前记录中指定字段的值，并转换为短整数型数据后返回。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从1开始。 |
| 2 | `执行结果` | 逻辑型 | 可空、参考 | 如果提供本参数，其中将存放本方法执行结果——成功为“真”，失败为“假”。 |

### 读字段字节值

> 英文名 `GetFieldByteValue` ｜ 返回值 **字节型**

读取当前记录中指定字段的值，并转换为字节型数据后返回。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段名称或位置` | 通用型 | - | 指定欲读取的字段，可以为一个字段名称文本或者一个字段位置数值，字段位置数值从1开始。 |
| 2 | `执行结果` | 逻辑型 | 可空、参考 | 如果提供本参数，其中将存放本方法执行结果——成功为“真”，失败为“假”。 |

