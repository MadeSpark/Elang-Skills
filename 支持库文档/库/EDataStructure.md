# 数据结构支持库

- **库标识**: `EDataStructure`
- **版本**: 2.0
- **GUID**: `0AFF5F28B2CB42ce906AA27D6E485457`
- **作者**: 大连大有吴涛易语言软件软件开发有限公司
- **主页**: http://www.eyuyan.com
- **文件**: `EDataStructure`
- **可用命令数**: 97

提供节点，链表，栈，队列，树，二叉树、表(Map)等数据类型

## 数据类型

| 名称 | 说明 |
|---|---|
| `节点` | 该数据类型是一种带有属性数据的节点数据类型，一个节点可以拥有零个或多个属性数据 |
| `链表` | 该数据类型是一种数据容器，其中可以保存“节点”型数据，在内存中以数据链形式存在 |
| `栈` | 该数据类型是一种数据容器，其中可以保存“节点”型数据，该容器以“后进先出”的顺序存取数据 |
| `队列` | 该数据类型是一种数据容器，其中可以保存“节点”型数据，该容器以“先进先出”的顺序存取数据 |
| `二叉树` | 该数据类型是一种数据容器，其中可以保存“节点”型数据，该容器以二叉树结构存取数据。该二叉树是根据每个节点的键值组织成为二叉排序树，该二叉树的上的任何一个节点的键值一定大于它的左子树节点的键值并且小于右子树节点的键值 |
| `树` | 该数据类型是一种数据容器，其中可以保存“节点”型数据，该容器以普通树结构存取数据 |
| `表` | 该数据类型是一种数据容器，其数据为多个“键-值”对，其中“键”为字节集类型，“值”为“节点”数据。表（Map）的主要用途是在大批量数据中快速查找相应的数据（根据“键”查找“值”）。 |

## 命令

### 加入属性

> 英文名 `AddAttr` ｜ 返回值 **逻辑型**

在节点中加入属性。成功加入返回真，失败返回假。如果失败，可以通过“取失败原因”方法得到出错信息

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `属性名称` | 文本型 | - | “加入属性”方法中欲加入的新的属性名称 |
| 2 | `属性值` | 通用型 | - | “加入属性”方法中欲加入的新的属性值 |

### 删除属性

> 英文名 `DelAttr` ｜ 返回值 **逻辑型**

从节点中删除属性。成功删除返回真，失败返回假。如果失败，可以通过“取失败原因”方法得到出错信息

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `属性名称` | 文本型 | - | “删除属性”方法中欲删除的属性名称 |

### 修改属性

> 英文名 `ModifyAttr` ｜ 返回值 **逻辑型**

修改节点的属性值。成功修改返回真，失败返回假。如果失败，可以通过“取失败原因”方法得到出错信息

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `属性名称` | 文本型 | - | “修改属性”方法中欲修改的属性名称 |
| 2 | `新属性值` | 通用型 | - | “修改属性”方法中欲修改的新的属性值 |

### 取全部属性名

> 英文名 `GetAllAttrName` ｜ 返回值 **逻辑型**

取出节点的全部属性名称。成功返回真，失败返回假。如果失败，可以通过“取失败原因”方法得到出错信息

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `属性名称数组` | 文本型 | - | “取全部属性名”方法的填充参数。方法执行完毕后，该参数的内容为全部属性名称的数组。如果方法执行失败，该参数内容无意义 |

### 取属性类型

> 英文名 `GetAttrKind` ｜ 返回值 **逻辑型**

取出节点属性值的数据类型。成功返回真，失败返回假。如果失败，可以通过“取失败原因”方法得到出错信息

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `属性名称` | 文本型 | - | “取属性类型”方法中欲取类型的属性名称 |
| 2 | `类型值` | 整数型 | 参考 | “取属性类型”方法的填充参数。方法执行完毕后，该参数的内容为属性类型的代表数值。0代表数值型，1代表逻辑型，2代表日期型，3代表文本型，4代表字节集型。如果方法执行失败，该参数内容无意义 |

### 取数值

> 英文名 `GetDoubleValue` ｜ 返回值 **逻辑型**

取出节点的数值型属性值。成功返回真，失败返回假。如果失败，可以通过“取失败原因”方法得到出错信息

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `属性名称` | 文本型 | - | “取数值”方法中欲取值的属性名称 |
| 2 | `属性值` | 通用型 | 参考 | “取数值”方法的填充参数。该参数必须为数值型，否则“取数值”方法返回假。方法执行完毕后，该参数的内容为属性的数值，如果方法执行失败，该参数内容无意义 |

### 取逻辑值

> 英文名 `GetBoolValue` ｜ 返回值 **逻辑型**

取出节点的逻辑型属性值。成功返回真，失败返回假。如果失败，可以通过“取失败原因”方法得到出错信息

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `属性名称` | 文本型 | - | “取逻辑值”方法中欲取值的属性名称 |
| 2 | `属性值` | 逻辑型 | 参考 | “取逻辑值”方法的填充参数。方法执行完毕后，该参数的内容为属性的逻辑值，如果方法执行失败，该参数内容无意义 |

### 取日期值

> 英文名 `GetDateValue` ｜ 返回值 **逻辑型**

取出节点的日期型属性值。成功返回真，失败返回假。如果失败，可以通过“取失败原因”方法得到出错信息

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `属性名称` | 文本型 | - | “取日期值”方法中欲取值的属性名称 |
| 2 | `属性值` | 日期时间型 | 参考 | “取日期值”方法的填充参数。方法执行完毕后，该参数的内容为属性的日期值，如果方法执行失败，该参数内容无意义 |

### 取文本值

> 英文名 `GetTextValue` ｜ 返回值 **逻辑型**

取出节点的文本型属性值。成功返回真，失败返回假。如果失败，可以通过“取失败原因”方法得到出错信息

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `属性名称` | 文本型 | - | “取文本值”方法中欲取值的属性名称 |
| 2 | `属性值` | 文本型 | 参考 | “取文本值”方法的填充参数。方法执行完毕后，该参数的内容为属性的文本值，如果方法执行失败，该参数内容无意义 |

### 取字节集值

> 英文名 `GetBinValue` ｜ 返回值 **逻辑型**

取出节点的字节集型属性值。成功返回真，失败返回假。如果失败，可以通过“取失败原因”方法得到出错信息

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `属性名称` | 文本型 | - | “取字节集值”方法中欲取值的属性名称 |
| 2 | `属性值` | 字节集型 | 参考 | “取字节集值”方法的填充参数。方法执行完毕后，该参数的内容为属性的字节集值，如果方法执行失败，该参数内容无意义 |

### 是否为空

> 英文名 `ListIsEmpty` ｜ 返回值 **逻辑型**

判断链表中是否存有数据。如果链表为空返回真，如果链表非空返回假

### 取大小

> 英文名 `ListGetCount` ｜ 返回值 **整数型**

取出链表中节点的数量

### 加入节点

> 英文名 `ListAddNode` ｜ 返回值 **逻辑型**

在链表中加入新的节点。加入链表中自动根据键值参数进行从小到大的排序。键值参数可以省略，如果省略，默认键值为链表中最大的键值加一。该方法执行成功返回真，失败返回假。如果失败，可以通过“取失败原因”方法得到出错信息

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `节点值` | 链表 | - | “加入节点”方法中欲加入到链表中的节点数据 |
| 2 | `键值` | 整数型 | 可空 | “加入节点”方法中欲加入到链表中的节点数据的键值。这个值也是节点在链表中的唯一标识，节点在链表中存在的过程中键值是不允许改变的。该键值必须大于等于0 |

### 删除首节点

> 英文名 `ListRemoveHead` ｜ 返回值 **逻辑型**

删除链表的首节点。该方法执行成功返回真，失败返回假。如果失败，可以通过“取失败原因”方法得到出错信息

### 删除尾节点

> 英文名 `ListRemoveTail` ｜ 返回值 **逻辑型**

删除链表的尾节点。该方法执行成功返回真，失败返回假。如果失败，可以通过“取失败原因”方法得到出错信息

### 全部删除

> 英文名 `ListRemoveAll` ｜ 返回值 **逻辑型**

删除链表的全部节点。该方法执行成功返回真，失败返回假。如果失败，可以通过“取失败原因”方法得到出错信息

### 删除当前节点

> 英文名 `ListRemoveCurNode` ｜ 返回值 **逻辑型**

删除链表的当前节点。该方法执行成功返回真，失败返回假。如果失败，可以通过“取失败原因”方法得到出错信息

### 删除节点

> 英文名 `ListRemoveNodeFromKey` ｜ 返回值 **逻辑型**

删除链表中指定键值的节点。该方法执行成功返回真，失败返回假。如果失败，可以通过“取失败原因”方法得到出错信息

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `键值` | 整数型 | - | “删除节点”方法中欲删除节点数据的键值 |

### 修改节点

> 英文名 `ListUpdateNodeFromKey` ｜ 返回值 **逻辑型**

使用参数节点替换链表中指定键值的节点。该方法执行成功返回真，失败返回假。如果失败，可以通过“取失败原因”方法得到出错信息

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `节点值` | 链表 | - | “修改节点”方法中链表节点被更改后的新节点数据 |
| 2 | `键值` | 整数型 | - | “修改节点”方法中欲更改的节点数据的键值 |

### 修改当前节点

> 英文名 `ListUpdateCurNode` ｜ 返回值 **逻辑型**

修改链表中的当前节点为参数节点。该方法执行成功返回真，失败返回假。如果失败，可以通过“取失败原因”方法得到出错信息

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `节点值` | 链表 | - | “修改当前节点”方法中链表节点被更改后的新节点数据 |

### 向下移动

> 英文名 `ListGoNext` ｜ 返回值 **逻辑型**

链表中的当前节点向下移动一个节点。该方法执行成功返回真，执行失败返回假。如果是链表尾节点执行“向下移动”，返回假

### 向上移动

> 英文名 `ListGoPrev` ｜ 返回值 **逻辑型**

链表中的当前节点向上移动一个节点。该方法执行成功返回真，执行失败返回假。如果是链表首节点执行“向上移动”，返回假

### 到首节点

> 英文名 `ListGoHead`

链表中的当前节点移动到首节点

### 到尾节点

> 英文名 `ListGoTail`

链表中的当前节点移动到尾节点

### 搜索节点

> 英文名 `ListSearch` ｜ 返回值 **逻辑型**

链表中的当前节点移动到参数键值指定的节点。该方法执行成功返回真，执行失败返回假。如果失败，可以通过“取失败原因”方法得到出错信息

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `键值` | 整数型 | - | “搜索节点”方法中欲搜索节点数据的键值 |

### 取当前节点键值

> 英文名 `ListGetCurNodeKey` ｜ 返回值 **逻辑型**

取链表中当前节点的键值。该方法执行成功返回真，执行失败返回假。如果失败，可以通过“取失败原因”方法得到出错信息

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `键值` | 整数型 | 参考 | “取当前节点键值”方法的填充参数。方法成功执行，该参数的内容为链表中当前节点的键值 |

### 取当前节点

> 英文名 `CListGetCurNode` ｜ 返回值 **逻辑型**

取链表中的当前节点。该方法执行成功返回真，执行失败返回假。如果失败，可以通过“取失败原因”方法得到出错信息

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `节点值` | 链表 | 参考 | “取当前节点”方法的填充参数。方法成功执行，该参数的内容为链表中当前节点。如果方法执行失败，该参数内容无意义 |

### 取节点

> 英文名 `CListGetNode` ｜ 返回值 **逻辑型**

根据参数键值取链表中的节点。该方法执行成功返回真，执行失败返回假。如果失败，可以通过“取失败原因”方法得到出错信息

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `节点值` | 链表 | 参考 | “取节点”方法的填充参数。方法成功执行，该参数的内容为链表中的节点数据。如果方法执行失败，该参数内容无意义 |
| 2 | `键值` | 整数型 | - | “取节点”方法中欲取得节点数据的键值 |

### 是否为空

> 英文名 `StackIsEmpty` ｜ 返回值 **逻辑型**

判断栈中是否存有数据。如果栈为空返回真，如果栈非空返回假

### 取大小

> 英文名 `StackGetCount` ｜ 返回值 **整数型**

取出栈中节点的数量

### 清空

> 英文名 `StackClear`

删除栈中的所有节点

### 取栈顶节点

> 英文名 `StackPeek` ｜ 返回值 **逻辑型**

取栈顶端的节点值，与“弹出”方法不同的是栈顶的节点并不出栈。该方法执行成功返回真，执行失败返回假。如果失败，可以通过“取失败原因”方法得到出错信息

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `节点值` | 链表 | 参考 | “取栈顶节点”方法的填充参数，该方法执行完毕，该参数的内容为从栈顶的节点值。如果方法执行失败，该参数内容无意义 |

### 压入

> 英文名 `StackPush` ｜ 返回值 **逻辑型**

向栈中压入一个节点。该方法执行成功返回真，执行失败返回假。如果失败，可以通过“取失败原因”方法得到出错信息

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `节点值` | 链表 | - | “压入”方法中欲压到栈中节点 |

### 弹出

> 英文名 `StackPop` ｜ 返回值 **逻辑型**

从栈中弹出一个节点。该方法执行成功返回真，执行失败返回假。如果失败，可以通过“取失败原因”方法得到出错信息

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `节点值` | 链表 | 参考 | “弹出”方法的填充参数，该方法执行完毕，该参数的内容为从栈顶弹出的节点值。如果方法执行失败，该参数内容无意义 |

### 是否为空

> 英文名 `QueueIsEmpty` ｜ 返回值 **逻辑型**

判断队列中是否存有数据。如果栈为空返回真，如果栈非空返回假

### 取大小

> 英文名 `QueueGetCount` ｜ 返回值 **整数型**

取出队列中节点的数量

### 清空

> 英文名 `QueueClear`

删除队列中的所有节点

### 取队列首节点

> 英文名 `QueuePeek` ｜ 返回值 **逻辑型**

取队列的首节点，与“弹出”方法不同的是对列的首节点并不弹出，仍保留在对列中。该方法执行成功返回真，执行失败返回假。如果失败，可以通过“取失败原因”方法得到出错信息

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `节点值` | 链表 | 参考 | “取队列首节点”方法的填充参数，该方法执行完毕，该参数的内容为队列首节点值。如果方法执行失败，该参数内容无意义 |

### 压入

> 英文名 `QueuePush` ｜ 返回值 **逻辑型**

向队列的尾部压入一个节点。该方法执行成功返回真，执行失败返回假。如果失败，可以通过“取失败原因”方法得到出错信息

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `节点值` | 链表 | - | “压入”方法中欲压到队列尾的节点 |

### 弹出

> 英文名 `QueuePop` ｜ 返回值 **逻辑型**

从队列的首部弹出一个节点。该方法执行成功返回真，执行失败返回假。如果失败，可以通过“取失败原因”方法得到出错信息

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `节点值` | 链表 | 参考 | “弹出”方法的填充参数，该方法执行完毕，该参数的内容为从队列首弹出的节点值 |

### 是否为空

> 英文名 `BiTreeIsEmpty` ｜ 返回值 **逻辑型**

判断二叉树中是否存有数据。如果二叉树为空返回真，如果二叉树非空返回假

### 取大小

> 英文名 `BiTreeGetCount` ｜ 返回值 **整数型**

取出二叉树中节点的数量

### 取二叉树深度

> 英文名 `BiTreeGetDeep` ｜ 返回值 **整数型**

取出二叉树的深度

### 清空

> 英文名 `BiTreeEmpty`

删除二叉树中的所有节点

### 到左子树

> 英文名 `BiTreeGoLeft` ｜ 返回值 **逻辑型**

移动二叉树的当前节点到它的左子树节点上。成功返回真，如果失败或者二叉树的左子树为空，返回假

### 到右子树

> 英文名 `BiTreeGoRight` ｜ 返回值 **逻辑型**

移动二叉树的当前节点到它的右子树节点上。成功返回真，如果失败或者二叉树的右子树为空，返回假

### 搜索节点

> 英文名 `BiTreeFind` ｜ 返回值 **逻辑型**

移动二叉树的当前节点到指定键值的节点上。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `节点键值` | 整数型 | - | 欲搜索二叉树中节点的键值 |

### 到根节点

> 英文名 `BiTreeGoRoot` ｜ 返回值 **逻辑型**

移动二叉树的当前节点到根节点上。成功返回真，失败返回假

### 到父节点

> 英文名 `BiTreeGoParent` ｜ 返回值 **逻辑型**

移动二叉树的当前节点到其父节点上。成功返回真，失败返回假。如果当前节点为根节点，执行该方法返回假

### 取当前节点

> 英文名 `BiTreeGetCurNode` ｜ 返回值 **逻辑型**

取二叉树的当前节点。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `节点值` | 链表 | 参考 | “取当前节点”方法的填充参数，该方法执行完毕，该参数的内容为二叉树的当前节点值 |

### 取当前节点键值

> 英文名 `BiTreeGetCurNodeKey` ｜ 返回值 **逻辑型**

取二叉树的当前节点的键值。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `键值` | 整数型 | 参考 | “取当前节点键值”方法的填充参数，该方法执行完毕，该参数的内容为二叉树的当前节点的键值 |

### 插入节点

> 英文名 `BiTreeInsert` ｜ 返回值 **逻辑型**

向二叉树中插入节点。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `节点值` | 链表 | - | “插入节点”方法中欲插入到二叉树中的节点值 |
| 2 | `键值` | 整数型 | 可空 | “插入节点”方法中欲插入到二叉树中的节点的键值。该键值不能为负数。该参数可以省略，如果省略，则新插入节点的键值为二叉树中的最大键值加一 |

### 修改节点

> 英文名 `BiTreeUpdate` ｜ 返回值 **逻辑型**

修改二叉树中指定键值的节点。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `节点值` | 链表 | - | “修改节点”方法中修改后的节点值 |
| 2 | `键值` | 整数型 | - | “修改节点”方法中欲修改节点的键值 |

### 修改当前节点

> 英文名 `BiTreeUpdateCurNode` ｜ 返回值 **逻辑型**

修改二叉树中当前节点。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `节点值` | 链表 | - | “修改当前节点”方法中修改后的节点值 |

### 删除节点

> 英文名 `BiTreeDelete` ｜ 返回值 **逻辑型**

删除二叉树中指定键值的节点。如果参数“删除标志”为假，仅删除一个节点的情况下分为以下几种不同的情形和执行结果：一，欲删除节点没有子节点，直接删除节点。二，欲删除节点仅有左子节点，删除节点后，把删除节点的左子节点连接到删除节点的双亲上。三，欲删除节点仅有右子节点，删除节点后，把删除节点的右子节点连接到删除节点的双亲上。四，欲删除节点有左子节点和右子节点，删除节点后，把删除节点的左子树上键值最大的节点（也是最右边的节点）连接到删除节点的双亲上，也就是提升到删除节点的位置，然后把它的左子树连接到它以前位置的双亲上。成功删除后，二叉树的当前节点值恢复为根节点值。该方法成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `键值` | 整数型 | - | “删除节点”方法中欲删除节点的键值 |
| 2 | `删除标记` | 逻辑型 | 可空 | 该标记为真时表示删除节点及其所有的子节点，为假时表示仅删除单个节点。该参数可以省略，默认为假 |

### 删除当前节点

> 英文名 `BiTreeDeleteCurNode` ｜ 返回值 **逻辑型**

删除二叉树中当前节点。删除原则和“删除节点”方法相同。成功删除后，二叉树的当前节点值恢复为根节点值。该方法成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `删除标记` | 逻辑型 | 可空 | 该标记为真时表示删除节点及其所有的子节点，为假时表示仅删除单个节点。该参数可以省略，默认为假 |

### 是否为空

> 英文名 `TreeIsEmpty` ｜ 返回值 **逻辑型**

判断树中是否存有数据。如果树为空返回真，如果树非空返回假

### 取大小

> 英文名 `TreeGetCount` ｜ 返回值 **整数型**

取出树中节点的数量

### 清空

> 英文名 `TreeEmpty`

删除树中的所有节点

### 到子节点

> 英文名 `TreeGoChild` ｜ 返回值 **逻辑型**

移动当前节点到它的第一个子节点。成功返回真，失败或者当前节点没有子节点返回假。树的一个节点可以有多个子节点，该方法仅能移动当前节点到它的第一个子节点，其他节点的遍历可以通过“到兄弟节点”方法实现

### 到兄弟节点

> 英文名 `TreeGoSibling` ｜ 返回值 **逻辑型**

移动当前节点到它的下一个兄弟节点。成功返回真，失败或者当前节点没有下一个兄弟节点返回假。父节点的所有子节点以链表的形态存在，该方法仅能向一个方向移动，在其中的一个子节点可以通过该方法移动到下一个兄弟节点，但是无法反向移动

### 到根节点

> 英文名 `TreeGoRoot` ｜ 返回值 **逻辑型**

移动当前节点到树的根节点。成功返回真，失败返回假。

### 到父节点

> 英文名 `TreeGoParent` ｜ 返回值 **逻辑型**

移动当前节点到其父节点。成功返回真，失败返回假。

### 取当前节点

> 英文名 `TreeGetCurNode` ｜ 返回值 **逻辑型**

取树的当前节点。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `节点值` | 链表 | 参考 | “取当前节点”方法的填充参数，该方法执行完毕，该参数的内容为树的当前节点值 |

### 取子节点数

> 英文名 `TreeGetChildCount` ｜ 返回值 **整数型**

取当前节点的子节点数量

### 插入节点

> 英文名 `TreeInsert` ｜ 返回值 **逻辑型**

向树中插入节点，插入位置为当前节点的子节点链表的末端。成功返回真，失败返回假。如果树是一个空的树，则把插入的节点作为树的根节点

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `节点值` | 链表 | - | “插入节点”方法中欲插入到树中的节点值 |

### 修改当前节点

> 英文名 `TreeUpdateCurNode` ｜ 返回值 **逻辑型**

修改树中当前节点的值为参数节点的值。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `节点值` | 链表 | - | “修改当前节点”方法中修改后的节点值 |

### 删除当前节点

> 英文名 `TreeDeleteCurNode` ｜ 返回值 **逻辑型**

删除树中当前节点。成功删除后，树的当前节点值恢复为根节点值。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `删除标记` | 逻辑型 | 可空 | 该标记为真时表示删除节点及其所有的子节点，为假时表示仅删除单个节点。如果该节点有子节点，该参数同时为假，该方法不执行删除操作，返回假。该参数可以省略，默认为假 |

### 取节点索引

> 英文名 `TreeGetCurNodeIndex` ｜ 返回值 **整数型**

取当前节点在其父节点的所有子节点数组中的索引。如果失败将返回-1

### 取失败原因

> 英文名 `DSGetErrMsg` ｜ 返回值 **文本型**

得到上次方法执行失败的原因信息。该方法必须在方法执行失败后马上执行，才能正确取出失败原因

### 加入键值对

> 英文名 `AddKeyValue` ｜ 返回值 **逻辑型**

将“键”和“值”两者配对加入到对象中。如果已存在相同的“键”，则新“值”将覆盖旧“值”。执行成功返回真，失败返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `键` | 字节集型 | - |  |
| 2 | `值` | 链表 | - |  |

### 取键值

> 英文名 `GetKeyValue` ｜ 返回值 **逻辑型**

如果对象中没有指定的键，则返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `键` | 字节集型 | - |  |
| 2 | `值` | 链表 | 参考 |  |

### 取键值数

> 英文名 `GetSize` ｜ 返回值 **整数型**

返回对象中“键-值”对的个数。

### 取所有键

> 英文名 `GetAllKeys` ｜ 返回值 **字节集型**

返回由所有键组合的字节集数组。

### 删除键值对

> 英文名 `DelKeyValue` ｜ 返回值 **逻辑型**

删除对象中指定的键及其值。成功返回真，失败返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `键` | 字节集型 | - |  |

### 删除所有键值对

> 英文名 `DelAllKeyValues` ｜ 返回值 **逻辑型**

