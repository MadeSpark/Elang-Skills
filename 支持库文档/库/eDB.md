# 数据库操作支持库

- **库标识**: `eDB`
- **版本**: 2.7
- **GUID**: `46E94341933A462383A4DE26B146322C`
- **作者**: 大有吴涛易语言软件公司
- **主页**: http://dywt.com.cn
- **文件**: `eDB`
- **可用命令数**: 56

本支持库用来访问各种类型数据库。由于本库是架构在ADO基础上的，因此请确保您的系统或者程序使用的系统装有ADO 2.10.3711.9或以上版本。当前最新版本是MDAC_TYP2.8，其中包括了ADO，如果需要，可到微软网站上下载（下载参考地址：http://www.microsoft.com/downloads/details.aspx?displaylang=zh-cn&FamilyID=6c050fe3-c795-4b7d-b037-185d0506396c）

## 数据类型

| 名称 | 说明 |
|---|---|
| `数据库连接` | 本类型用来连接大多数数据库，起到数据库连接的作用。连接后，请使用“记录集”类型绑定连接（记录集.置连接()）后读写数据库数据。 |
| `记录集` | 本类型用来打开并操作记录集，与“数据库连接”对象配合使用。操作记录集前应调用“记录集.置连接()”设置关联的数据库连接对象。 |

## 命令

### 连接

> 英文名 `Connect` ｜ 返回值 **逻辑型**

连接到数据源，如果连接成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `连接文本` | 文本型 | - | 本参数提供连接时使用的连接文本 |

### 连接Access

> 英文名 `ConnectAccess` ｜ 返回值 **逻辑型**

连接Access数据库，如果连接成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `文件名` | 文本型 | - | 本参数提供Access数据库文件的完整路径名 |
| 2 | `密码` | 文本型 | 有默认值 | 本参数提供Access数据库的访问密码。可以为空，若为空，表示Access数据库没有密码 |

### 连接SQLServer

> 英文名 `ConnectSQLServer` ｜ 返回值 **逻辑型**

连接SQL Server数据库，如果连接成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `服务器名` | 文本型 | - | 本参数提供 SQL SERVER 服务器名 |
| 2 | `数据库名` | 文本型 | - |  |
| 3 | `用户名` | 文本型 | - |  |
| 4 | `密码` | 文本型 | - |  |

### 关闭

> 英文名 `Close` ｜ 返回值 **逻辑型**

断开当前数据库连接，成功返回真，失败返回假

### 执行SQL

> 英文名 `ExecuteSQL` ｜ 返回值 **逻辑型**

执行参数指定的SQL语句或存储过程调用，执行成功返回真，否则返回假。另请参考“记录集.打开()”。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `SQL语句` | 文本型 | - | 本参数提供要执行的SQL语句。有关SQL语句的具体内容请参看SQL的专业手册 |
| 2 | `超时时间` | 整数型 | 可空 | 单位为秒，用于设定本命令最长执行时间，超时后直接返回假。本命令执行后将自动恢复先前的超时时间。如果本参数被省略或为-1，表示不设置超时时间（即自动采用先前设置的超时时间，或数据库默认的超时时间）。如果本参数为0，表示设置超时时间为无限长。 |

### 取得权限

> 英文名 `GetMode` ｜ 返回值 **整数型**

取得访问及相关权限，并返回一个整数代表用户连接当前的权限。该整数为以下常量值之一或之和：0、#未知权限； 1、#只读权限； 2、#只写权限； 3、#读写权限； 4、#独占读权限； 8、#独占写权限； 12、#独占权限；16、#共享权限

### 设置权限

> 英文名 `SetMode` ｜ 返回值 **逻辑型**

设置访问及相关权限，并返回一个逻辑型值，如果设置成功，即返回真，如果设置不成功，即返回假。注意，该方法只能在连接处于关闭状态的时候才可以使用。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `要设置的模式` | 整数型 | - | 要设置的模式为以下常量值之一或之和：0、#未知权限； 1、#只读权限； 2、#只写权限； 3、#读写权限； 4、#独占读权限； 8、#独占写权限； 12、#独占权限；16、#共享权限 |

### 置连接超时

> 英文名 `SetConnectTimeout` ｜ 返回值 **逻辑型**

用于限制连接数据库时最长等待时间，以秒为单位，若时间超出，则连接失败

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `超时时间` | 整数型 | - | 单位为秒。0表示超时时间为无限长。 |

### 取连接超时

> 英文名 `GetConnectTimeout` ｜ 返回值 **整数型**

取得连接超时时间，以秒为单位

### 开始事务

> 英文名 `BeginTrans` ｜ 返回值 **整数型**

开始新事务，返回一个整数型值，该数值表示当前事务嵌套的层数，譬如如果返回1，则表明为顶层事务

### 保存事务

> 英文名 `CommitTrans` ｜ 返回值 **逻辑型**

保存任何更改并结束当前事务，返回一个逻辑值，如果保存事务成功返回真，否则返回假

### 回滚事务

> 英文名 `RollbackTrans` ｜ 返回值 **逻辑型**

取消当前事务中所作的任何更改并结束事务，返回一个逻辑值，如果操作成功返回真，否则返回假

### 置连接

> 英文名 `SetConnection` ｜ 返回值 **逻辑型**

设置数据库连接

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `连接` | 记录集 | - | 本参数提供要设置的数据库连接对象 |

### 取连接

> 英文名 `GetConnection` ｜ 返回值 **记录集**

取得数据库连接

### 打开

> 英文名 `Open` ｜ 返回值 **逻辑型**

打开记录集。根据参数“命令类型”的不同，可以打开SQL记录集、数据库表、XML或ADTG持久文件，或执行存储过程。执行成功返回真，否则返回假。注意：必须先使用“数据库连接”连接数据库，再用“记录集.置连接()”关联数据库连接，然后才能打开记录集进行数据操作。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `命令文本` | 文本型 | - | 本参数可以是SQL语句、数据表名、XML或ADTG持久文件名，或存储过程调用语句，具体视参数“命令类型”而定。 |
| 2 | `命令类型` | 整数型 | - | 本参数提供指定命令文本的类型，可以为以下常量或数值之一：1、#SQL语句；2、#数据表名；4、#存储过程；256、持久文件名。如果要打开持久文件，与本记录集绑定的数据库连接可先调用“数据库连接.连接持久文件()”。 |
| 3 | `游标类型` | 整数型 | 可空 | 可以为以下常量或数值之一：1、#无游标；2、#服务器端游标；3、#客户端游标。如果此参数被省略，默认为 #客户端游标。以上三个常量分别对应ADO中CursorLocationEnum类型常量 adUseNone, adUseServer, adUseClient，如不理解请保持默认值不变。在某些情况下设置为 #服务器端游标 可解决对Access数据库执行SQL语句不能应用like操作符的错误。 |

### 打开并排序

> 英文名 `OpenAndSort` ｜ 返回值 **逻辑型**

打开数据表，请切以指定条件排序。返回一个逻辑型值，如果排序成功返回真，否则返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `表名` | 文本型 | - | 本参数提供数据库表的名称 |
| 2 | `排序条件` | 文本型 | - | 本参数指定排序条件，ASC为升序，DESC为降序例如让学生姓名字段按照升序排，成绩字段按照降序排，那排序条件可以为 "学生姓名 ASC,成绩 DESC" |

### 关闭

> 英文名 `Close`

关闭当前打开的数据库表，打开一个数据表并使用后必须进行关闭操作

### 添加

> 英文名 `AddNew` ｜ 返回值 **逻辑型**

添加一个新的空记录，返回一个逻辑值。如果添加成功返回真，否则返回假

### 更新

> 英文名 `Update` ｜ 返回值 **逻辑型**

更新记录，刷新显示，返回一个逻辑值。如果更新成功返回真，否则返回假

### 删除

> 英文名 `Delete` ｜ 返回值 **逻辑型**

删除记录，返回一个逻辑值。如果删除成功返回真，否则返回假，选项可为删除当前记录，或全部记录

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `删除选项` | 整数型 | - | 本参数为以下常量或数值之一：1、删除当前记录；3、删除全部记录。注：删除全部记录仅对以“表名称”的打开的记录集有效，对以“查询类SQL语句(selete)”打开的记录集无效；请参考“打开”方法。 |

### 写文本

> 英文名 `WriteString` ｜ 返回值 **逻辑型**

通过序号或字段名写入文本。返回一个逻辑型值，如果写成功返回真，否则返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `序号或字段名` | 通用型 | - | 本参数提供字段的序号或者字段名，只能为数值或文本型，序号值从 0 开始 |
| 2 | `文本` | 文本型 | - | 本参数提供所要写入的文本 |

### 写双精度

> 英文名 `WriteDouble` ｜ 返回值 **逻辑型**

通过序号或字段名写入双精度值。返回一个逻辑型值，如果写成功返回真，否则返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `序号或字段名` | 通用型 | - | 本参数提供字段的序号或者字段名，只能为数值或文本型，序号值从 0 开始 |
| 2 | `双精度` | 双精度小数型 | - | 本参数提供要写入的双精度小数值 |

### 写单精度

> 英文名 `WriteFloat` ｜ 返回值 **逻辑型**

通过序号或字段名写入单精度值。返回一个逻辑型值，如果写成功返回真，否则返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `序号或字段名` | 通用型 | - | 本参数提供字段的序号或者字段名，只能为数值或文本型，序号值从 0 开始 |
| 2 | `单精度` | 小数型 | - | 本参数提供要写入的单精度小数值 |

### 写整数

> 英文名 `WriteInt` ｜ 返回值 **逻辑型**

通过序号或字段名写入整型值。返回一个逻辑型值，如果写成功返回真，否则返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `序号或字段名` | 通用型 | - | 本参数提供字段的序号或者字段名，只能为数值或文本型，序号值从 0 开始 |
| 2 | `整数` | 整数型 | - | 本参数提供要写入的整数值 |

### 写逻辑值

> 英文名 `WriteBool` ｜ 返回值 **逻辑型**

通过序号或字段名写入逻辑型数据。返回一个逻辑型值，如果写成功返回真，否则返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `序号或字段名` | 通用型 | - | 本参数提供字段的序号或者字段名，只能为数值或文本型，序号值从 0 开始 |
| 2 | `逻辑` | 逻辑型 | - | 本参数提供要写入的逻辑型值 |

### 写日期时间

> 英文名 `WriteDateTime` ｜ 返回值 **逻辑型**

通过序号或字段名写入日期型数据。返回一个逻辑型值，如果写成功返回真，否则返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `序号或字段名` | 通用型 | - | 本参数提供字段的序号或者字段名，只能为数值或文本型，序号值从 0 开始 |
| 2 | `日期时间` | 日期时间型 | - | 本参数提供要写入的日期型数据 |
| 3 | `日期时间类型` | 整数型 | 可空 | 本参数提供要写入的日期时间型数据的类型，为以下值之一：0、日期时间型；1、日期型；2、时间型。注意：如果参数值被省略或为无效值，将视为0 |

### 读文本

> 英文名 `ReadString` ｜ 返回值 **逻辑型**

通过序号或字段名读取文本数据到变量中。返回一个逻辑型值，如果读成功返回真，否则返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `序号或字段名` | 通用型 | - | 本参数提供字段的序号或者字段名，只能为数值或文本型，序号值从 0 开始 |
| 2 | `文本变量` | 文本型 | 参考 | 本参数提供要读取到的文本变量 |

### 读双精度

> 英文名 `ReadDouble` ｜ 返回值 **逻辑型**

通过序号或字段名读取双精度小数数据到变量中。返回一个逻辑型值，如果读成功返回真，否则返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `序号或字段名` | 通用型 | - | 本参数提供字段的序号或者字段名，只能为数值或文本型，序号值从 0 开始 |
| 2 | `双精度变量` | 双精度小数型 | 参考 | 本参数提供要读取到的双精度变量 |

### 读单精度

> 英文名 `ReadFloat` ｜ 返回值 **逻辑型**

通过序号或字段名读取单精度小数数据到变量中。返回一个逻辑型值，如果读成功返回真，否则返回假。注意，MSSQL中的float类型视其精度可能是单精度或双精度，如为后者，用本命令读取将返回假，但其值仍被尽量读取（数值超出易语言小数范围则被截断）。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `序号或字段名` | 通用型 | - | 本参数提供字段的序号或者字段名，只能为数值或文本型，序号值从 0 开始 |
| 2 | `单精度变量` | 小数型 | 参考 | 本参数提供要读取到的单精度变量 |

### 读整数

> 英文名 `ReadInt` ｜ 返回值 **逻辑型**

通过序号或字段名读取整数数据到变量中。返回一个逻辑型值，如果读成功返回真，否则返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `序号或字段名` | 通用型 | - | 本参数提供字段的序号或者字段名，只能为数值或文本型，序号值从 0 开始 |
| 2 | `整型变量` | 整数型 | 参考 | 本参数提供要读取到的整型变量 |

### 读逻辑值

> 英文名 `ReadBool` ｜ 返回值 **逻辑型**

通过序号或字段名读取逻辑型数据到变量中。返回一个逻辑型值，如果读成功返回真，否则返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `序号或字段名` | 通用型 | - | 本参数提供字段的序号或者字段名，只能为数值或文本型，序号值从 0 开始 |
| 2 | `逻辑变量` | 逻辑型 | 参考 | 本参数提供要读取到的逻辑变量 |

### 读日期时间

> 英文名 `ReadDateTime` ｜ 返回值 **逻辑型**

通过序号或字段名读取日期数据到变量中。返回一个逻辑型值，如果读成功返回真，否则返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `序号或字段名` | 通用型 | - | 本参数提供字段的序号或者字段名，只能为数值或文本型，序号值从 0 开始 |
| 2 | `日期变量` | 日期时间型 | 参考 | 本参数提供要读取到的日期变量 |

### 保存到XML

> 英文名 `SaveToXML` ｜ 返回值 **逻辑型**

将数据库表保存到XML文本文件，以作为交换通用数据表，返回一个逻辑值，如果保存XML文件成功返回真，否则返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `XML文件名` | 文本型 | - | 本参数提供要写出到的XML文件名 |

### 保存到ADTG

> 英文名 `SaveToADTG` ｜ 返回值 **逻辑型**

将数据库表用专用的"Advanced Data Tablegram"格式保存。返回一个逻辑值，如果保存ADTG文件成功返回真，否则返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `ADTG文件名` | 文本型 | - | 本参数提供要写出到的ADTG文件名 |

### 到首记录

> 英文名 `MoveFirst` ｜ 返回值 **逻辑型**

移动到表的第一条记录，返回一个逻辑值，如果移动成功返回真，否则返回假。操作本命令之前要确保数据库及表打开

### 到尾记录

> 英文名 `MoveLast` ｜ 返回值 **逻辑型**

移动到表的最后一条记录，返回一个逻辑值，如果移动成功返回真，否则返回假。操作本命令之前要确保数据库及表打开

### 到下一条

> 英文名 `MoveNext` ｜ 返回值 **逻辑型**

移动到下一条记录，返回一个逻辑值，如果移动成功返回真，否则返回假。操作本命令之前要确保数据库及表打开

### 到前一条

> 英文名 `MovePrevious` ｜ 返回值 **逻辑型**

移动到前一条记录，返回一个逻辑值，如果移动成功返回真，否则返回假。操作本命令之前要确保数据库及表打开

### 移到

> 英文名 `Move` ｜ 返回值 **逻辑型**

移到指定记录

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `目标记录号` | 整数型 | - | 本参数提供目标记录号值，该值从 0 开始 |

### 取字段名

> 英文名 `GetFieldName` ｜ 返回值 **文本型**

取得指定序号字段的名称，返回一个文本型值，为字段名称文本

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字段序号` | 整数型 | - | 本参数提供当前表中指定字段的序号值，从 0 开始 |

### 取字段属性

> 英文名 `GetFieldAttributes` ｜ 返回值 **整数型**

通过序号或字段名取得字段属性，返回一个整数值。为以下常量值之一或之和： 2、#字段被延迟； 4、#字段可写入； 8、#写入无法确定； 16、#定长数据； 32、#允许空值； 64、#可以读空值； 128、#二进制类型； 256、#持久标识符； 512、#包含标记； 4096、#字段被缓存

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `序号或字段名` | 通用型 | - | 本参数提供字段的序号或者字段名，只能为数值或文本型，序号值从 0 开始 |

### 取字段定义长度

> 英文名 `GetFieldDefineSize` ｜ 返回值 **整数型**

通过序号或字段名取得字段定义长度，本命令返回一个整数值

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `序号或字段名` | 通用型 | - | 本参数提供字段的序号或者字段名，只能为数值或文本型，序号值从 0 开始 |

### 取字段实际长度

> 英文名 `GetFieldActualSize` ｜ 返回值 **整数型**

通过序号或字段名取得字段实际长度，返回一个整数型值

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `序号或字段名` | 通用型 | - | 本参数提供字段的序号或者字段名，只能为数值或文本型，序号值从 0 开始 |

### 取字段类型

> 英文名 `GetFieldType` ｜ 返回值 **整数型**

通过序号或字段名取得字段类型，返回以下常量值之一： 20、#超长整数字段； 128、#二进制字段； 11、#逻辑型字段； 8、#宽文本字段1； 129、#字符型字段； 6、#货币型字段； 7、#日期型字段； 133、#数据库日期型字段； 134、#时间型字段； 135、#时间戳字段； 14、#数值型字段； 5、#双精度字段； 0、#空白型字段； 10、#错误码字段； 72、#标识型字段； 9、#IDispatch字段； 3、#整数型字段； 13、#IUnknown字段； 205、#超变长二进制字段； 201、#超变长文本字段； 203、#超变长宽文本字段； 131、#数字型字段； 4、#单精度字段； 2、#短整数字段； 16、#有符号字节字段； 21、#无符号超长整数字段； 19、#无符号整数字段； 18、#无符号短整数字段； 17、#字节型字段； 132、#自定义型字段； 204、#变长二进制字段； 200、#变长文本字段； 12、#变体型字段； 202、#变长宽文本字段； 130、#宽文本字段2

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `序号或字段名` | 通用型 | - | 本参数提供字段的序号或者字段名，只能为数值或文本型，序号值从 0 开始 |

### 字段是否为空

> 英文名 `IsFieldNull` ｜ 返回值 **逻辑型**

判断指定字段是否为空。返回一个逻辑型值，如果为空即为真，否则为假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `序号或字段名` | 通用型 | - | 本参数提供字段的序号或者字段名，只能为数值或文本型，序号值从 0 开始 |

### 查找

> 英文名 `Find` ｜ 返回值 **逻辑型**

查找满足条件的记录，返回一个逻辑型值，如果找到记录即返回真，并停留在满足条件的记录上，否则返回假。找到第一个满足条件的记录后，以后可以用"查找下一个"方法继续进行查找

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `查找条件` | 文本型 | - | 本参数提供在数据表中查找的条件 |
| 2 | `查找方向` | 整数型 | - | 本参数指定在数据表中查找的方向。可以为以下常量之一：1、#正向搜索； -1、#反向搜索 |

### 查找下一个

> 英文名 `FindNext` ｜ 返回值 **逻辑型**

查找下一个满足条件的记录。此命令用于"查找"命令之后使用，可继续按查找命令找到下一个满足条件的记录。返回一个逻辑型值，如果找到返回真，否则返回假

### 过滤记录

> 英文名 `SetFilter` ｜ 返回值 **逻辑型**

过滤出满足条件的记录，返回一个逻辑型值，如果过滤成功返回真，否则返回假。注意：在不使用过滤的时候，一定要再次调用本函数并将过滤条件指定为空文本

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `过滤条件` | 文本型 | - | 本参数提供过滤的条件 |

### 打开并过滤

> 英文名 `OpenFilter` ｜ 返回值 **逻辑型**

用指定过滤条件打开一个表

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `表名` | 文本型 | - | 数据库表名 |
| 2 | `过滤条件` | 文本型 | - | 过滤的条件，例如："文本字段 = 'abc' and 整数字段 > 1" |

### 写字节集

> 英文名 `WriteBin` ｜ 返回值 **逻辑型**

通过序号或字段名写入字节集数据到变量中。返回一个逻辑型值，如果读成功返回真，否则返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `序号或字段名` | 通用型 | - | 本参数提供字段的序号或者字段名，只能为数值或文本型，序号值从 0 开始 |
| 2 | `数据` | 字节集型 | - | 本参数将写入读取的数据 |

### 读字节集

> 英文名 `ReadBin` ｜ 返回值 **逻辑型**

通过序号或字段名读取字节集数据到变量中。返回一个逻辑型值，如果读成功返回真，否则返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `序号或字段名` | 通用型 | - | 本参数提供字段的序号或者字段名，只能为数值或文本型，序号值从 0 开始 |
| 2 | `数据` | 字节集型 | 参考 | 本参数提供要读入的数据 |

### 写长整数

> 英文名 `WriteInt64` ｜ 返回值 **逻辑型**

通过序号或字段名写入长整数值。返回一个逻辑型值，如果写成功返回真，否则返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `序号或字段名` | 通用型 | - | 本参数提供字段的序号或者字段名，只能为数值或文本型，序号值从 0 开始 |
| 2 | `长整数值` | 长整数型 | - | 本参数提供要写入的长整数值 |

### 读长整数

> 英文名 `ReadInt64` ｜ 返回值 **逻辑型**

通过序号或字段名读取长整数型数据到变量中。返回一个逻辑型值，如果读成功返回真，否则返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `序号或字段名` | 通用型 | - | 本参数提供字段的序号或者字段名，只能为数值或文本型，序号值从 0 开始 |
| 2 | `长整数变量` | 长整数型 | 参考 | 本参数提供要读取到的长整数变量 |

### 连接持久文件

> 英文名 `ConnectPersist` ｜ 返回值 **逻辑型**

用于连接持久文件，如：用“记录集.保存到XML()”生成的XML文件，或用“记录集.保存到ADTG()”生成的ADTG文件。参见“记录集.打开()”。

### 置命令超时

> 英文名 `SetCommandTimeout` ｜ 返回值 **逻辑型**

用于限制数据库操作命令的最长执行时间，以秒为单位，若时间超出，则命令执行失败并提前返回

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `超时时间` | 整数型 | - | 单位为秒。0表示超时时间为无限长。 |

### 取命令超时

> 英文名 `GetCommandTimeout` ｜ 返回值 **整数型**

取得命令执行超时时间，以秒为单位


## 常量

| 名称 | 值 | 说明 |
|---|---|---|
| `未知权限` |  | 表明权限尚未设置或无法确定 |
| `只读权限` |  | 表明访问权限为只读 |
| `只写权限` |  | 表明访问权限为只写 |
| `读写权限` |  | 表明访问权限为读或写 |
| `独占读权限` |  | 防止其他用户使用读权限打开连接 |
| `独占写权限` |  | 防止其他用户使用写权限打开连接 |
| `独占权限` |  | 防止其他用户使用任何权限打开连接 |
| `共享权限` |  | 不拒绝其他任何用户使用任何权限打开连接 |
| `SQL语句` |  | 指示命令文本为SQL语句。参见“记录集.打开()”。 |
| `数据表名` |  | 指示命令文本为数据表名，系统将自动生成SQL语句查询返回该表中的所有行。参见“记录集.打开()”。 |
| `删除当前记录` |  |  |
| `删除全部记录` |  |  |
| `字段被延迟` |  | 指示字段被延迟, 即不从拥有整个记录的数据源检索字段值, 仅在显式访问这些字段时才进行检索 |
| `字段可写入` |  | 指示可以写入该字段 |
| `写入无法确定` |  | 指示提供者无法确定是否可以写入该字段 |
| `定长数据` |  | 指示该字段包含定长数据 |
| `允许空值` |  | 指示该字段接受 Null 值 |
| `可以读空值` |  | 指示可以从该字段读取 Null 值 |
| `二进制类型` |  | 指示该字段为长二进制字段。例如: 可以保存图像文件 |
| `持久标识符` |  | 指示字段包含持久的行标识符, 该标识符无法被写入,并且除了对行进行标识(如记录号、唯一标识符等)外不存在有意义的值 |
| `包含标记` |  | 指示该字段包含用来跟踪更新的某种时间或日期标记 |
| `字段被缓存` |  | 指示提供者缓存了字段值, 并已完成随后对缓存的读取 |
| `超长整数字段` |  | 8字节带符号的整数 |
| `二进制字段` |  | 二进制值 |
| `逻辑型字段` |  | 布尔型值 |
| `宽文本字段1` |  | 以零字符结尾的 Unicode 字符串 |
| `字符型字段` |  | 字符串值 (ASCII) |
| `货币型字段` |  | 货币值.货币数字的小数点位置固定、小数点右侧有四位数字。该值保存为 8 字节范围为10,000 的带符号整型值 |
| `日期型字段` |  | 日期值。日期按双精度型数值来保存，数字全部表示从 1899 年 12 月 30 开始的日期数。小数部分是一天当中的片段时间 |
| `数据库日期型字段` |  | 日期值。日期按 yyyymmdd 的方式保存 |
| `时间型字段` |  | 时间值 (hhmmss) |
| `时间戳字段` |  | 时间戳 (yyyymmddhhmmss 加 10 亿分之一的小数) |
| `数值型字段` |  | 具有固定精度和范围的精确数值 |
| `双精度字段` |  | 双精度浮点值 |
| `空白型字段` |  | 未指定值 |
| `错误码字段` |  | 32位错误代码 |
| `标识型字段` |  | 全局唯一的标识符 (GUID) |
| `IDispatch字段` |  | OLE 对象上 Idispatch 接口的指针 |
| `整数型字段` |  | 4 字节的带符号整型 |
| `IUnknown字段` |  | OLE 对象上 IUnknown 接口的指针 |
| `超变长二进制字段` |  | 超长的可变长度二进制值 |
| `超变长文本字段` |  | 超长的可变长度字符串值 |
| `超变长宽文本字段` |  | 宽字符的长字符串值 |
| `数字型字段` |  | 具有固定精度和范围的精确数字值 |
| `单精度字段` |  | 单精度浮点值 |
| `短整数字段` |  | 2 字节带符号整型 |
| `有符号字节字段` |  | 1 字节带符号整型 |
| `无符号超长整数字段` |  | 8 字节不带符号整型 |
| `无符号整数字段` |  | 4 字节不带符号整型 |
| `无符号短整数字段` |  | 2 字节不带符号整型 |
| `字节型字段` |  | 1字节不带符号整型 |
| `自定义型字段` |  | 用户自定义数据类型字段 |
| `变长二进制字段` |  |  |
| `变长文本字段` |  |  |
| `变体型字段` |  | 自动变体型 |
| `变长宽文本字段` |  | 以零字符结尾的可变长 Unicode 字符串 |
| `宽文本字段2` |  | 以零字符结尾的 Unicode 字符串 |
| `正向搜索` |  | 向前搜索。 |
| `反向搜索` |  | 向后搜索。 |
| `无游标` |  | 向前搜索。 |
| `服务器端游标` |  | 向后搜索。 |
| `客户端游标` |  | 向后搜索。 |
| `存储过程` |  | 指示命令文本为存储过程调用语句。参见“记录集.打开()”。 |
| `持久文件名` |  | 指示命令文本为持久文件完整路径文件名，如：用“记录集.保存到XML()”生成的XML文件，或用“记录集.保存到ADTG()”生成的ADTG文件。参见“记录集.打开()”。 |
