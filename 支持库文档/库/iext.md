# 扩展界面支持库一

- **库标识**: `iext`
- **版本**: 2.0
- **GUID**: `27bb20fdd3e145e4bee3db39ddd6e64c`
- **作者**: 大有吴涛易语言软件公司
- **主页**: http://www.eyuyan.com
- **文件**: `iext`
- **可用命令数**: 90

本支持库用作实现对扩展界面元素的支持，需要 Internet Explorer 4.0 或以上版本的支持。

## 数据类型

| 名称 | 说明 |
|---|---|
| `树型框` |  |
| `状态条` |  |
| `工具条` |  |
| `超级列表框` |  |
| `透明标签` |  |

## 命令

### 取项目数

> 英文名 `GetCount` ｜ 返回值 **整数型**

返回树型框中所有项目的数目

### 取缩进层次

> 英文名 `GetIndent` ｜ 返回值 **整数型**

返回指定项目所处缩进层次，层次值从1开始。如果未找到指定项目，返回 0 

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `项目索引` | 整数型 | - | 0 为项目一，1 为项目二，如此类推 |

### 取项目文本

> 英文名 `GetText` ｜ 返回值 **文本型**

返回指定项目的文本。如果该项目不存在，将返回空文本

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `项目索引` | 整数型 | - | 0 为项目一，1 为项目二，如此类推 |

### 取图片

> 英文名 `GetImage` ｜ 返回值 **整数型**

返回指定项目的图片索引。如果该项目不存在，将返回 -1 

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `项目索引` | 整数型 | - | 0 为项目一，1 为项目二，如此类推 |

### 取选中图片

> 英文名 `GetSelImage` ｜ 返回值 **整数型**

返回指定项目被选中后的图片索引。如果该项目不存在，将返回 -1 

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `项目索引` | 整数型 | - | 0 为项目一，1 为项目二，如此类推 |

### 取项目数值

> 英文名 `GetData` ｜ 返回值 **整数型**

返回与指定项目相关联的数值。如果该项目不存在，将返回 0 

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `项目索引` | 整数型 | - | 0 为项目一，1 为项目二，如此类推 |

### 置项目文本

> 英文名 `SetText` ｜ 返回值 **逻辑型**

设置指定项目的文本。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `项目索引` | 整数型 | - | 0 为项目一，1 为项目二，如此类推 |
| 2 | `欲置入的项目文本` | 文本型 | - |  |

### 置图片

> 英文名 `SetImage` ｜ 返回值 **逻辑型**

设置在指定项目首部所显示图片的索引。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `项目索引` | 整数型 | - | 0 为项目一，1 为项目二，如此类推 |
| 2 | `图片索引` | 整数型 | - | 图片索引用于指定图片组属性中的某张图片，从 0 开始 |

### 置选中图片

> 英文名 `SetSelImage` ｜ 返回值 **逻辑型**

设置指定项目被选中后所显示图片的索引。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `项目索引` | 整数型 | - | 0 为项目一，1 为项目二，如此类推 |
| 2 | `图片索引` | 整数型 | - | 图片索引用于指定图片组属性中的某张图片，从 0 开始 |

### 置项目数值

> 英文名 `SetData` ｜ 返回值 **逻辑型**

设置与指定项目相关联的数值。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `项目索引` | 整数型 | - | 0 为项目一，1 为项目二，如此类推 |
| 2 | `欲置入的项目数值` | 整数型 | 可空 | 该数值与指定项目相关联。如果本参数被省略，默认值为 0  |

### 加粗

> 英文名 `Bold`

将指定项目的标题加粗显示

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `项目索引` | 整数型 | - | 0 为项目一，1 为项目二，如此类推 |

### 取消加粗

> 英文名 `Normal`

将指定项目的标题取消加粗显示

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `项目索引` | 整数型 | - | 0 为项目一，1 为项目二，如此类推 |

### 扩展

> 英文名 `Expand`

打开指定项目的下属分枝

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `项目索引` | 整数型 | - | 0 为项目一，1 为项目二，如此类推 |

### 收缩

> 英文名 `Collapse`

缩回指定项目的下属分枝

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `项目索引` | 整数型 | - | 0 为项目一，1 为项目二，如此类推 |

### 保证显示

> 英文名 `EnsureVisible`

确保指定项目能够显示出来，如果不能显示，则自动滚动并显示该项目

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `项目索引` | 整数型 | - | 0 为项目一，1 为项目二，如此类推 |

### 进入编辑

> 英文名 `Edit`

让指定项目进入编辑状态，执行本命令成功的前提是“允许编辑”属性为真

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `项目索引` | 整数型 | - | 0 为项目一，1 为项目二，如此类推 |

### 加入项目

> 英文名 `Add` ｜ 返回值 **整数型**

将单个或一批项目加入到树型框中，如果仅加入一个项目，成功返回加入后该项目所处的位置，失败返回 -1 。如果成批加入项目，成功返回 0 ，失败返回 -1 。成批加入项目可以用作一次性快速加入大量项目到树型框中

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `父项目索引` | 整数型 | 可空 | 指定欲加入项目所处的父项目，0 为项目一，1 为项目二，如此类推。如果没有父项目（即欲加入项目为顶层项目），请提供值 -1 。如果本参数被省略，默认值为 -1  |
| 2 | `项目文本` | 文本型 | 任意类型 | 如果欲加入一批项目，请提供单维数组数据 |
| 3 | `图片索引` | 整数型 | 可空、任意类型 | 图片索引用于指定图片组属性中的某张图片，从 0 开始。如果欲加入一批项目，可以提供单维数组数据为每个被加入项目指定单独的图片索引值，也可以仅提供一个数值以应用于所有被加入项目。如果本参数被省略，所有被加入项目的图片索引都默认为 0  |
| 4 | `选中图片索引` | 整数型 | 可空、任意类型 | 选中图片索引用于指定项目被选中后所显示的图片，-1 表示与图片索引一致。如果欲加入一批项目，可以提供单维数组数据为每个被加入项目指定单独的选中图片索引值，也可以仅提供一个数值以应用于所有被加入项目。如果本参数被省略，所有被加入项目的选中图片索引都默认为 -1  |
| 5 | `项目数值` | 整数型 | 可空、任意类型 | 指定与本项目相关联的数值。如果欲加入一批项目，可以提供单维数组数据为每个被加入项目指定单独的项目数值，也可以仅提供一个数值以应用于所有被加入项目。如果本参数被省略，所有被加入项目的项目数值都默认为 0  |
| 6 | `是否加粗` | 逻辑型 | 可空、任意类型 | 指定本项目标题是否加粗显示。如果欲加入一批项目，可以提供单维数组数据为每个被加入项目单独指定是否加粗，也可以仅提供一个参数值以应用于所有被加入项目。如果本参数被省略，所有被加入项目的项目数值都默认为假。 |
| 7 | `检查框状态` | 整数型 | 可空、任意类型 | 指定本项目的检查框状态，仅在属性“是否有检查框”为真时生效。如果欲加入一批项目，可以提供单维数组数据为每个被加入项目指定单独的检查框状态，也可以仅提供一个参数值以应用于所有被加入项目。如果本参数被省略，所有被加入项目的项目数值都默认为 0 。默认情况下，0：未选中，1：选中，2：半选中。 |

### 删除项目

> 英文名 `Delete` ｜ 返回值 **逻辑型**

删除树型框中的指定项目。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `项目索引` | 整数型 | - | 0 为项目一，1 为项目二，如此类推 |

### 清空

> 英文名 `Clear`

删除树型框中的所有项目

### 清空

> 英文名 `Clear`

删除状态条中的所有栏目

### 加入栏目

> 英文名 `Add` ｜ 返回值 **整数型**

将指定栏目加入到状态条中，返回加入后该项目所处的位置

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `栏目文本` | 文本型 | 可空 | 栏目文本指定栏目中的文本内容。如果本参数被省略，默认值为空文本 |
| 2 | `宽度` | 整数型 | 可空 | 单位为像素，如果为 -1 ，表示一直到窗口的最右边。如果被省略，默认值为 50  |
| 3 | `动态提示文本` | 文本型 | 可空 | 动态提示文本仅当栏目只有图片但没有内容文本或者内容文本不能被全部显示的情况下才有效。如果本参数被省略，默认值为空文本 |
| 4 | `图片索引` | 整数型 | 可空 | 图片索引用于指定图片组属性中的某张图片，该图片将被显示在栏目的首部。索引值从 0 开始，-1 表示无图片。如果本参数被省略，默认值为 -1  |
| 5 | `外观类型` | 整数型 | 可空 | 外观类型指定栏目的显示外观，可以为以下常量值之一： 0: #凹入； 1: #无边框； 2: #凸出。如果本参数被省略，默认值为“#凹入” |

### 删除栏目

> 英文名 `Remove`

删除指定的栏目

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `栏目索引` | 整数型 | - | 0 为栏目一，1 为栏目二，如此类推 |

### 取栏目数

> 英文名 `GetCount` ｜ 返回值 **整数型**

取回状态条中栏目的总数

### 取文本

> 英文名 `GetText` ｜ 返回值 **文本型**

取回指定栏目的内容文本

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `栏目索引` | 整数型 | - | 0 为栏目一，1 为栏目二，如此类推 |

### 取提示文本

> 英文名 `GetTipText` ｜ 返回值 **文本型**

取回指定栏目的提示文本

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `栏目索引` | 整数型 | - | 0 为栏目一，1 为栏目二，如此类推 |

### 取宽度

> 英文名 `Width` ｜ 返回值 **整数型**

取回指定栏目的宽度(单位为像素)

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `栏目索引` | 整数型 | - | 0 为栏目一，1 为栏目二，如此类推 |

### 取类型

> 英文名 `GetType` ｜ 返回值 **整数型**

取回指定栏目的外观类型，返回值为以下常量值之一： 0: #凹入； 1: #无边框； 2: #凸出

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `栏目索引` | 整数型 | - | 0 为栏目一，1 为栏目二，如此类推 |

### 取图片

> 英文名 `GetPic` ｜ 返回值 **整数型**

取回指定栏目的图片索引，图片索引指向图片组属性中的某一图片，从 0 开始，-1 表示本栏目无图片标志

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `栏目索引` | 整数型 | - | 0 为栏目一，1 为栏目二，如此类推 |

### 取栏目左边

> 英文名 `GetLeft` ｜ 返回值 **整数型**

取回指定栏目在状态条中显示区域的左边位置，单位为像素

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `栏目索引` | 整数型 | - | 0 为栏目一，1 为栏目二，如此类推 |

### 取栏目顶边

> 英文名 `GetTop` ｜ 返回值 **整数型**

取回指定栏目在状态条中显示区域的顶边位置，单位为像素

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `栏目索引` | 整数型 | - | 0 为栏目一，1 为栏目二，如此类推 |

### 取栏目宽度

> 英文名 `GetWidth` ｜ 返回值 **整数型**

取回指定栏目在状态条中显示区域的宽度，单位为像素

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `栏目索引` | 整数型 | - | 0 为栏目一，1 为栏目二，如此类推 |

### 取栏目高度

> 英文名 `GetHeight` ｜ 返回值 **整数型**

取回指定栏目在状态条中显示区域的高度，单位为像素

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `栏目索引` | 整数型 | - | 0 为栏目一，1 为栏目二，如此类推 |

### 置文本

> 英文名 `SetText`

设置指定栏目的文本

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `栏目索引` | 整数型 | - | 0 为栏目一，1 为栏目二，如此类推 |
| 2 | `栏目文本` | 文本型 | 可空 | 栏目文本指定栏目中的文本内容。如果本参数被省略，默认值为空文本 |

### 置提示文本

> 英文名 `SetTipText`

设置指定栏目的提示文本

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `栏目索引` | 整数型 | - | 0 为栏目一，1 为栏目二，如此类推 |
| 2 | `动态提示文本` | 文本型 | 可空 | 动态提示文本仅当栏目只有图片但没有内容文本或者内容文本不能被全部显示的情况下才有效。如果本参数被省略，默认值为空文本 |

### 置宽度

> 英文名 `SetWidth`

设置指定栏目的宽度(单位像素)

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `栏目索引` | 整数型 | - | 0 为栏目一，1 为栏目二，如此类推 |
| 2 | `宽度` | 整数型 | 可空 | 单位为像素，如果为 -1 ，表示一直到窗口的最右边。如果被省略，默认值为 50  |

### 置类型

> 英文名 `SetType`

设置指定栏目的外观类型

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `栏目索引` | 整数型 | - | 0 为栏目一，1 为栏目二，如此类推 |
| 2 | `外观类型` | 整数型 | 可空 | 外观类型指定栏目的显示外观，可以为以下常量值之一： 0: #凹入； 1: #无边框； 2: #凸出。如果本参数被省略，默认值为“#凹入” |

### 置图片

> 英文名 `SetPic`

设置指定栏目的图片索引，图片索引指向图片组属性中的某一图片，从 0 开始，-1 表示本栏目无图片标志

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `栏目索引` | 整数型 | - | 0 为栏目一，1 为栏目二，如此类推 |
| 2 | `图片索引` | 整数型 | 可空 | 图片索引用于指定图片组属性中的某张图片，该图片将被显示在栏目的首部。索引值从 0 开始，-1 表示无图片。如果本参数被省略，默认值为 -1  |

### 插入按钮

> 英文名 `Insert` ｜ 返回值 **整数型**

将指定按钮插入到工具条中，返回插入后该按钮所处的位置

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `插入位置` | 整数型 | 可空 | 指定新按钮插入的位置索引，0 为栏目一，1 为栏目二，如此类推。如果提供 -1 ，则插入到工具条的尾部。如果本参数被省略，默认值为 -1  |
| 2 | `类型` | 整数型 | 可空 | 按钮的类型可以为以下常量值之一： 0: #普通按钮； 1: #多选按钮； 2: #单选按钮； 3: #下拉按钮； 4: #分隔条。 如果本参数被省略，默认值为“#普通按钮” |
| 3 | `图片索引` | 整数型 | 可空 | 图片索引用于指定图片组属性中的某张图片，该图片将被显示在按钮表面。索引值从 0 开始，-1 表示无图片。如果本参数被省略，默认值为 -1  |
| 4 | `标题` | 文本型 | 可空 | 本参数指定显示在按钮下的标题文本。如果被省略，默认值为空文本 |
| 5 | `动态提示文本` | 文本型 | 可空 | 本参数指定当鼠标移动到按钮上并停留一段时间后所动态显示的提示文本。如果被省略，默认值为空文本 |
| 6 | `按钮状态` | 整数型 | 可空 | 本参数指定按钮的存在状态。状态值为 0 或者以下常量值任意组合(相加)： 1: #选中； 2: #按下； 4: #禁止； 8: #隐藏； 16: #变灰。 其中“选中”状态在类型为多选按钮或单选按钮时有效，“按下”状态在类型为普通按钮时有效。如果本参数被省略，默认值为 0  |

### 删除按钮

> 英文名 `Remove`

删除指定的按钮

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `按钮索引` | 整数型 | - | 0 为按钮一，1 为按钮二，如此类推 |

### 取按钮数

> 英文名 `GetCount` ｜ 返回值 **整数型**

取回工具条中按钮的总数

### 取标题

> 英文名 `GetCaption` ｜ 返回值 **文本型**

取回指定按钮的标题文本

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `按钮索引` | 整数型 | - | 0 为按钮一，1 为按钮二，如此类推 |

### 取提示文本

> 英文名 `GetTipText` ｜ 返回值 **文本型**

取回指定按钮的提示文本

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `按钮索引` | 整数型 | - | 0 为按钮一，1 为按钮二，如此类推 |

### 取类型

> 英文名 `GetType` ｜ 返回值 **整数型**

取回指定按钮的类型，返回值为以下常量值之一： 0: #普通按钮； 1: #多选按钮； 2: #单选按钮； 3: #下拉按钮； 4: #分隔条

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `按钮索引` | 整数型 | - | 0 为按钮一，1 为按钮二，如此类推 |

### 取状态

> 英文名 `GetState` ｜ 返回值 **整数型**

取回指定按钮的状态，状态值为 0 或者以下常量值任意组合(相加)： 1: #选中； 2: #按下； 4: #禁止； 8: #隐藏； 16: #变灰。 可以用“位与”命令来判断指定的状态值是否被置位，如：“如果真 (位与 (工具条1.取状态 (0)，#禁止) = #禁止)”语句即可判断工具条1的第一个按钮是否已被禁止

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `按钮索引` | 整数型 | - | 0 为按钮一，1 为按钮二，如此类推 |

### 取图片

> 英文名 `GetPic` ｜ 返回值 **整数型**

取回指定按钮的图片索引，图片索引指向图片组属性中的某一图片，从 0 开始，-1 表示本按钮无图片标志

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `按钮索引` | 整数型 | - | 0 为按钮一，1 为按钮二，如此类推 |

### 置提示文本

> 英文名 `SetTipText`

设置指定按钮的提示文本

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `按钮索引` | 整数型 | - | 0 为按钮一，1 为按钮二，如此类推 |
| 2 | `动态提示文本` | 文本型 | 可空 | 本参数指定当鼠标移动到按钮上并停留一段时间后所动态显示的提示文本。如果被省略，默认值为空文本 |

### 加入状态

> 英文名 `AddState`

将指定状态值加入到按钮的存在状态中

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `按钮索引` | 整数型 | - | 0 为按钮一，1 为按钮二，如此类推 |
| 2 | `按钮状态` | 整数型 | 可空 | 本参数指定按钮的存在状态。状态值为 0 或者以下常量值任意组合(相加)： 1: #选中； 2: #按下； 4: #禁止； 8: #隐藏； 16: #变灰。 其中“选中”状态在类型为多选按钮或单选按钮时有效，“按下”状态在类型为普通按钮时有效。如果本参数被省略，默认值为 0  |

### 去除状态

> 英文名 `RemoveState`

从按钮的存在状态中去除指定状态值

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `按钮索引` | 整数型 | - | 0 为按钮一，1 为按钮二，如此类推 |
| 2 | `按钮状态` | 整数型 | 可空 | 本参数指定按钮的存在状态。状态值为 0 或者以下常量值任意组合(相加)： 1: #选中； 2: #按下； 4: #禁止； 8: #隐藏； 16: #变灰。 其中“选中”状态在类型为多选按钮或单选按钮时有效，“按下”状态在类型为普通按钮时有效。如果本参数被省略，默认值为 0  |

### 取标题

> 英文名 `GetCaption` ｜ 返回值 **文本型**

取指定表项或子项的标题

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `表项索引` | 整数型 | - | 用于指定列表框中的某一项目，索引值从 0 开始 |
| 2 | `列索引` | 整数型 | 可空 | 用作指定报表型列表框中项目所处的列，索引值从 0 开始，如果当前列表框类型不是报表型列表框，提供 0 值即可。如果本参数被省略，默认值为 0  |

### 取图片

> 英文名 `GetPic` ｜ 返回值 **整数型**

取回指定表项或子项的图片索引，图片索引指向图片组属性中的某一图片，从 0 开始，-1 表示无图片

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `表项索引` | 整数型 | - | 用于指定列表框中的某一项目，索引值从 0 开始 |
| 2 | `列索引` | 整数型 | 可空 | 用作指定报表型列表框中项目所处的列，索引值从 0 开始，如果当前列表框类型不是报表型列表框，提供 0 值即可。如果本参数被省略，默认值为 0  |

### 取当前状态图片

> 英文名 `GetStatePic` ｜ 返回值 **整数型**

取回指定表项的当前状态图片索引，图片索引指向状态图片组属性中的某一图片，从 0 开始，-1 表示无图片。如果“是否有检查框”属性为真，返回值是当前检查框的选中状态，1表示被选中，0表示未选中

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `表项索引` | 整数型 | - | 用于指定列表框中的某一项目，索引值从 0 开始 |

### 取缩进数目

> 英文名 `GetIndent` ｜ 返回值 **整数型**

取回指定表项的缩进数目

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `表项索引` | 整数型 | - | 用于指定列表框中的某一项目，索引值从 0 开始 |

### 取表项数值

> 英文名 `GetData` ｜ 返回值 **整数型**

返回与指定表项相关联的数值。如果该表项不存在，返回 0 

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `表项索引` | 整数型 | - | 用于指定列表框中的某一项目，索引值从 0 开始 |

### 置标题

> 英文名 `SetCaption`

设置指定表项或子项的标题

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `表项索引` | 整数型 | - | 用于指定列表框中的某一项目，索引值从 0 开始 |
| 2 | `列索引` | 整数型 | 可空 | 用作指定报表型列表框中项目所处的列，索引值从 0 开始，如果当前列表框类型不是报表型列表框，提供 0 值即可。如果本参数被省略，默认值为 0  |
| 3 | `标题` | 文本型 | 可空 | 本参数指定表项或子项的标题文本。如果被省略，默认值为空文本 |

### 置图片

> 英文名 `SetPic`

设置指定表项或子项的图片

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `表项索引` | 整数型 | - | 用于指定列表框中的某一项目，索引值从 0 开始 |
| 2 | `列索引` | 整数型 | 可空 | 用作指定报表型列表框中项目所处的列，索引值从 0 开始，如果当前列表框类型不是报表型列表框，提供 0 值即可。如果本参数被省略，默认值为 0  |
| 3 | `图片索引` | 整数型 | 可空 | 图片索引用于指定“图片组”属性中的某张图片。索引值从 0 开始，-1 表示无图片。如果本参数被省略，默认值为 -1  |

### 置状态图片

> 英文名 `SetStatePic`

设置指定表项的状态图片。如果“是否有检查框”属性为真，调用“置状态图片 (表项索引, 0)”或“置状态图片 (表项索引, 1)”可以设置表项检查框的选中状态（前者为不选中，后者为选中）

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `表项索引` | 整数型 | - | 用于指定列表框中的某一项目，索引值从 0 开始 |
| 2 | `状态图片索引` | 整数型 | 可空 | 状态图片索引用于指定“状态图片组”属性中的某张图片。索引值从 0 开始，最大为 14 ， -1 表示无图片。如果本参数被省略，默认值为 -1  |

### 置缩进数目

> 英文名 `SetIndent`

设置指定表项的缩进数目

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `表项索引` | 整数型 | - | 用于指定列表框中的某一项目，索引值从 0 开始 |
| 2 | `缩进数目` | 整数型 | 可空 | “缩进数目”仅在报表型列表框才有效，用作指定显示时在表项首部增加的空白数。如果本参数被省略，默认值为 0  |

### 置表项数值

> 英文名 `SetData`

设置与指定表项相关联的数值

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `表项索引` | 整数型 | - | 用于指定列表框中的某一项目，索引值从 0 开始 |
| 2 | `表项数值` | 整数型 | 可空 | 该数值与指定表项相关联。如果本参数被省略，默认值为 0  |

### 取被选择表项

> 英文名 `GetSelItems` ｜ 返回值 **整数型**

返回一个整数数组，內含所有当前被选择表项的位置索引。如果当前没有被选择表项，返回空数组

### 取已选择表项数

> 英文名 `GetSelCount` ｜ 返回值 **整数型**

返回当前已被选择表项的数目

### 是否被选择

> 英文名 `IsSelected` ｜ 返回值 **逻辑型**

判断指定表项是否已经被选择，如已被选择，返回真，否则返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `表项索引` | 整数型 | - | 用于指定列表框中的某一项目，索引值从 0 开始 |

### 选择表项

> 英文名 `Select`

选择指定表项

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `表项索引` | 整数型 | - | 用于指定列表框中的某一项目，索引值从 0 开始 |

### 取消表项选择

> 英文名 `UnSelect`

取消对指定表项的选择

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `表项索引` | 整数型 | - | 用于指定列表框中的某一项目，索引值从 0 开始 |

### 取表项数

> 英文名 `GetCount` ｜ 返回值 **整数型**

返回列表框中所有表项的数目

### 插入表项

> 英文名 `Insert` ｜ 返回值 **整数型**

在指定的位置插入新表项，成功返回新表项的位置索引，失败返回 -1 

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `插入位置` | 整数型 | 可空 | 指定新表项插入时的位置索引，索引值从 0 开始。如果提供 -1 ，则插入到列表框的尾部。如果本参数被省略，默认值为 -1。注意，“类型”属性为“大图标列表框”或“小图标列表框”的情况下，将始终在所有表项的最后插入新的表项。 |
| 2 | `标题` | 文本型 | 可空 | 本参数指定表项的标题文本。如果被省略，默认值为空文本 |
| 3 | `图片索引` | 整数型 | 可空 | 图片索引用于指定“图片组”属性中的某张图片。索引值从 0 开始，-1 表示无图片。如果本参数被省略，默认值为 -1  |
| 4 | `状态图片索引` | 整数型 | 可空 | 状态图片索引用于指定“状态图片组”属性中的某张图片。索引值从 0 开始，最大为 14 ， -1 表示无图片。如果本参数被省略，默认值为 -1  |
| 5 | `缩进数目` | 整数型 | 可空 | “缩进数目”仅在报表型列表框才有效，用作指定显示时在表项首部增加的空白数。如果本参数被省略，默认值为 0  |
| 6 | `表项数值` | 整数型 | 可空 | 该数值与指定表项相关联。如果本参数被省略，默认值为 0  |

### 删除表项

> 英文名 `Remove`

删除指定位置的表项

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `表项索引` | 整数型 | - | 用于指定列表框中的某一项目，索引值从 0 开始 |

### 全部删除

> 英文名 `RemoveAll`

删除所有的表项

### 查找表项

> 英文名 `Find` ｜ 返回值 **整数型**

查找具有指定标题的表项，成功返回被找到表项的位置索引，失败返回 -1 

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `标题` | 文本型 | - | 本参数指定欲查找表项的标题文本 |
| 2 | `起始查找位置` | 整数型 | 可空 | 本参数指定查找表项的起始位置。如果本参数被省略，默认值为 0 ，即从首项开始查找 |
| 3 | `是否精确匹配` | 逻辑型 | 可空 | 本参数指定匹配标题文本时是否为精确匹配，如果本参数为假，则只要表项标题首部包含指定文本即认为匹配成功。如果本参数被省略，默认值为真 |
| 4 | `列索引` | 整数型 | 可空 | 本参数仅在报表模式下有效，用于指定查找表项中的哪一列。如为 -1，表示查找所有列。如果本参数被省略，默认值为 0 ，即查找第一列 |

### 查找表项数值

> 英文名 `FindData` ｜ 返回值 **整数型**

查找具有指定关联数值的表项，成功返回被找到表项的位置索引，失败返回 -1 

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `起始查找位置` | 整数型 | 可空 | 本参数指定查找表项的起始位置。如果本参数被省略，默认值为 0  ，即从首项开始查找 |
| 2 | `表项数值` | 整数型 | 可空 | 该数值与指定表项相关联。如果本参数被省略，默认值为 0  |

### 保证显示

> 英文名 `EnsureVisible`

确保指定表项能够显示出来，如果不能显示，则自动滚动并显示该表项

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `表项索引` | 整数型 | - | 用于指定列表框中的某一项目，索引值从 0 开始 |

### 排列图标

> 英文名 `Arrange`

自动排列大图标或小图标列表框中的图标

### 取列标题

> 英文名 `GetColCaption` ｜ 返回值 **文本型**

返回报表型列表框中指定列的标题

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `列索引` | 整数型 | - | 用作指定报表型列表框中项目所处的列，索引值从 0 开始 |

### 取列对齐方式

> 英文名 `GetAlignType` ｜ 返回值 **整数型**

取回报表型列表框中指定列的对齐方式。返回值为以下常量之一： 0：#左对齐； 1：#中间对齐； 2：#右对齐

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `列索引` | 整数型 | - | 用作指定报表型列表框中项目所处的列，索引值从 0 开始 |

### 取列宽

> 英文名 `GetColWidth` ｜ 返回值 **整数型**

返回报表型列表框中指定列的宽度，如指定列不存在，返回 0 

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `列索引` | 整数型 | - | 用作指定报表型列表框中项目所处的列，索引值从 0 开始 |

### 取列图片

> 英文名 `GetColPic` ｜ 返回值 **整数型**

返回报表型列表框中指定列的图片索引

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `列索引` | 整数型 | - | 用作指定报表型列表框中项目所处的列，索引值从 0 开始 |

### 图片是否居右

> 英文名 `GetColPicPos` ｜ 返回值 **逻辑型**

如果报表型列表框中指定列的图片居右显示，返回真，否则返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `列索引` | 整数型 | - | 用作指定报表型列表框中项目所处的列，索引值从 0 开始 |

### 置列标题

> 英文名 `SetColCaption`

设置报表型列表框中指定列的标题

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `列索引` | 整数型 | - | 用作指定报表型列表框中项目所处的列，索引值从 0 开始 |
| 2 | `标题` | 文本型 | 可空 | 本参数指定报表列的标题文本。如果被省略，默认值为空文本 |

### 置列宽

> 英文名 `SetColWdith`

设置报表型列表框中指定列的宽度

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `列索引` | 整数型 | - | 用作指定报表型列表框中项目所处的列，索引值从 0 开始 |
| 2 | `列宽` | 整数型 | 可空 | 本参数指定报表列的宽度，单位为像素。如果参数值提供为 -1 ，则根据列数据最大宽度自动调整列尺寸，如果参数值提供为 -2 ，则根据列标题宽度自动调整列尺寸。如果本参数被省略，默认值为 -1  |

### 置列图片

> 英文名 `SetColPic`

设置报表型列表框中指定列的标志图片

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `列索引` | 整数型 | - | 用作指定报表型列表框中项目所处的列，索引值从 0 开始 |
| 2 | `图片索引` | 整数型 | 可空 | 图片索引用于指定“表头图片组”属性中的某张图片。索引值从 0 开始，-1 表示无图片。如果本参数被省略，默认值为 -1  |

### 取列数

> 英文名 `GetColCount` ｜ 返回值 **整数型**

返回报表型列表框中列的总数

### 插入列

> 英文名 `InsertCol` ｜ 返回值 **整数型**

在报表型列表框中指定位置插入新的列，成功返回新列的位置索引，失败返回 -1 

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `插入位置` | 整数型 | 可空 | 指定新列插入时的位置索引，索引值从 0 开始。如果提供 -1 ，则插入到尾部。如果本参数被省略，默认值为 -1  |
| 2 | `标题` | 文本型 | 可空 | 本参数指定报表列的标题文本。如果被省略，默认值为空文本 |
| 3 | `列宽` | 整数型 | 可空 | 本参数指定报表列的宽度，单位为像素。如果被省略，默认值为 50  |
| 4 | `对齐方式` | 整数型 | 可空 | 参数可以为以下常量之一： 0：#左对齐； 1：#中间对齐； 2：#右对齐。如果本参数被省略，默认值为“#左对齐” |
| 5 | `图片索引` | 整数型 | 可空 | 图片索引用于指定“表头图片组”属性中的某张图片。索引值从 0 开始，-1 表示无图片。如果本参数被省略，默认值为 -1  |
| 6 | `图片是否居右` | 逻辑型 | 可空 | 指定图片是否显示在右边，如果本参数被省略，默认值为假 |

### 删除列

> 英文名 `RemoveCol`

删除报表型列表框中指定位置处的列

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `列索引` | 整数型 | - | 用作指定报表型列表框中项目所处的列，索引值从 0 开始 |

### 进入编辑

> 英文名 `Edit`

让指定表项进入编辑状态，执行本命令成功的前提是“允许编辑”属性为真

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `表项索引` | 整数型 | - | 用于指定列表框中的某一项目，索引值从 0 开始 |

### 取父项目

> 英文名 `GetParent` ｜ 返回值 **整数型**

返回指定项目的父项目索引。如果该项目为顶层项目，将返回 -1 

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `项目索引` | 整数型 | - | 0 为项目一，1 为项目二，如此类推 |

### 是否有子项目

> 英文名 `IsHasChild` ｜ 返回值 **逻辑型**

如果指定项目存在子项目，返回真，否则返回假。如果本命令返回真，意味着指定项目的下一项即为此项目的第一个子项目

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `项目索引` | 整数型 | - | 0 为项目一，1 为项目二，如此类推 |

### 是否已扩展

> 英文名 `IsExpanded` ｜ 返回值 **逻辑型**

如果树型框中的指定项目具有子项目并已扩展显示，返回真，否则返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `项目索引` | 整数型 | - | 0 为项目一，1 为项目二，如此类推 |

### 置标题

> 英文名 `SetCaption`

设置指定按钮的标题

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `按钮索引` | 整数型 | - | 0 为按钮一，1 为按钮二，如此类推 |
| 2 | `按钮标题` | 文本型 | 可空 | 如果被省略，默认值为空文本 |

### 是否已加粗

> 英文名 `IsBold` ｜ 返回值 **逻辑型**

如果树型框中的指定项目的标题被加粗显示，返回真，否则返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `项目索引` | 整数型 | - | 0 为项目一，1 为项目二，如此类推 |

### 取检查框状态

> 英文名 `GetStateImageIndex` ｜ 返回值 **整数型**

返回值表示检查框中选中状态。默认情况下，0：未选中，1：选中，2：半选中。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `项目索引` | 整数型 | - | 0 为项目一，1 为项目二，如此类推 |

### 置检查框状态

> 英文名 `SetStateImageIndex`

设置项目的检查框状态

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `项目索引` | 整数型 | - | 0 为项目一，1 为项目二，如此类推。在参数“连同子项目”为真的情况下，此参数可为-1，表示设置所有项目的检查框状态。 |
| 2 | `检查框状态` | 整数型 | - | 此参数对应“检查框图片组”中某个图片的索引，从0开始。默认情况下，0表示未选中，1表示已选中，2表示半选中。 |
| 3 | `连同子项目` | 逻辑型 | 可空 | 如果此参数为真，则参数“项目索引”所指定项目的所有子项目也被一并设置其检查框状态。默认为假。 |


## 常量

| 名称 | 值 | 说明 |
|---|---|---|
| `凹入` |  |  |
| `无边框` |  |  |
| `凸出` |  |  |
| `普通按钮` |  |  |
| `多选按钮` |  |  |
| `单选按钮` |  |  |
| `下拉按钮` |  |  |
| `分隔条` |  |  |
| `选中` |  |  |
| `按下` |  |  |
| `禁止` |  |  |
| `隐藏` |  |  |
| `变灰` |  |  |
| `左对齐` |  |  |
| `中间对齐` |  |  |
| `右对齐` |  |  |
