# 应用接口支持库

- **库标识**: `eAPI`
- **版本**: 3.1
- **GUID**: `F7FC1AE45C5C4758AF03EF19F18A395D`
- **作者**: 大连大有吴涛易语言软件开发有限公司
- **主页**: http://www.eyuyan.com
- **文件**: `eAPI`
- **可用命令数**: 89

本支持库封装了常用的Windows API功能。

## 数据类型

| 名称 | 说明 |
|---|---|
| `接口常量` | 提供应用接口支持库中所要使用的常量枚举值 |
| `矩形数据` |  |
| `CPU信息` | CPU详细信息。 |
| `系统信息` | System详细信息。 |
| `文件版本信息` | 文件版本详细信息。 |
| `BIOS信息` | BIOS详细信息。 |
| `硬盘信息` | 硬盘详细信息。成员数据为空文本或0，表示该成员信息不可知或未被正确获取。 |
| `网卡信息` | 网卡详细信息。 |
| `进程信息` | 进程详细信息。 |
| `显示模式信息` | 显示模式详细信息。 |
| `打印信息` | 打印详细信息。 |

## 命令

### 取键盘指示灯状态

> 英文名 `GetKeyboardLockState` ｜ 返回值 **逻辑型**

取得三个键盘提示灯当前状态。开返回真；关返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `指示灯类别` | 整数型 | 可空 | 键盘指示灯类别，为以下常量值之一：0、#接口常量.数字锁定键盘灯； 1、#接口常量.大小写锁定键盘灯； 2、#接口常量.滚动锁定键盘灯。 |

### 模拟按键

> 英文名 `SimulateKey`

模拟键盘的输入。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `虚拟键值1` | 通用型 | - |  |
| 2 | `虚拟键值2` | 通用型 | 可空 |  |
| 3 | `虚拟键值3` | 通用型 | 可空 |  |

### 模拟鼠标点击

> 英文名 `SimulateMouse`

模拟鼠标的点击。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `X坐标` | 整数型 | - |  |
| 2 | `Y坐标` | 整数型 | - |  |
| 3 | `点击类型` | 整数型 | 可空 | 模拟鼠标点击的类型，可以为以下常量值之一：0、#接口常量.左键单击；1、#接口常量.左键双击；2、#接口常量.右键单击；3、#接口常量.右键双击。 |

### 取硬盘信息

> 英文名 `GetHDInfo` ｜ 返回值 **逻辑型**

取硬件出厂信息。Win95/98/Me中可能无效。特殊情况下也许只能获取部分硬盘信息，其它未被成功获取的部分，将被设置为空文本或0。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `硬盘信息` | 网卡信息 | - | 待取得的硬盘信息。 |
| 2 | `硬盘序号` | 整数型 | 可空 | 待取得的硬盘信息的硬盘序号。可以为以下常量之一：0、#接口常量.磁盘0；1、#接口常量.磁盘1；2、#接口常量.磁盘2；3、#接口常量.磁盘3。 |

### 取驱动器数量

> 英文名 `GetDrivesNum` ｜ 返回值 **整数型**

取当前指定类型的驱动器数量。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `驱动器类型` | 整数型 | 可空 | 驱动器的类型，可以为以下常量值之一：0、#接口常量.所有驱动器；1、#接口常量.硬盘驱动器；2、#接口常量.光盘驱动器；3、#接口常量.可移动驱动器；4、#接口常量.网络驱动器。 |

### 取驱动器列表

> 英文名 `GetDrivesList` ｜ 返回值 **文本型**

取当前指定类型的驱动器列表。成功返回驱动器列表数组；失败返回空。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `驱动器类型` | 整数型 | 可空 | 驱动器的类型，可以为以下常量值之一：0、#接口常量.所有驱动器；1、#接口常量.硬盘驱动器；2、#接口常量.光盘驱动器；3、#接口常量.可移动驱动器；4、#接口常量.网络驱动器。 |

### 弹出光驱

> 英文名 `PopupCdrom`

弹出光驱门。

### 关闭光驱

> 英文名 `CloseCdrom`

关闭光驱门。

### 取光驱盘符

> 英文名 `GetCdrom` ｜ 返回值 **文本型**

取光驱盘符。失败返回空

### 光驱中是否有盘

> 英文名 `IsDiskInside` ｜ 返回值 **逻辑型**

光驱中是否有盘。有盘返回真；否则返回假。

### 取系统进程列表

> 英文名 `GetProcessList` ｜ 返回值 **显示模式信息**

取所有正在运行的程序列表。成功返回程序列表数组；失败返回空。

### 终止进程

> 英文名 `KillProcess` ｜ 返回值 **逻辑型**

终止指定进程。成功返回真，失败返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `进程标识` | 通用型 | - | 要终止进程的进程ID、进程名称或窗口标题。 |

### 取正在使用DLL列表

> 英文名 `GetDllList` ｜ 返回值 **文本型**

取所有正在使用的DLL列表。成功返回DLL列表数组；失败返回空。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `进程标识符` | 整数型 | 可空 | 要取得正在使用DLL列表的进程标识符。置-1则取所有正在使用DLL列表。 |

### 取没有响应程序列表

> 英文名 `GetHungProgramList` ｜ 返回值 **文本型**

取没有响应程序列表。成功返回程序标题数组，失败返回空。

### 取系统信息

> 英文名 `GetSystemInfo`

取系统信息。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `系统信息` | 文件版本信息 | - | 待取得的系统信息。 |

### 取BIOS信息

> 英文名 `GetBiosInfo`

取BIOS信息。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `BIOS信息` | 硬盘信息 | - | 待取得的BIOS信息。 |

### 取文件版本信息

> 英文名 `GetFileVersionInfo` ｜ 返回值 **逻辑型**

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `文件名` | 文本型 | - | 要取得版本信息的文件名。 |
| 2 | `文件版本信息` | BIOS信息 | - | 待取得的文件版本信息。 |

### 取CPU信息

> 英文名 `GetCpuInfo`

取CPU信息。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `CPU信息` | 系统信息 | - | 待取得的CPU信息。 |

### 取CPU占用率

> 英文名 `GetCpuUsges` ｜ 返回值 **整数型**

取当前CPU占用率。成功返回CPU占用率；失败返回-1。Win98系统下使用本方法取得的数值可能会有误。

### 取内存容量信息

> 英文名 `GetMemoryInfo` ｜ 返回值 **整数型**

取内存容量信息，单位为M（兆）。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `内存类型` | 整数型 | 可空 | 要取得的内存类型，可以为以下常量值之一：0、#接口常量.总物理内存；1、#接口常量.可用物理内存；2、#接口常量.总虚拟内存；3、#接口常量.可用虚拟内存；4、#接口常量.总页面文件；5、#接口常量.可用页面文件。 |

### 取声卡名称

> 英文名 `GetAudioCard` ｜ 返回值 **文本型**

取系统声卡名称，失败返回空。

### 打开监视器

> 英文名 `OpenMonitor`

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `窗口句柄` | 整数型 | - |  |

### 关闭监视器

> 英文名 `CloseMonitor`

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `窗口句柄` | 整数型 | - |  |

### 添加右键菜单

> 英文名 `AddRightMenu` ｜ 返回值 **逻辑型**

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `扩展名` | 文本型 | - |  |
| 2 | `右键菜单标题` | 文本型 | - | 显示在右键菜单上的标题。 |
| 3 | `命令行` | 文本型 | - |  |

### 删除右键菜单

> 英文名 `DeleteRightMenu` ｜ 返回值 **逻辑型**

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `扩展名` | 文本型 | - |  |
| 2 | `右键菜单标题` | 文本型 | - | 显示在右键菜单上的标题。 |

### 设置自动运行

> 英文名 `SetAutoRun` ｜ 返回值 **逻辑型**

设置程序在开机时自动运行。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `程序名称` | 文本型 | - | 程序名称。 |
| 2 | `程序文件名` | 文本型 | - | 要设置为自动运行的程序文件名（包含绝对路径）。 |
| 3 | `位置` | 整数型 | 可空 | 为以下常量值之一：0、#接口常量.到注册表；1、#接口常量.到启动组。 |

### 删除临时文件

> 英文名 `DeleteTmpFile` ｜ 返回值 **逻辑型**

删除指定位置的临时文件。成功返回真；失败返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `临时文件类型` | 整数型 | 可空 | 临时文件类型，可以为以下常量值之一：0、#接口常量.系统临时文件；1、#接口常量.IE临时文件；2、#接口常量.回收站；3、#接口常量.存根文件。 |

### 清除历史记录

> 英文名 `ClearHistory` ｜ 返回值 **逻辑型**

清除指定类型的历史记录。成功返回真；失败返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `历史记录类型` | 整数型 | 可空 | 历史记录类型，可以为以下常量值之一：0、#接口常量.浏览器历史地址记录；1、#接口常量.表单自动完成历史记录；2、#接口常量.自动密码历史记录；3、#接口常量.收藏夹；4、#接口常量.自动拨号历史记录；5、#接口常量.运行中的自动匹配历史记录；6、#接口常量.打开文档历史记录；7、#接口常量.网络连接历史记录；8、#接口常量.远程登录历史记录；9、#接口常量.浏览网址历史记录。 |

### 取系统字体列表

> 英文名 `GetFontList` ｜ 返回值 **文本型**

取系统所有字体的列表。成功返回字体数组；失败返回空。

### 安装字体

> 英文名 `AddFont` ｜ 返回值 **整数型**

在Windows系统中添加一种字体资源。添加完毕后，该字体即可由任何Windows应用程序调用。成功返回添加的字体数量；失败返回0。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字体文件名` | 文本型 | - | 字体资源文件的文件名。可以是.FON，.FNT，.TTF或.FOT文件。 |

### 删除字体

> 英文名 `RemoveFont` ｜ 返回值 **逻辑型**

从Windows系统中删除一种字体资源。如删除的字体目前正由其他应用程序使用，则并不将其立即删除。成功返回真；失败返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `字体文件名` | 文本型 | - | 字体资源文件的文件名。可以是.FON，.FNT，.TTF或.FOT文件。 |

### 取图片宽度

> 英文名 `GetImageWidth` ｜ 返回值 **整数型**

取得指定图片的宽度。成功返回图片宽度；失败返回0。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `图片文件名` | 文本型 | - | 支持以下几种图片格式：BMP、JPG、GIF、ICO。 |

### 取图片高度

> 英文名 `GetImageHeight` ｜ 返回值 **整数型**

取得指定图片的高度。成功返回图片高度；失败返回0。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `图片文件名` | 文本型 | - | 支持以下几种图片格式：BMP、JPG、GIF、ICO。 |

### 提取资源文件图标

> 英文名 `GetIconFromResource` ｜ 返回值 **整数型**

从指定的资源文件中提取图标，并保存为ICO、BMP文件或复制到剪贴板。成功返回图标数量；失败返回0。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `资源文件名` | 文本型 | - | 要提取图标的资源文件名。扩展名为EXE、DLL、ICL、ICO。 |
| 2 | `提取到` | 整数型 | 可空 | 要提取图标到何处，可以为以下常量值之一：0、#接口常量.到文件； 1、#接口常量.到剪贴板。 |
| 3 | `保存文件名` | 文本型 | 可空 | 提取的图标要保存的文件名。扩展名为ICO、BMP。 |
| 4 | `图标序号` | 整数型 | 可空 | 当资源文件中包含多个图标时，表示要提取图标的序号。 |

### 取IE版本号

> 英文名 `GetIEVersion` ｜ 返回值 **文本型**

取得IE的版本号。成功返回版本号；失败返回空。

### 添加IE工具条按钮

> 英文名 `AddButtonToIE` ｜ 返回值 **逻辑型**

向IE工具栏添加自己的按钮。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `按钮文字` | 文本型 | - |  |
| 2 | `图标文件名` | 文本型 | - |  |
| 3 | `执行动作` | 文本型 | - | 在IE中按下按钮后执行一个可执行文件或打开网页。 |

### 删除IE工具条按钮

> 英文名 `DeleteButtonFromIE` ｜ 返回值 **逻辑型**

删除指定的IE工具栏按钮。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `按钮文字` | 文本型 | - |  |

### 创建程序组

> 英文名 `CreateProgramGroup` ｜ 返回值 **逻辑型**

在指定的位置建立程序组。成功返回真，失败返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `程序组名称` | 文本型 | - | 要创建的程序组名称。 |
| 2 | `位置` | 整数型 | 可空 | 程序组的起始位置。可以为以下常量值之一：0、#接口常量.开始菜单；1、#接口常量.开始菜单程序；2、#接口常量.桌面。 |

### 删除程序组

> 英文名 `DeleteProgramGroup` ｜ 返回值 **逻辑型**

在指定的位置删除指定的程序组。成功返回真，失败返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `程序组名称` | 文本型 | - | 要删除的程序组名称。 |
| 2 | `位置` | 整数型 | 可空 | 程序组的起始位置。可以为以下常量值之一：0、#接口常量.开始菜单；1、#接口常量.开始菜单程序；2、#接口常量.桌面。 |

### 创建程序项

> 英文名 `CreateProgramItem` ｜ 返回值 **逻辑型**

在指定的程序组中创建程序项。成功返回真，失败返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `程序组名称` | 文本型 | - | 程序项所在程序组的名称。 |
| 2 | `程序项名称` | 文本型 | - | 要创建的程序项名称。 |
| 3 | `程序文件名` | 文本型 | - | 执行程序文件名。 |
| 4 | `位置` | 整数型 | 可空 | 程序项的起始位置。可以为以下常量值之一：0、#接口常量.开始菜单；1、#接口常量.开始菜单程序；2、#接口常量.桌面。 |

### 删除程序项

> 英文名 `DeleteProgramItem` ｜ 返回值 **逻辑型**

在指定的程序组中删除指定的程序项。成功返回真，失败返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `程序组名称` | 文本型 | - | 程序项所在程序组的名称。 |
| 2 | `程序项名称` | 文本型 | - | 要删除的程序项名称。 |
| 3 | `位置` | 整数型 | 可空 | 程序项的起始位置。可以为以下常量值之一：0、#接口常量.开始菜单；1、#接口常量.开始菜单程序；2、#接口常量.桌面。 |

### 取快捷方式目标

> 英文名 `GetShortCutTarget` ｜ 返回值 **文本型**

获取指定快捷方式（.lnk）的目标和参数。成功返回目标，失败返回空。只有第一个参数为输入参数，后面的其它参数均为输出参数（可省略）。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `快捷方式文件名` | 文本型 | - |  |
| 2 | `目标` | 文本型 | 可空、参考 |  |
| 3 | `参数` | 文本型 | 可空、参考 |  |
| 4 | `启始位置` | 文本型 | 可空、参考 |  |
| 5 | `图标` | 文本型 | 可空、参考 | 返回的文本格式为：图标所属文件 + , + 图标序号（0为第一个），例如：“c:\x.exe,1”。 |
| 6 | `运行方式` | 整数型 | 可空、参考 | 1：常规窗口；2：最小化；3：最大化 |
| 7 | `快捷键` | 通用型 | 可空、参考 | 可以传入文本型或整数型型变量。如果为文本型，返回快捷键文本形式，如“Ctrl + Shift + Alt + A”。如果为整数型，返回一个表示该快捷键的数值（参见http://msdn.microsoft.com/en-us/library/bb774926(VS.85).aspx）。 |
| 8 | `备注` | 文本型 | 可空、参考 |  |

### 取网卡信息列表

> 英文名 `GetApapterList` ｜ 返回值 **进程信息**

取得本地计算机的网卡信息列表。失败返回空。

### 取本机网卡名

> 英文名 `GetLocalAdapterName` ｜ 返回值 **文本型**

取得本地计算机指定序号的网卡名称。失败返回空。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `网卡序号` | 整数型 | 可空 | 当本机有多块网卡时，指定要对其操作的网卡序号（从1开始）。 |

### 取本机网卡物理地址

> 英文名 `GetLocalMac` ｜ 返回值 **文本型**

取得本地计算机指定序号的网卡物理地址。失败返回空。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `网卡序号` | 整数型 | 可空 | 当本机有多块网卡时，指定要对其操作的网卡序号（从1开始）。 |

### 取远程网卡物理地址

> 英文名 `GetRemoteMac` ｜ 返回值 **文本型**

根据指定的IP地址取远程机器网卡的物理地址。失败返回空。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `IP地址` | 文本型 | - |  |

### 取远程机器名

> 英文名 `GetRemoteName` ｜ 返回值 **文本型**

根据指定的IP地址取远程机器名称。失败返回空。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `IP地址` | 文本型 | - |  |

### 取IP地址

> 英文名 `GetIpFromHostName` ｜ 返回值 **文本型**

根据指定的机器名取IP地址。失败返回空。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `机器名` | 文本型 | 可空 | 要取得IP地址的机器名。 |

### 撰写邮件

> 英文名 `RunEmailAddr`

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `Email地址` | 文本型 | - |  |

### 取网络类型列表

> 英文名 `GetNetList` ｜ 返回值 **文本型**

取所有的网络类型。成功返回程序列表数组；失败返回空。

### 取网络工作组列表

> 英文名 `GetGroupList` ｜ 返回值 **文本型**

取局域网中指定网络类型的工作组列表。成功返回列表数组；失败返回空。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `网络类型` | 文本型 | 可空 | 网络类型名，为空则取所有工作组。 |

### 取网络计算机列表

> 英文名 `GetComputerList` ｜ 返回值 **文本型**

取局域网中指定工作组的计算机列表。成功返回列表数组；失败返回空。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `工作组` | 文本型 | 可空 | 工作组名称，为空则取所有计算机。 |

### 是否与互联网连接

> 英文名 `IsConnectToInternet` ｜ 返回值 **逻辑型**

检测当前网络状态是否正在与外部互联网连接。成功返回真；失败返回假。如果本命令返回真，当前必然可以访问外部互联网。

### 端口检测

> 英文名 `CheckPort` ｜ 返回值 **逻辑型**

检测指定的端口是否开放。开放返回真；否则返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `端口` | 整数型 | - | 检测的端口。 |
| 2 | `IP地址` | 文本型 | 可空 | 置空则检测本机端口。 |

### 打开特殊系统窗口

> 英文名 `OpenSysWindow`

打开特殊的系统窗口，如控制面板。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `窗口类型` | 整数型 | - | 系统窗口类型，为以下常量值之一：0、#接口常量.控制面板；1、#接口常量.系统属性；2、#接口常量.显示属性；3、#接口常量.辅助功能选项；4、#接口常量.添加打印机向导；5、#接口常量.添加或删除程序；6、#接口常量.日期和时间属性；7、#接口常量.字体文件夹；8、#接口常量.游戏控制器；9、#接口常量.鼠标属性；10、#接口常量.键盘属性；11、#接口常量.声音和音频属性；12、#接口常量.ODBC数据源管理器；13、#接口常量.电源选项属性；14、#接口常量.区域和语言选项；15、#接口常量.互联网属性；16、#接口常量.用户帐号；17、#接口常量.系统信息对话框。 |
| 2 | `属性页序号` | 整数型 | 可空 | 当有多个属性页时，表示要打开属性页的序号（从0开始）。 |

### 打开指定网址

> 英文名 `OpenURL`

用系统默认浏览器打开指定的网址。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `网址` | 文本型 | 可空 |  |

### 隐藏桌面图标

> 英文名 `HideDesktopIcon`

### 显示桌面图标

> 英文名 `ShowDesktopIcon`

### 隐藏任务栏

> 英文名 `HideTaskBar`

### 显示任务栏

> 英文名 `ShowTaskBar`

### 隐藏系统时钟

> 英文名 `HideClock`

### 显示系统时钟

> 英文名 `ShowClock`

### 隐藏开始按钮

> 英文名 `HideStartButton`

### 显示开始按钮

> 英文名 `ShowStartButton`

### 设置桌面墙纸

> 英文名 `SetDeskWallPaper` ｜ 返回值 **逻辑型**

成功返回真；失败返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `文件名` | 文本型 | - | 墙纸文件名（bmp格式）。将文件名置为空可以取消墙纸。 |
| 2 | `样式` | 整数型 | 可空 | 墙纸样式，可以为以下常量值之一：0、#接口常量.居中；1、#接口常量.平铺；2、#接口常量.拉伸。 |

### 设置窗口透明度

> 英文名 `SetDiaphaneity`

该命令在Windows 95/98下无效。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `窗口句柄` | 整数型 | - | 要设置透明度的窗口句柄。 |
| 2 | `透明度` | 整数型 | 可空 | 为0-255之间的整数。0 - 整个窗口透明, 255 - 完全不透明 |

### 取显示模式列表

> 英文名 `GetVideoList` ｜ 返回值 **打印信息**

取系统支持的显示模式列表(Win95/98/Me下无法取得刷新频率)。成功返回显示模式信息数组，失败返回空。

### 取当前显示模式

> 英文名 `GetCurVideo` ｜ 返回值 **打印信息**

取系统当前的显示模式(Win95/98/Me下无法取得刷新频率)。

### 设置屏幕分辨率

> 英文名 `SetResolveRatio` ｜ 返回值 **逻辑型**

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `宽度` | 整数型 | - | 屏幕显示区域的宽度，单位为像素点。 |
| 2 | `高度` | 整数型 | - | 屏幕显示区域的高度，单位为像素点。 |
| 3 | `位数` | 整数型 | 可空 | 每像素的位数。 |
| 4 | `刷新频率` | 整数型 | 可空 | 屏幕的刷新频率。 |

### 屏幕单位转换

> 英文名 `ChangeUnit` ｜ 返回值 **双精度小数型**

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `源单位值` | 双精度小数型 | - | 要转换的源单位值。 |
| 2 | `源单位` | 整数型 | 可空 | 单位类型，为以下常量值之一：0、#接口常量.像素；1、#接口常量.英寸；2、#接口常量.毫米；3、#接口常量.厘米。 |
| 3 | `目的单位` | 整数型 | 可空 | 单位类型，为以下常量值之一：0、#接口常量.像素；1、#接口常量.英寸；2、#接口常量.毫米；3、#接口常量.厘米。 |

### 取当前鼠标处颜色值

> 英文名 `GetPointRGB` ｜ 返回值 **逻辑型**

取当前鼠标所在位置的RGB值。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `R值` | 整数型 | 参考 | 要取得的R值。 |
| 2 | `G值` | 整数型 | 参考 | 要取得的G值。 |
| 3 | `B值` | 整数型 | 参考 | 要取得的B值。 |

### 捕获鼠标

> 英文名 `SetCapture` ｜ 返回值 **整数型**

将鼠标捕获设置到指定的窗口。在鼠标按钮按下的时候，这个窗口会为当前应用程序或整个系统接收所有鼠标输入。返回值为之前拥有鼠标捕获的窗口的句柄，如没有这样的窗口则返回空。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `窗口句柄` | 整数型 | - | 要接收所有鼠标输入的窗口的句柄。 |

### 释放鼠标

> 英文名 `ReleaseCapture` ｜ 返回值 **逻辑型**

为当前的应用程序释放鼠标捕获。成功返回真、失败返回假。

### 截取屏幕区域

> 英文名 `GetScreenBitmap` ｜ 返回值 **通用型**

将选定的屏幕区域拷贝到位图文件、剪贴板或字节集中。如拷贝到位图文件或剪贴板，则成功返回真；失败返回假。如拷贝到字节集，则成功返回位图字节集；失败返回空。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `屏幕区域` | CPU信息 | 可空 | 要截取的屏幕区域，为空则截取整个屏幕。 |
| 2 | `截取到` | 整数型 | 可空 | 要截取屏幕区域到何处，可以为以下常量值之一：0、#接口常量.到文件； 1、#接口常量.到剪贴板； 2、#接口常量.到字节集。 |
| 3 | `位图文件名` | 文本型 | 可空 |  |

### 取所有窗口列表

> 英文名 `GetAllWindowsList` ｜ 返回值 **文本型**

取当前打开的所有窗口列表。成功返回窗口句柄数组，失败返回空。

### 取窗口标题

> 英文名 `GetWindowText` ｜ 返回值 **文本型**

取指定窗口的标题。成功返回窗口标题，失败返回空。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `窗口句柄` | 整数型 | - |  |

### 取窗口类名

> 英文名 `GetClassName` ｜ 返回值 **文本型**

取指定窗口的类名。成功返回窗口类名，失败返回空。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `窗口句柄` | 整数型 | - |  |

### 取鼠标所在窗口句柄

> 英文名 `GetHwndFromPoint` ｜ 返回值 **整数型**

取鼠标所在窗口句柄。

### 映射网络驱动器

> 英文名 `NetAddConnection` ｜ 返回值 **逻辑型**

将指定的网络资源映射到本地驱动器。成功返回真；失败返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `网络资源路径` | 文本型 | - | 要映射到本地的网络资源路径。 |
| 2 | `驱动器` | 文本型 | - | 映射到本地的驱动器名。 |

### 发送局域网消息

> 英文名 `NetSendMessage` ｜ 返回值 **逻辑型**

发送消息到指定的机器（即Windows信使服务，只有启动了Messenger服务的机器才能收到该命令发送的消息）。成功返回真；失败返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `发送目标` | 文本型 | 可空 | 将要接收消息的目标机器，可以为机器名、IP地址、工作组名（具体由目标类型决定） |
| 2 | `消息` | 文本型 | - | 将要发送的消息文本。 |
| 3 | `目标类型` | 整数型 | 可空 | 指定发送目标的类型。为以下常量值之一：0、#接口常量.计算机；1、#接口常量.工作组；2、#接口常量.广播。 |

### 取网络共享资源列表

> 英文名 `GetShareResourceList` ｜ 返回值 **文本型**

取局域网中指定计算机的共享资源列表。成功返回列表数组；失败返回空。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `计算机` | 文本型 | 可空 | 计算机名称，为空则取所有计算机的共享资源。 |
| 2 | `共享资源类型` | 整数型 | 可空 | 共享资源类型，可以为以下常量值之一：0、#接口常量.所有共享资源；1、#接口常量.共享目录或文件；2、#接口常量.共享打印机。 |

### 取消网络驱动器映射

> 英文名 `CancelNetConnection` ｜ 返回值 **逻辑型**

取消指定的网络驱动器映射。成功返回真；失败返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `驱动器` | 文本型 | - | 映射到本地的驱动器名。 |

### 取消自动运行

> 英文名 `CancelAutoRun` ｜ 返回值 **逻辑型**

取消程序的自动运行。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `程序名称` | 文本型 | - | 程序名称。 |

### 格式化文本

> 英文名 `sprintf` ｜ 返回值 **文本型**

将一个格式化的字符串输出到一个目的字符串中。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `格式` | 文本型 | - | 转换的格式，以百分比符号 % 开始到转换字符为止。而在转换的格式间依序包括了1. 对齐方式。默认值为向右对齐，负号表向左对齐。2. 字段宽度。为最小宽度。3. 精确度。指在小数点后的浮点数位数。4. 类型:  % - 印出百分比符号，不转换； c - 整数转成对应的 ASCII 字符； d - 整数； f - 双精度数字； o - 整数转成八进位； s - 字符串； x - 整数转成小写十六进位； X - 整数转成大写十六进位。 |
| 2 | `参数` | 通用型 | - |  |

### 取打印机列表

> 英文名 `GetPrinterList` ｜ 返回值 **文本型**

枚举系统打印机列表。

### 取默认打印机

> 英文名 `GetDefaultPrinter` ｜ 返回值 **文本型**

取系统默认打印机。成功返回默认打印机名称，失败返回空。

### 设置默认打印机

> 英文名 `SetDefaultPrinter` ｜ 返回值 **逻辑型**

设置系统的默认打印机。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `打印机名称` | 文本型 | - | 要设置为默认打印机的名称。 |

### 打开打印机对话框

> 英文名 `OpenPrintSetDlg` ｜ 返回值 **整数型**

打开打印机设置对话框。返回1：点击了确定按钮；2：点击了取消按钮。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `对话框类型` | 整数型 | 可空 | 要打开的对话框类型。可以为以下两个常量值之一：0、#接口常量.标准打印设置对话框；1、#接口常量.打印对话框；2、#接口常量.打印属性对话框。默认值为0。 |
| 2 | `打印信息` | 库类型#11 | 可空 | 指定打印机初始信息。当打印对话框关闭后，该信息内容为用户在对话框中设置的数据。 |

