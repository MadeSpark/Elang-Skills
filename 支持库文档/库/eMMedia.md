# 多媒体支持库

- **库标识**: `eMMedia`
- **版本**: 3.0
- **GUID**: `824F144B108A4bcbB966F45670D42A00`
- **作者**: 大连大有吴涛易语言软件开发有限公司
- **主页**: http://www.eyuyan.com
- **文件**: `eMMedia`
- **可用命令数**: 81

## 数据类型

| 名称 | 说明 |
|---|---|
| `系统音量` | 本对象提供对控制Windows系统音量的支持 |
| `录音` | 本对象提供对Wave格式录音的支持 |
| `MIDI演奏` | 本对象提供对MIDI演奏的基本支持 |
| `录音音波` | 本对象提供对获取录音音波的支持 |
| `CD播放` | 本对象提供对CD播放的支持。注意：本对象是架构在Windows MCI基础之上的，本对象提供了"取别名"方法，您可以通过该方法所得的文本，通过Windows API mciSendString等命令和本对象一起控制MCI播放 |
| `媒体播放` | 本对象提供对媒体播放的支持，注意：本对象是架构在Windows MCI基础之上的，本对象提供了"取别名"方法，您可以通过该方法所得的文本，通过Windows API mciSendString等命令和本对象一起控制MCI播放。如果有媒体文件不能播放，请安装相应解码器 |
| `音量类型` |  |
| `音频转换` | 本对象提供了对音频格式转换的支持。目前支持wav到mp3，mp3到wav以及wav到其他支持格式的转换。部分功能需要DirectX支持，转换的成功与否依赖于您Windows系统中关于音频的编码器和解码器。 |

## 命令

### 打开

> 英文名 `Open` ｜ 返回值 **整数型**

同时打开系统的一个音量类型的音量控制和静音控制，返回值为下列之一：1、#全部打开成功；0、全部打开失败；-1、音量控制打开失败；-2、#静音打开失败

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `设备标识` | 整数型 | - | 该参数必须提供合法的设备标识，标识只能为本支持库的"音量类型"下的常量之一 |

### 是否打开

> 英文名 `IsOpen` ｜ 返回值 **逻辑型**

判断音量控制或静音控制是否打开，如打开返回真,否则返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `类型` | 整数型 | 有默认值 | 0、#音量；1、#静音； 其他值、#全部 |

### 关闭

> 英文名 `Close` ｜ 返回值 **逻辑型**

关闭音量控制或静音控制

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `类型` | 整数型 | 有默认值 | 0、#音量；1、#静音； 其他值、#全部 |

### 取静音

> 英文名 `GetMute` ｜ 返回值 **逻辑型**

如果当前处于静音状态返回真，否则返回假

### 置静音

> 英文名 `SetMute` ｜ 返回值 **逻辑型**

设置是否静音。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `静音` | 逻辑型 | - | 该参数设定是否静音 |

### 取最大音量

> 英文名 `GetVolumeMax` ｜ 返回值 **整数型**

获取当前音量控制可能被调整到的最大值

### 取最小音量

> 英文名 `GetVolumeMin` ｜ 返回值 **整数型**

获取当前音量控制可能被调整到的最小值

### 取音量

> 英文名 `GetVolume` ｜ 返回值 **逻辑型**

获取左声道和右声道的音量，成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `左声道音量` | 整数型 | 参考 | 该参数接收左声道的音量 |
| 2 | `右声道音量` | 整数型 | 参考 | 该参数接收右声道的音量 |

### 置音量

> 英文名 `SetVolume` ｜ 返回值 **逻辑型**

设置左声道和右声道的音量。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `左声道音量` | 整数型 | - | 要设置的左声道的音量 |
| 2 | `右声道音量` | 整数型 | - | 要设置的右声道的音量 |

### 取设备号

> 英文名 `GetDeviceID` ｜ 返回值 **整数型**

获取音量控制或静音控制的标识号

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `设备标识` | 整数型 | 有默认值 | 0表示音量，非0表示静音 |

### 取设备名

> 英文名 `GetDeviceName` ｜ 返回值 **文本型**

获取设备的名称

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `设备标识` | 整数型 | 有默认值 | 0表示音量，非0表示静音 |

### 录制

> 英文名 `Record` ｜ 返回值 **逻辑型**

开始录制。成功返回真，失败返回假

### 是否在录制

> 英文名 `IsRecording` ｜ 返回值 **逻辑型**

判断是否正在录制。成功返回真，失败返回假

### 暂停

> 英文名 `Pause` ｜ 返回值 **逻辑型**

暂停录制。成功返回真，失败返回假

### 继续

> 英文名 `Continue` ｜ 返回值 **逻辑型**

恢复一个被暂停的录制。成功返回真，失败返回假

### 停止

> 英文名 `Stop` ｜ 返回值 **逻辑型**

停止录制。成功返回真，失败返回假

### 置格式

> 英文名 `SetWaveFormat` ｜ 返回值 **逻辑型**

设置录音的格式，注意参数的正确性，成功返回真，失败返回假。注意：本方法必须在使用"录音"方法前使用

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `线路数` | 整数型 | 有默认值 | 本参数为录音的线路数，为以下常量值之一：1、#单声道；2、立体声双声道 |
| 2 | `采样率` | 整数型 | 有默认值 | 本参数为每秒采样率(hertz)，为以下常量值之一：8000、#8000HZ；11025、#11025HZ；22050、#22050HZ；44100、#44100HZ |
| 3 | `采样大小` | 整数型 | 有默认值 | 本参数为采样大小，以位为单位，为以下常量值之一：8、#8位；16、#16位 |

### 取格式

> 英文名 `GetWaveFormat` ｜ 返回值 **逻辑型**

获取录音格式

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `线路数` | 整数型 | 参考 | 本参数为录音的线路数，为以下常量值之一：1、#单声道；2、立体声双声道 |
| 2 | `采样率` | 整数型 | 参考 | 本参数为每秒采样率(hertz)，为以下常量值之一：11025、#11025HZ；22050、#22050HZ；44100、#44100HZ |
| 3 | `采样大小` | 整数型 | 参考 | 本参数为采样大小，以位为单位，为以下常量值之一：8、#8位；16、#16位 |

### 保存文件

> 英文名 `SaveFile` ｜ 返回值 **逻辑型**

以*.wav格式保存当前录制的声音到指定文件中，成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `保存文件名` | 文本型 | - |  |
| 2 | `覆盖已有文件` | 逻辑型 | 有默认值 |  |

### 取位置

> 英文名 `GetPosition` ｜ 返回值 **整数型**

返回当前已录制的字节的(Bytes)位置, 失败返回-1

### 取设备数量

> 英文名 `GetDeviceNum` ｜ 返回值 **整数型**

获取当前可以使用MIDI设备的数量

### 取设备名

> 英文名 `GetDeviceName` ｜ 返回值 **文本型**

获取指定MIDI设备的名称

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `设备序号` | 整数型 | - | 设备序号从0至最大设备数减1，最大设备数可以用"取设备数量"命令获取 |

### 打开

> 英文名 `Open` ｜ 返回值 **逻辑型**

通过MIDI设备序号打开指定的MIDI设备，成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `设备序号` | 整数型 | - | 设备序号从0至最大设备数减1，最大设备数可以用"取设备数量"命令获取 |

### 是否打开

> 英文名 `IsOpen` ｜ 返回值 **逻辑型**

判断MIDI设备是否打开，如打开返回真，否则返回假

### 关闭

> 英文名 `Close` ｜ 返回值 **逻辑型**

关闭当前MIDI设备，成功返回真，否则返回假

### 置乐器

> 英文名 `SetInstrument` ｜ 返回值 **逻辑型**

以指定的乐器序号设置当前MIDI设置要演奏的乐器，成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `乐器序号` | 整数型 | - | 乐器序号范围为1至最大支持乐器数，最大支持乐器数可以通过"取乐器数量"方法取得 |
| 2 | `通道` | 整数型 | 可空 | 该参数指定设置乐器序号的通道，从0 ~ 15 |

### 取乐器序号

> 英文名 `GetInstrument` ｜ 返回值 **整数型**

获取指定通道当前使用的乐器序号，乐器序号从1开始

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `通道` | 整数型 | 可空 | 该参数指定乐器序号的通道，从0 ~ 15 |

### 取乐器数量

> 英文名 `GetInstrumentNum` ｜ 返回值 **整数型**

获取当前所有支持的乐器的数量

### 取乐器名

> 英文名 `GetInstrumentName` ｜ 返回值 **文本型**

通过指定乐器序号获取乐器名称

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `乐器序号` | 整数型 | - | 乐器序号范围为1至最大支持乐器数，最大支持乐器数可以通过"取乐器数量"方法取得 |

### 奏

> 英文名 `NoteOn` ｜ 返回值 **逻辑型**

演奏一个音符。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `音符` | 字节型 | - | 该参数范围为0至127。0表示音C，1表示C#，依次类推，127表示第10个8度的127 |
| 2 | `通道` | 整数型 | 可空 | 该参数指定音符演奏的通道，从0 ~ 15 |

### 停奏

> 英文名 `NoteOff` ｜ 返回值 **逻辑型**

停止演奏一个音符。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `音符` | 字节型 | - | 该参数范围为0至127。0表示音C，1表示C#，依次类推，127表示第10个8度的127 |
| 2 | `通道` | 整数型 | 可空 | 该参数指定音符停止演奏的通道，从0 ~ 15 |

### 打开

> 英文名 `Open` ｜ 返回值 **逻辑型**

打开设备接收声波输入，成功返回真，失败返回假

### 是否打开

> 英文名 `IsOpen` ｜ 返回值 **逻辑型**

判断设备是否打开，如打开返回真，否则返回假

### 关闭

> 英文名 `Close` ｜ 返回值 **逻辑型**

关闭已打开设备，成功返回真，失败返回假

### 取上限

> 英文名 `GetUpBound` ｜ 返回值 **整数型**

取可以接收的声波值的最大频段（也可以理解为数组上限）

### 取声波值

> 英文名 `GetWaveValue` ｜ 返回值 **逻辑型**

获取指定频段的声波值。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `声波值` | 短整数型 | 参考 | 接收声波值的变量 |
| 2 | `复制序号` | 整数型 | - | 要声波值的序号，序号的范围为0至最大频段减1，最大频段可以通过"取上限"方法获取 |

### 打开

> 英文名 `Open` ｜ 返回值 **逻辑型**

打开指定的光驱。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `光驱盘符` | 文本型 | - | 该参数指定要控制的光驱的盘符，必须为如下格式: "H:\"，以下格式是非法的： "H", "H:"；如果提供空文本，表示打开默认第一个光驱 |

### 关闭

> 英文名 `Close` ｜ 返回值 **逻辑型**

关闭光驱。成功返回真，失败返回假

### 开仓门

> 英文名 `DoorOpen` ｜ 返回值 **逻辑型**

打开光驱仓门。成功返回真，失败返回假

### 关仓门

> 英文名 `DoorClose` ｜ 返回值 **逻辑型**

关闭光驱仓门。成功返回真，失败返回假

### 是否有CD

> 英文名 `HasCD` ｜ 返回值 **逻辑型**

判断当前光驱中是否有光盘，如果有返回真，否则返回假

### 取状态

> 英文名 `GetMode` ｜ 返回值 **整数型**

返回当前光驱的状态，为以下常量值之一：0、#未知状态；1、#播放状态；2、#停止状态；3、#弹出状态和无光盘状态

### 播放

> 英文名 `Play` ｜ 返回值 **逻辑型**

开始播放，成功返回真，失败返回假

### 暂停

> 英文名 `Pause` ｜ 返回值 **逻辑型**

暂停播放，成功返回真，失败返回假

### 恢复

> 英文名 `Resume` ｜ 返回值 **逻辑型**

恢复播放，成功返回真，失败返回假

### 停止

> 英文名 `Stop` ｜ 返回值 **逻辑型**

停止播放，成功返回真，失败返回假

### 跳到

> 英文名 `JumpTo` ｜ 返回值 **逻辑型**

跳到指定曲目的指定位置。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `曲目号` | 整数型 | - | 该参数为要转到的曲目号 |

### 取位置

> 英文名 `GetPosition` ｜ 返回值 **逻辑型**

获取当前的播放位置，成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `曲目号` | 整数型 | 参考 | 该参数接收曲目号 |
| 2 | `位置` | 整数型 | 参考 | 该参数为当前播放在相应曲目中的位置，以毫秒为单位 |

### 取曲目数

> 英文名 `GetCount` ｜ 返回值 **整数型**

获取当前CD光盘中的曲目数量

### 取长度

> 英文名 `GetLength` ｜ 返回值 **整数型**

获取指定曲目的时间长度，以毫秒为单位

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `曲目号` | 整数型 | - | 该参数为要取长度的曲目号 |

### 取总长度

> 英文名 `GetFullLength` ｜ 返回值 **整数型**

获取全部曲目的时间长度的总和，以毫秒为单位

### 取别名

> 英文名 `GetMCIAlias` ｜ 返回值 **文本型**

获取当前对象使用的MCI控制别名

### 打开

> 英文名 `Open` ｜ 返回值 **逻辑型**

打开指定媒体文件。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `文件名` | 文本型 | - | 该参数为要打开的媒体文件名 |

### 关闭

> 英文名 `Close` ｜ 返回值 **逻辑型**

关闭当前播放的媒体。成功返回真，失败返回假

### 是否视频

> 英文名 `IsVideo` ｜ 返回值 **逻辑型**

判断当前播放的内容是否是视频。如果是返回真，否则返回假

### 取句柄

> 英文名 `GetHwnd` ｜ 返回值 **整数型**

获取当前视频播放窗口的Windows 窗口句柄

### 置句柄

> 英文名 `SetHwnd` ｜ 返回值 **逻辑型**

设置当前视频播放窗口的Windows 窗口句柄，成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `窗口句柄` | 整数型 | - | 要设置的窗口句柄 |

### 取状态

> 英文名 `GetMode` ｜ 返回值 **整数型**

获取当前的播放状态，为以下常量值之一：0、#未知状态；1、#播放状态；2、#停止状态；3、#暂停状态

### 取长度

> 英文名 `GetLength` ｜ 返回值 **整数型**

获取当前媒体文件的长度

### 取位置

> 英文名 `GetPosition` ｜ 返回值 **整数型**

获取当前的播放位置

### 取总时间

> 英文名 `GetTotalSec` ｜ 返回值 **整数型**

以秒为单位获取总播放时间

### 取比率

> 英文名 `GetFrameRate` ｜ 返回值 **整数型**

获取每一帧的比率

### 播放

> 英文名 `Play` ｜ 返回值 **逻辑型**

从指定位置开始播放。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `播放位置` | 整数型 | 有默认值 | 该参数为播放位置，以毫秒为单位。默认为-1，表示从当前处播放 |
| 2 | `播放窗口` | 整数型 | 可空 | 指定播放窗口的窗口句柄。如果未指定此参数，或句柄非法，则自动使用之前“置句柄”指定的窗口句柄。 |

### 暂停

> 英文名 `Pause` ｜ 返回值 **逻辑型**

暂停播放。成功返回真，失败返回假

### 停止

> 英文名 `Stop` ｜ 返回值 **逻辑型**

停止播放。成功返回真，失败返回假

### 取别名

> 英文名 `GetMCIAlias` ｜ 返回值 **文本型**

获取当前对象使用的MCI别名

### 取起始时间

> 英文名 `GetTrackBeginTime` ｜ 返回值 **整数型**

获取指定曲目的起始时间

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `曲目号` | 整数型 | 有默认值 | 该参数为曲目的序号 |

### 初始化

> 英文名 `Init` ｜ 返回值 **逻辑型**

初始化音频转换。成功返回真，失败返回假。在使用本对象前，必须保证成功调用过本方法

### 取编码数

> 英文名 `GetCodeCount` ｜ 返回值 **整数型**

获取当前编码器可用的编码格式数量。失败返回0

### 取编码信息

> 英文名 `GetCodeInfo` ｜ 返回值 **逻辑型**

根据当前编码器的指定编码格式索引获取编码格式的相关信息。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `编码索引` | 整数型 | - | 编码格式的索引，从0 ~ 编码数量 - 1 |
| 2 | `是否为单声道` | 逻辑型 | 参考 | 判断是否为单声道，如不是则是双声道 |
| 3 | `采样频率` | 整数型 | 参考 | 每秒的采样频率，以kHz为单位 |
| 4 | `音频大小` | 整数型 | 参考 | 每秒音频的大小，以kbps为单位 |

### 转换

> 英文名 `Convert` ｜ 返回值 **逻辑型**

转换音频文件。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `源文件名` | 文本型 | - | 要转换的源文件文件名 |
| 2 | `目标文件名` | 文本型 | - | 要转换的目的文件文件名，先前的文件将被复写 |
| 3 | `编码索引` | 整数型 | - | 编码格式的索引，从0 ~ 编码数量 - 1 |

### 取编码器数

> 英文名 `GetEncoderCount` ｜ 返回值 **整数型**

获取当前可用的编码器的数量。失败返回0

### 取编码器名

> 英文名 `GetEncoderName` ｜ 返回值 **文本型**

获取指定编码器索引对应的编码器名称。失败返回空文本

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `编码器索引` | 整数型 | - | 编码器索引从0 ~ 编码器数量 -1 |

### 取当前编码器

> 英文名 `GetCurrentEncoder` ｜ 返回值 **文本型**

获取当前使用的编码器的名称。

### 置当前编码器

> 英文名 `SetCurrentEncoder` ｜ 返回值 **逻辑型**

设置当前使用的编码器的名称。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `编码器名称` | 文本型 | - | 编码器的名称。 |

### 置音量

> 英文名 `SetVolume` ｜ 返回值 **逻辑型**

设置当前播放媒体的音量，不影响系统音量。左右声道中只要有一个设置失败，就会返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `左声道音量` | 整数型 | 可空 | 范围为0-100，0为静音，100为最大音量。如果本参数被省略或超出范围，则不修改此声道音量。 |
| 2 | `右声道音量` | 整数型 | 可空 | 范围为0-100，0为静音，100为最大音量。如果本参数被省略或超出范围，则不修改此声道音量。 |

### 取音量

> 英文名 `GetVolume` ｜ 返回值 **逻辑型**

获取当前播放音量（非系统音量）。左右声道中只要有一个获取失败，就会返回假。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `左声道音量` | 整数型 | 可空、参考 | 范围为0-100，0为静音，100为最大音量。如果本参数被省略，则不返回此声道音量。 |
| 2 | `右声道音量` | 整数型 | 可空、参考 | 范围为0-100，0为静音，100为最大音量。如果本参数被省略，则不返回此声道音量。 |

