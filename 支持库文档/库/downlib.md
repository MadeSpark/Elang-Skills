# 网络传送支持库

- **库标识**: `downlib`
- **版本**: 3.1
- **GUID**: `80CF4A6B3E09425bA57935A3A0E4C473`
- **作者**: 大连大有吴涛易语言软件开发有限公司
- **主页**: http://www.eyuyan.com
- **文件**: `downlib`
- **可用命令数**: 66

本支持库实现对多种协议断点续传下载与FTP上传的支持，并提供了丰富的设置方式。当前版本支持HTTP、FTP、MMS多线程下载及断点续传，以及对RTSP协议的单线程下载及断点续传。用户可以根据各自的主机及网络情况进行设置，本支持库可以运行在linux下，详细情况参见例程。注意：当前版本使用了映射文件模式，并且是一次全部映射的方法，所以最大传送不超过一次映射文件的长度。

## 数据类型

| 名称 | 说明 |
|---|---|
| `下载对象` |  |
| `FTP上传对象` |  |
| `FTP辅助对象` |  |
| `任务参数` |  |
| `传送常量` | 本数据类型包括了下载及FTP上传对象用到的常量 |

## 命令

### 网络通讯设置

> 英文名 `InitDownLib` ｜ 返回值 **逻辑型**

初始化下载支持库，进行基本设置.

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `写入文件大小` | 整数型 | 有默认值 | 本参数指定每次写入文件的指定大小，默认为20480, 注意:如果待下载文件的总长度小于此值，那么将不能使用多个线程下载 |
| 2 | `重连时间` | 整数型 | 有默认值 | 出错时连接服务器间隔时间，默认为0 |
| 3 | `重连次数` | 整数型 | 有默认值 | 出错时重复连接服务器次数，默认为0 |

### 设重试时间

> 英文名 `SetTautologyTime`

设置重新尝试连接的时间

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `重试时间` | 整数型 | 有默认值 | 尝试重新连接的时间 |

### 取总下载速度

> 英文名 `GetAllRate` ｜ 返回值 **整数型**

获得所有任务的下载速度

### 增加新任务

> 英文名 `AddDownFile` ｜ 返回值 **逻辑型**

增加一个下载文件的任务,当前支持HTTP，FTP，MMS，RTSP协议。成功返回真，失败返回假。注意：默认线程原则上没有限制。但使用时应根据具体情况设置。否则不但起不到提高速度的作用还会影响系统的稳定

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `配置信息` | 传送常量 | - | 配置任务相关信息 |

### 停止指定任务

> 英文名 `StopDownFile` ｜ 返回值 **逻辑型**

停止一个下载文件的任务,当前支持HTTP，FTP协议。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `任务数据` | 字节集型 | 参考 | 本参数是用于保存下载任务的相关数据的 |
| 2 | `执行方式` | 整数型 | 有默认值 | 本参数无意义 |

### 继续下载任务

> 英文名 `ContinueDownFile` ｜ 返回值 **逻辑型**

继续一个下载文件的任务,当前支持HTTP，FTP，MMS，RTSP协议。成功返回真，失败返回假。注意：继续下载时默认线程数不是根据任务参数中的默认线程数进行设置的，而是根据任务信息中的没有完成的断点信息的数量进行设置的。断点的数量则是根据任务自本次运行以前开启的线程的总数减去已经完成下载任务的线程数获得的

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `配置信息` | 传送常量 | - | 配置任务相关信息 |
| 2 | `任务数据` | 字节集型 | - | 本参数是通过“停止任务”命令所获得的任务数据 |

### 增加线程

> 英文名 `AddDownThread` ｜ 返回值 **逻辑型**

增加一个下载线程,当前支持HTTP，FTP，MMS协议。如果最大块的长度小于接收大小的2倍时将不能开启新线程注意：下载线程原则上可以无限增加，但是请您根据您的实际情况谨慎使用。不要过多或过与频繁的增加线程，否则不但不能提高效率，相反会影响系统的稳定

### 减少线程

> 英文名 `DecreaseDownThread` ｜ 返回值 **逻辑型**

减少一个下载线程, 当前支持HTTP，FTP，MMS协议。当只有一个下载线程工作时，将不能结束这个线程

### 限制速度

> 英文名 `SetDownRate` ｜ 返回值 **逻辑型**

限制下载速度。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `速度` | 整数型 | - | 限制后的速度，单位为 字节/秒 |

### 取下载速度

> 英文名 `GetDownRate` ｜ 返回值 **整数型**

获得下载速度，本命令获得的速度为即时速度。如，每间隔1秒调用一次本命令，那么获得的值表示1秒内收到的数据,间隔2秒则表示2秒内收到的数据

### 取已下载长

> 英文名 `GetDownFileSize` ｜ 返回值 **整数型**

获得任务已下载大小

### 取任务数据

> 英文名 `GetThreadSize` ｜ 返回值 **逻辑型**

获得任务数据，在指定的时间内执行本命令来取得任务数据。如果下载任务出现异常，可以通过“继续下载任务”来继续任务

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `任务数据` | 字节集型 | 参考 | 本参数是用于保存下载任务的相关数据的 |

### 取重试次数

> 英文名 `GetTautologyCount` ｜ 返回值 **整数型**

获得重试次数

### 取对象信息

> 英文名 `GetObjInfo` ｜ 返回值 **整数型**

获得下载对象信息,通常用于多个对象使用相同的日志或交互回调函数时，在日志或交互回调函数中确定对象，如果失败返回-1

### 取线程信息

> 英文名 `GetThreadInfo` ｜ 返回值 **逻辑型**

获得线程信息，通常用于多个对象使用相同的日志或交互回调函数时，在日志或交互回调函数中确定线程

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `线程信息数组` | 整数型 | - | 本参数是用于保存线程信息数组 |

### 取任务数据项

> 英文名 `GetStopDownObjInfo` ｜ 返回值 **整数型**

获得通过“停止指定任务”命令返回的任务信息中的各类数据。失败返回-1

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `任务数据` | 字节集型 | - | 本参数是通过“停止任务”命令所获得的任务数据 |
| 2 | `数据类型` | 整数型 | - | 需要获得的任务信息的类型。如果”类型参数“作为指定具体断点序号使用，那么它从1开始不大于断点个数,1 待下载文件总长度，2 已下载总长度,3 断点个数,4 指定断点开始下载位置,类型参数为序号,5 指定断点待下载长度,类型参数为序号,6 指定断点已下载长度,类型参数为序号 |
| 3 | `类型参数` | 整数型 | 有默认值 | 根据”数据类型“参数的不同而不同。当需要查询的信息类型没有指定本参数的意义时，本参数无意义 |

### 取本地文件路径

> 英文名 `GetFilePath` ｜ 返回值 **文本型**

获得待下载文件保存在本地的路径。本命令在收到”存在同名文件“消息时使用可以获得已经存在的同名文件的路径。本命令在收到“下载上传开始”消息后使用可以获得当前下载的文件保存在本地的准确路径

### 设新文件名

> 英文名 `SetNewName` ｜ 返回值 **逻辑型**

本命令用来设置新的本地文件名。本命令必须是在处理“存在同名文件”这个消息的过程中使用。并且返回 #设置新名

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `新文件名` | 文本型 | - | 新的本地文件名 |

### 上传文件

> 英文名 `UpDataFile` ｜ 返回值 **逻辑型**

增加一个上传文件的任务,当前支持FTP协议。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `配置信息` | 传送常量 | - | 配置任务相关信息 |

### 停止上传

> 英文名 `StopUpData` ｜ 返回值 **逻辑型**

停止一个上传文件的任务,当前支持FTP协议。成功返回真，失败返回假

### 限制速度

> 英文名 `SetRate` ｜ 返回值 **逻辑型**

限制上传速度。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `速度` | 整数型 | - | 指定上传速度 |

### 取上传速度

> 英文名 `GetRate` ｜ 返回值 **整数型**

获得上传速度,本命令获得的速度为即时速度。如，每间隔1秒调用一次本命令，那么获得的值表示1秒内收到的数据间隔2秒则表示2秒内收到的数据

### 取已上传长

> 英文名 `GetUpDataSize` ｜ 返回值 **整数型**

获得任务已上传大小

### 取重试数

> 英文名 `GetUpDataTautologyCount` ｜ 返回值 **整数型**

获得重试次数

### 设新文件名

> 英文名 `SetNewName` ｜ 返回值 **逻辑型**

本命令用来设置新的上传文件名

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `新文件名` | 文本型 | 有默认值 | 上传到服务器的文件的新名 |

### 取对象信息

> 英文名 `GetFtpObjInfo` ｜ 返回值 **整数型**

获得FTP上传对象信息,通常用于多个FTP上传对象使用相同的日志或交互回调函数时，在日志或交互回调函数中确定FTP上传对象, 如果失败返回-1

### 连接FTP服务器

> 英文名 `FtpLogin` ｜ 返回值 **逻辑型**

连接到互联网上指定地址处的FTP服务器。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `FTP服务器地址` | 文本型 | 有默认值 | 本参数指定FTP服务器在互联网上的地址，格式为 ftp://域名或ip/ |
| 2 | `用户名` | 文本型 | 有默认值 | 本参数提供用作通过FTP服务器用户验证的用户名称，如果被省略，默认值为匿名用户名“anonymous” |
| 3 | `密码` | 文本型 | 有默认值 | 本参数提供用作通过FTP服务器用户验证的密码，如果被省略，默认值为空文本 |
| 4 | `端口` | 整数型 | 有默认值 | 本参数提供欲连接到FTP服务器上的端口号，如果被省略，默认值为标准端口 21 |
| 5 | `日志回调函数` | 子程序指针型 | 可空 | 注册一个回调函数，系统产生日志时会调用此函数通知用户,如果为0则不能接收日志，默认为0。回调函数需要有5个参数，第一个参数（整数型），线程信息，第二个参数（文本型），消息产生的时间。第三个参数（文本型），消息的正文。第四个参数（整数型），消息类型，参见”传送常量“第五个参数（整数型），对象信息，可以通过“取对象信息”获得下载或上传对象的信息与本参数比较，从而确定本消息属于哪个对象 |
| 6 | `超时` | 整数型 | 可空 | 登录过程中数据交互的超时时间,单位为毫秒，如果忽略则默认为2000 |

### 断开FTP服务器

> 英文名 `FtpQuit` ｜ 返回值 **逻辑型**

断开通过“连接FTP服务器”命令建立的到互联网上FTP服务器的连接

### 删除文件

> 英文名 `FtpDelFile` ｜ 返回值 **逻辑型**

删除FTP服务器上的指定文件。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `欲删除的FTP文件` | 文本型 | - | 本参数指定在FTP服务器上欲删除文件的路径 |

### 文件改名

> 英文名 `FtpReFileName` ｜ 返回值 **逻辑型**

更改FTP服务器上指定文件的名称。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `欲改名的FTP文件` | 文本型 | - | 本参数指定FTP服务器上欲改名文件的路径 |
| 2 | `新文件名称` | 文本型 | - | 本参数指定欲改为的文件名称 |

### 创建目录

> 英文名 `FtpCreateDir` ｜ 返回值 **逻辑型**

在FTP服务器上指定位置处创建新的目录。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `FTP目录路径` | 文本型 | - | FTP目录路径 |

### 删除目录

> 英文名 `FtpDelDir` ｜ 返回值 **逻辑型**

删除FTP服务器上的指定目录。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `FTP目录路径` | 文本型 | - | FTP目录路径 |

### 置现行目录

> 英文名 `FtpSetCurrentDir` ｜ 返回值 **逻辑型**

设置FTP服务器上的当前目录，设置后可以在其他FTP命令中使用相对路径来指定文件。成功返回真，失败返回假

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `FTP目录路径` | 文本型 | - | FTP目录路径 |

### 取现行目录

> 英文名 `FtpGetCurrentDir` ｜ 返回值 **文本型**

返回FTP服务器上的当前目录。成功返回目录路径，失败返回空文本

### 取目录列表

> 英文名 `FtpGetDirList` ｜ 返回值 **整数型**

返回FTP服务器上指定目录内的所有匹配文件和子目录信息。成功返回被找到的文件和子目录的数目，失败返回 -1失败的原因有可能是FTP服务器的目录结构暂时不被支持

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `欲列出的文件` | 文本型 | 可空 | 可以包含路径，可以使用多字符 (*) 和单字符 (?) 通配符来指定多重文件及目录 |
| 2 | `存放文件名的数组变量` | 文本型 | 可空 | 提供参数数据时只能提供变量数组。在命令执行完毕后，本变量数组内被顺序填入所找到的匹配文件和子目录名称。变量数组内原有数据被全部销毁，变量数组的维数被自动调整为所找到的文件数目 |
| 3 | `存放文件属性的数组变量` | 整数型 | 可空 | 当前不被支持,留做以后扩展 |
| 4 | `存放文件尺寸的数组变量` | 长整数型 | 可空 | 提供参数数据时只能提供变量数组。在命令执行完毕后，本变量数组内被顺序填入所找到的匹配文件的尺寸，并与文件名数组成员一一对应 |
| 5 | `存放文件时间的数组变量` | 日期时间型 | 可空 | 提供参数数据时只能提供变量数组。在命令执行完毕后，本变量数组内被顺序填入所找到的匹配文件的最后修改时间，并与文件名数组成员一一对应。变量数组内原有数据被全部销毁，变量数组的维数被自动调整为所找到的文件数目 |

### 移动文件

> 英文名 `FtpMoveFile` ｜ 返回值 **逻辑型**

移动文件或目录到新的位置

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `旧文件路径` | 文本型 | - | 旧文件路径 |
| 2 | `新文件路径` | 文本型 | - | 新文件路径 |

### 执行FTP命令

> 英文名 `FtpRunCmd` ｜ 返回值 **整数型**

执行一条FTP命令, 返回服务器返回的信息代码。如果执行失败返回0。 本命令只有通过“连接FTP服务器”命令连接到FTP服务器以后才能使用。

| # | 参数名 | 类型 | 修饰 | 说明 |
|---|---|---|---|---|
| 1 | `FTP命令` | 文本型 | - | 欲执行的FTP命令 |
| 2 | `返回命令数据` | 文本型 | 可空、参考 | 执行FTP命令后服务器返回的命令文本，最大支持返回51200字节。 |

