.版本 2

.程序集 程序集1
.程序集变量 输出文件号, 整数型
.程序集变量 目录总数, 整数型
.程序集变量 文件总数, 整数型
.程序集变量 最大层数, 整数型

.子程序 _启动子程序, 整数型, , 本子程序在程序启动后最先执行
    .局部变量 输出路径, 文本型

    最大层数 ＝ 3
    输出路径 ＝ 取运行目录 () ＋ “\C盘结构.txt”
    输出文件号 ＝ 打开文件 (输出路径, #重写, )
    
    ' 「判断」：判断成功 ＝ 打不开输出文件，报错退出；
    '           判断失败（.默认）＝ 一切正常，走完整流程。
    .判断开始 (输出文件号 ＝ 0)
        输出调试文本 (“错误：无法创建输出文件 ” ＋ 输出路径)
        返回 (0)
    .默认
        写文本行 (输出文件号, “C 盘目录结构（最多下钻 ” ＋ 到文本 (最大层数) ＋ “ 层）”)
        写文本行 (输出文件号, “”)
        列目录 (“C:\”, 1)
        关闭文件 (输出文件号)
        输出调试文本 (“完成：共 ” ＋ 到文本 (目录总数) ＋ “ 个目录、” ＋ 到文本 (文件总数) ＋ “ 个文件。”)
        输出调试文本 (“结果已写入 ” ＋ 输出路径)
    .判断结束
    
    返回 (0)

.子程序 列目录, , , 递归列出目录内容；当前层从 1 开始，到 最大层数 为止不再下钻
    .参数 目录路径, 文本型
    .参数 当前层, 整数型
    .局部变量 名称, 文本型
    .局部变量 缩进, 文本型
    .局部变量 子目录表, 文本型, , "0"
    .局部变量 本层文件数, 整数型
    .局部变量 属性, 整数型
    .局部变量 i, 整数型

    缩进 ＝ 取重复文本 (当前层 － 1, “    ”)
    写文本行 (输出文件号, 缩进 ＋ “【” ＋ 目录路径 ＋ “】”)
    
    ' ① 先把本层的【真子目录】一次性收进数组。
    '    注意：「寻找文件」第 2 参数给 #子目录 时，返回的是本层全部条目（文件也在内），
    '    并不是“只要子目录”；要筛出目录必须再用 取文件属性() ＋ 位与(…, #子目录) ≠ 0 过滤。
    '    ⚠ 取文件属性失败（文件被系统独占等）返回 -1，而 位与(-1, #子目录) ≠ 0 恒成立，
    '      会把 hiberfil.sys 这类文件误判成目录 —— 所以必须先排除 -1。实测确认。
    '    另外「寻找文件」的迭代位置记在子程序上，边找边递归会被内层调用冲掉状态，
    '    造成漏项 / 提前结束且不报错，所以必须“先收完、再递归”。
    名称 ＝ 寻找文件 (补反斜杠 (目录路径) ＋ “*”, #子目录)
    .判断循环首 (名称 ≠ “”)
        属性 ＝ 取文件属性 (补反斜杠 (目录路径) ＋ 名称)
        .如果真 (属性 ≠ -1)
            .如果真 (位与 (属性, #子目录) ≠ 0)
                加入成员 (子目录表, 名称)
            .如果真结束
            
        .如果真结束
        
        名称 ＝ 寻找文件 ()
    .判断循环尾 ()
    
    ' ② 列出本层子目录（尾部加 \ 以便与文件区分）
    .计次循环首 (取数组成员数 (子目录表), i)
        目录总数 ＝ 目录总数 ＋ 1
        写文本行 (输出文件号, 缩进 ＋ “    ” ＋ 子目录表 [i] ＋ “\”)
    .计次循环尾 ()
    
    ' ③ 列出本层文件（第 2 参数留空 ＝ 只列文件，不含子目录）
    名称 ＝ 寻找文件 (补反斜杠 (目录路径) ＋ “*”, )
    .判断循环首 (名称 ≠ “”)
        本层文件数 ＝ 本层文件数 ＋ 1
        文件总数 ＝ 文件总数 ＋ 1
        写文本行 (输出文件号, 缩进 ＋ “    ” ＋ 名称)
        名称 ＝ 寻找文件 ()
    .判断循环尾 ()
    
    ' ④ 「如果 / 否则」：本层收尾 —— 空目录给个标记，否则补一行空行作分隔
    .如果 (取数组成员数 (子目录表) ＝ 0 且 本层文件数 ＝ 0)
        写文本行 (输出文件号, 缩进 ＋ “    （空目录）”)
    .否则
        写文本行 (输出文件号, “”)
    .如果结束
    
    ' ⑤ 深度限制：已到 最大层数 就不再往下钻（C 盘全展开非常耗时）
    .如果真 (当前层 ＜ 最大层数)
        .计次循环首 (取数组成员数 (子目录表), i)
            列目录 (补反斜杠 (目录路径) ＋ 子目录表 [i], 当前层 ＋ 1)
        .计次循环尾 ()
    .如果真结束

.子程序 补反斜杠, 文本型, , 路径不以 \ 结尾时补上一个；已结尾则原样返回
    .参数 路径, 文本型
    
    ' 「如果 / 否则」：两种情况各走一支
    .如果 (取文本中间 (路径, 取文本长度 (路径), 1) ＝ “\”)
        返回 (路径)
    .否则
        返回 (路径 ＋ “\”)
    .如果结束
