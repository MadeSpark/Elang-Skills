.版本 2

.程序集 程序集1
.程序集变量 日志文件号, 整数型

.子程序 记日志, , , 追加一行
    .参数 内容, 文本型

    写文本行 (日志文件号, 内容)

.子程序 _启动子程序, 整数型, , 嵌套控制流压测
    .局部变量 路径, 文本型

    路径 ＝ 取运行目录 () ＋ “\嵌套测试.txt”
    日志文件号 ＝ 打开文件 (路径, #重写, )
    .判断开始 (日志文件号 ＝ 0)
        输出调试文本 (“无法创建 ” ＋ 路径)
        返回 (0)
    .默认
        压测一 ()
        压测二 ()
        压测三 ()
        关闭文件 (日志文件号)
        输出调试文本 (“嵌套压测完成”)
    .判断结束

    返回 (0)

.子程序 压测一, , , 计次循环 × 判断 × 如果真 四层嵌套
    .局部变量 i, 整数型
    .局部变量 j, 整数型
    .局部变量 素数个数, 整数型
    .局部变量 是否素数, 逻辑型

    素数个数 ＝ 0
    .计次循环首 (50, i)
        是否素数 ＝ 真
        .如果真 (i ≥ 2)
            .计次循环首 (i － 2, j)
                .如果真 (i ％ (j ＋ 1) ＝ 0)
                    是否素数 ＝ 假
                    跳出循环 ()
                .如果真结束

            .计次循环尾 ()
        .如果真结束

        .判断开始 (是否素数 且 i ＞ 2)
            素数个数 ＝ 素数个数 ＋ 1
            记日志 (“素数 ” ＋ 到文本 (i))
        .判断 (i ＝ 1)
            记日志 (“1 既不是素数也不是合数”)
        .默认
            记日志 (“合数 ” ＋ 到文本 (i))
        .判断结束

    .计次循环尾 ()

    记日志 (“── 50 以内素数共 ” ＋ 到文本 (素数个数) ＋ “ 个 ──”)

.子程序 压测二, , , 判断循环 × 变量循环 × 如果 × 如果真 五层嵌套
    .局部变量 行, 整数型
    .局部变量 列, 整数型
    .局部变量 某行文本, 文本型

    行 ＝ 1
    .判断循环首 (行 ≤ 9)
        某行文本 ＝ “”
        .变量循环首 (1, 行, 1, 列)
            .如果真 (列 ＜ 行)
                某行文本 ＝ 某行文本 ＋ 到文本 (行 × 列) ＋ “  ”
            .如果真结束

        .变量循环尾 ()

        记日志 (“第 ” ＋ 到文本 (行) ＋ “ 行： ” ＋ 某行文本)
        行 ＝ 行 ＋ 1
    .判断循环尾 ()

    记日志 (“── 九九乘法表结束 ──”)

.子程序 压测三, , , 循环判断(do-while) × 判断 × 到循环尾
    .局部变量 n, 整数型
    .局部变量 累计, 整数型

    n ＝ 0
    累计 ＝ 0
    .循环判断首 ()
        n ＝ n ＋ 1
        .判断开始 (n ％ 2 ＝ 0)
            到循环尾 ()
        .默认
            累计 ＝ 累计 ＋ n
        .判断结束

    .循环判断尾 (n ＜ 10)

    记日志 (“1~10 中奇数之和 ＝ ” ＋ 到文本 (累计))
    记日志 (“── 压测全部结束 ──”)
